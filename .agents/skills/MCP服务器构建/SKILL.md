---
name: MCP服务器构建
description: 当用户以 MCP服务器构建 名称要求在 super-agent-v3 中新增、维护或审查 MCP sidecar/tool/server 时使用。
aliases: ["@MCP服务器构建", "@MCP协议"]
---

# MCP 服务器构建

这是显式点名旧技能名时的兼容入口。实际规则以 `MCP协议` 技能为唯一事实源；完整读取 `.agents/skills/MCP协议/SKILL.md` 后执行，不在这里复制路径、transport、DAG 或验证规则。
