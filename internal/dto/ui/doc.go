// Package ui defines UI projection events and incremental patch payloads.
//
// The package owns display-boundary DTOs only. It must not access UI state,
// stores, providers, or runtime services.
package ui
