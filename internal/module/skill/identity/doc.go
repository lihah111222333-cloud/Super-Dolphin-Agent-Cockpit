// Package identity owns skill name, display-name, alias, and frontmatter identity
// rules.
//
// It must remain pure and must not own filesystem mirroring or provider runtime
// behavior.
package identity
