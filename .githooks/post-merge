#!/usr/bin/env bash
set -euo pipefail

if command -v make >/dev/null 2>&1 && command -v go >/dev/null 2>&1 && [ -f Makefile ]; then
  make project-map || echo "WARN: post-merge project map refresh failed; run make project-map manually" >&2
fi
