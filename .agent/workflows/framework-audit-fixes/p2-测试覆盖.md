---
description: 扩展现有测试 + 补齐真实空白区（非从零新建）
---

# P2: 测试覆盖补齐（等 P1 完成，可内部并行，~3h）

## 基线修正（v1→v2）

v1 假设这些包"零测试"，实际当前已有大量测试：

| 包 | 现有 _test.go | 现有 Test* | v1 说法 |
|----|:-----------:|:--------:|---------|
| `internal/store/*` | 6 | 33 | "全零" ❌ |
| `internal/module/thread` | 12 | 46 | "0 个" ❌ |
| `internal/provider/codexapp` | 7 | 24 | "0 个" ❌ |
| `internal/provider/claudecli` | 8 | 20 | "近零" ❌ |
| `internal/platform/rpc` | 9 | 37 | "近零" ❌ |

**因此 P2 不是"从零建 15+ 测试文件"，而是"扩展现有 + 补真实空白"。**

## 真实空白区（需新增测试）

| 空白区 | 原因 | 新建文件 |
|-------|------|---------|
| ~~`internal/store/thread/`~~ | ~~已有 `store_test.go` (2 个 Test*)~~ | ~~已存在，改为扩展~~ |
| `internal/module/turn/tracker.go` | P1-A 重写后需测试 | `tracker_test.go` |
| P1-B review/start handler | 新实现 | 扩展现有 `rpc_test.go` |
| P1-C binding 扩展方法 | 新增 Rebind/ListMap | 扩展现有 `store_test.go` |
| P1-D LogSink Start() | 新生命周期 | 扩展现有 `bus_test.go` |
| P1-E SafeGo | 新 helper | `safe_go_test.go` |
| P1-F LogIgnoredError | 新 helper | `log_error_test.go` |

## 需扩展的现有测试

| 现有测试文件 | 需扩展内容 |
|------------|----------|
| `cmd/mcp-orch/orchestration/*_test.go` | P1-A recovered trigger 清理后的回归 |
| `internal/platform/rpc/handler_test.go` | P1-B InvalidParams → CodeInvalidParams |
| `internal/module/turn/service_test.go` | P1-A tracker SM 迁移后的回归 |
| `internal/store/ailog/store_test.go` | P1-C keyword 下推 |
| `internal/store/dbquery/executor_test.go` | P1-C READ ONLY 加固 |

## 任务分配（可内部并行）

### T1: store 扩展 + P1-C 扩展（~0.75h）
- 扩展 `internal/store/thread/store_test.go`（已存在 2 个 Test*，补充 CRUD 覆盖）
- 扩展 `internal/store/binding/store_test.go`（Rebind/ListMap）
- 扩展 `internal/store/ailog/store_test.go`（keyword）
- 扩展 `internal/store/dbquery/executor_test.go`（READ ONLY）

### T2: turn tracker + orchestration 回归（~0.75h）
- 新建 `internal/module/turn/tracker_test.go`（SM 状态转换）
- 扩展 `cmd/mcp-orch/orchestration/*_test.go`（recovered trigger）

### T3: RPC + review/start + error mapping（~0.75h）
- 扩展 `internal/platform/rpc/handler_test.go`（CodeInvalidParams）
- 扩展 `internal/module/turn/*_test.go`（review/start）
- 新建或扩展 CapabilityError mapping 测试

### T4: helper 测试 + bus lifecycle（~0.75h）
- 新建 `internal/platform/shared/safe_go_test.go`
- 新建 `internal/platform/shared/log_error_test.go`
- 扩展 `internal/platform/bus/bus_test.go`（LogSink Start lifecycle）

## 死代码清理（必做）

- 新增或扩展测试后，用 `lsp_grep` 确认测试文件未再引用 P1 已删除的旧符号、旧错误码、旧状态字面量或旧事件名
- 确认新增测试文件的 import 路径和包名正确，避免复制现有测试时残留错误引用
- 对扩展现有测试的场景，删除被替换的旧断言、旧 helper 和失效 case，避免新旧测试分支并存
- 若测试辅助文件因目标符号删除而只剩空壳，及时删除空 helper、空 test case 或空测试文件

## 完成标准

- [ ] `internal/store/thread/` 测试已扩展（≥ 5 个 Test*）
- [ ] `internal/module/turn/tracker_test.go` 覆盖 SM 状态转换
- [ ] 所有 P1 新增/修改的代码有对应测试
- [ ] `go test ./internal/... -count=1` 全部通过
- [ ] `lsp_grep` 搜索旧模式关键字，全仓零残留
- [ ] `go vet ./...` 无 unused import 警告
- [ ] 无空函数/空文件/空目录残留

## 验证命令

```bash
go test ./internal/... -count=1 -timeout=120s
go test ./cmd/mcp-orch/... -count=1
go test ./internal/archtest/... -v

# 死代码清理验证
go vet ./...
```
