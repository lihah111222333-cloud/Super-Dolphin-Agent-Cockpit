---
description: 全量验证 + ⚠️ 维度快速清理 + 文档更新
---

# P3: 集成验证 + 清理（串行，~1.5h）

## 任务范围

### I1: 全量编译与测试验证（~0.5h）

```bash
go build ./internal/... ./cmd/mcp-orch/...
go vet ./...
go test ./... -count=1 -timeout=120s
go test ./internal/archtest/... -v
```

### I2: ⚠️ 维度快速清理（~0.5h）

| 维度 | 快速修复 | 说明 |
|------|---------|------|
| fx 悬空 provider | 删除 5 个无消费者的 typed emitter provider（`AgentEmitters/TurnEmitters/ToolEmitters/TaskEmitters/UIEmitters`）。`ThreadEmitters` 和 `WorkspaceEmitters` 有真实消费者，保留。 | `bus/module.go` + `bus/emitters.go` |
| 代码守卫 | `lifecycle.go` 当前 372 行（已有 `lifecycle_helpers.go`）。如 P1-F 修改后超 380，再拆 `lookupThreadMeta/lookupBindingCWD` 到 helpers | 仅在需要时执行 |
| import 方向 | dto 内部 shared 复用属设计决策，不改。wails 跨层 1 处评估是否可抽接口 | 低优先级 |
| two-zone DRY | **不处理**（P10 范围） | 明确延期 |

### I3: framework_audit.md 最终更新（~0.25h）

根据 P1-P2 修复结果，更新审查报告：

| 维度 | 修复前 | 修复后 | 条件 |
|------|--------|--------|------|
| stateless | ❌ | ⚠️ | recovered trigger 清理 + tracker 迁移完成 |
| jrpc2 | ❌ | ⚠️ | shape 关键 handler 对齐，但覆盖率仍 <60% |
| 错误处理 | ❌ | ⚠️ | 高风险吞错已改 LogIgnoredError |
| 测试覆盖 | ❌ | ⚠️ | 真实空白已补，但深度覆盖仍薄 |
| event bus | ⚠️ | ⚠️ | 零发布清零+LogSink，但 V2 method/前端消费仍未闭环 |
| lifecycle | ⚠️ | ⚠️→✅ | 前置条件：E2 shutdown 顺序验证通过或偏差已修复 |
| store parity | ⚠️ | ⚠️ | binding/AILog/DBQuery 三项已修，但 prompt/commandcard/systemlog/cwdlock/uipreference 漂移延期 |

> **注意**: event bus 不应判为 ✅（V2 method family 兼容、前端消费链、`agent-event` 双入口仍未闭环）

### I4: session-summary.md 更新（~0.25h）

更新 `docs/plans/迁移/session-summary.md` 中的迁移状态和未完成项。

## 死代码清理（必做）

- I2 fx 清理: 删除 5 个 emitter provider 后，用 `lsp_grep` 搜索 `AgentEmitters`、`TurnEmitters`、`ToolEmitters`、`TaskEmitters`、`UIEmitters`，确认全仓零残留
- I2 fx 清理: 如果 `emitters.go` 中对应构造函数已无引用，则同步删除构造函数和 provider wiring，避免 provider/constructor 半残留
- 通用: `go vet ./...` 必须无 unused import；集成验证结束后不允许留下空 provider、空 module hook、空文件或空目录

## 完成标准

- [ ] `go build ./...` 零错误
- [ ] `go vet ./...` 零警告
- [ ] `go test ./...` 全部通过
- [ ] archtest 8/8 通过
- [ ] `lsp_file diagnostics` 零错误
- [ ] framework_audit.md 已更新
- [ ] ❌ 不合规项从 4 个降至 0 个
- [ ] ✅ 合规项从 1 个升至 2 个（pgx + lifecycle）
- [ ] `lsp_grep` 搜索旧模式关键字，全仓零残留
- [ ] `go vet ./...` 无 unused import 警告
- [ ] 无空函数/空文件/空目录残留

## 验证命令

```bash
go build ./internal/... ./cmd/mcp-orch/...
go vet ./...
go test ./... -count=1 -timeout=120s
go test ./internal/archtest/... -v

# 死代码清理验证
go vet ./...
```
