---
description: 定义共享 helper，为 P1 并行任务打基础
---

# P0: 共享准备（串行，~0.5h）

## 执行步骤

### 1. 新建 LogIgnoredError helper（供 P1-F 使用）

**新建文件**: `internal/platform/shared/log_error.go`

```go
// LogIgnoredError logs an error that is intentionally not propagated.
func LogIgnoredError(logger *slog.Logger, msg string, err error) {
    if err != nil && logger != nil {
        logger.Warn(msg, "error", err)
    }
}
```

### 2. 新建 SafeGo helper（供 P1-E 使用）

**新建文件**: `internal/platform/shared/safe_go.go`

```go
// SafeGo launches a goroutine with panic recovery.
func SafeGo(logger *slog.Logger, fn func()) {
    go func() {
        defer func() {
            if r := recover(); r != nil && logger != nil {
                logger.Error("recovered panic in goroutine", "panic", r,
                    "stack", string(debug.Stack()))
            }
        }()
        fn()
    }()
}
```

### 3. 确认现有 timeout 常量（供 P1-E 参考）

`internal/platform/config/timeouts.go` 已有 `ShutdownTimeout = 15s`，`app.go` 已用 `fx.StopTimeout(ShutdownTimeout)`。
P1-E 需新增 `fx.StartTimeout` 但应复用现有命名模式（如 `StartupTimeout`），**不要** 创建重名常量。

## 完成标准

- [ ] `go build ./internal/platform/shared/...` 编译通过
- [ ] 不破坏现有 archtest（`TestSharedBudget`）
