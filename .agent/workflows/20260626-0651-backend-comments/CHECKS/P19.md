# P19 后端注释治理检查

## 状态

DONE_WITH_CONCERNS

## 改动文件

- `internal/platform/db/module.go`
  - 为 `splitMigrationBody` 补充中文函数级注释，说明按迁移分段标记拆分 SQL，并保留无标记旧脚本整体执行的事务语义。
- `internal/platform/db/sqlite/migrate.go`
  - 为 `applyMigration` 补充中文函数级注释，说明单事务执行、schema_migrations 记录、迁移自记录 marker 时不重复插入，以及失败回滚边界。
  - 为 `splitMigrationBody` 补充中文函数级注释，说明 sentinel 分段、旧脚本兼容和空分段忽略。
- `internal/platform/httpegress/policy.go`
  - 为 `unsafeHeaderNames` 补充传输层头黑名单说明。
  - 为 `isUnsafeHTTPHost` 补充 SSRF 主机判定边界说明，明确调用方必须拒绝而不是降级放行。

## LSP 工具使用

- `grep(text_search)`：按 P19 相关目录定位 Go 符号候选与注释缺口。
- `structure(document_symbol)`：查看 `hooks/dispatch_worker.go`、`db/module.go`、`db/sqlite/migrate.go`、`httpegress/policy.go` 的符号大纲。
- `file(open_file/read_file/diagnostics)`：打开 LSP 文件、精读目标函数上下文，并对改动文件运行诊断。
- `inspect(hover)`：查看 `hookDispatchWorker`、`isUnsafeHTTPHost` 的符号信息与现有注释呈现。
- `xref(references)`：检查 `newHookDispatchWorker` 的引用面，确认 hooks worker 注释上下文不需要跨文件逻辑改动。

## 验证

- `gofmt -w internal/platform/db/module.go internal/platform/db/sqlite/migrate.go internal/platform/httpegress/policy.go`：exit 0
- `./scripts/test_with_guard.sh internal/platform/db/module.go`：exit 0
- `./scripts/test_with_guard.sh internal/platform/db/sqlite/migrate.go`：exit 0
- `./scripts/test_with_guard.sh internal/platform/httpegress/policy.go`：exit 0
- `file(diagnostics)` for the three changed Go files：0 diagnostics
- Final guard loop over all three changed Go files：exit 0 for each file

## 备注

- Native subagent dispatch used; no persistent mcp-orch DAG ledger recorded.
- 指定 worktree 内不存在 `.agents/skills/注释规范/SKILL.md`，也不存在 `.agents/` 目录；已读取可用的仓库本地 `.agent/skills/后端/SKILL.md` 与 `naming_formatting.md`，并按已加载的注释规范执行。
- 未提交、未 stage。当前 worktree 另有 P19 外脏改动/未跟踪 workflow 文件，未触碰、未回滚。
