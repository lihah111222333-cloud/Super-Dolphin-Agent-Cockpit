# P10 后端注释治理报告

## 状态

DONE_WITH_CONCERNS

## 改动文件

- `internal/module/dashboard/dag_snapshot.go`
  - 补充 dashboard DAG snapshot 查询、run 查询、行映射、JSON/int64 规范化 helper 的中文函数级注释。
  - 注释强调快照缺失、行映射失败、核心 ID/版本/预算字段解析失败时 fail-fast，展示层 JSON fallback 只用于历史脏数据展示兼容。
- `internal/module/datasource_v2/pdf_extract.go`
  - 补充 datasource_v2 PDF stream 扫描、FlateDecode 解压、literal/hex string 解析、escape 处理、编码转换和文本片段规范化 helper 的中文函数级注释。
  - 注释说明 PDF 解析状态机边界、损坏 stream 阻断导入、非法文本片段跳过策略。

## LSP 工具使用

- `mcp__lsp.grep(text_search)`: 定位 `getDAGDetailFromSnapshot` 等目标符号。
- `mcp__lsp.structure(document_symbol)`: 查看 `dag_snapshot.go` 和 `pdf_extract.go` 文件大纲，确认缺口集中位置。
- `mcp__lsp.inspect(hover)`: 查看 `getDAGDetailFromSnapshot`、`extractPDFTextFromStream` 签名。
- `mcp__lsp.xref(references/call_hierarchy)`: 检查 snapshot helper 和 PDF stream helper 的调用/引用关系。
- `mcp__lsp.file(open_file/read_file/diagnostics)`: 打开并精读目标代码区间，最终 diagnostics 返回 0 条诊断。

## 验证

- `./scripts/test_with_guard.sh internal/module/dashboard/dag_snapshot.go` -> exit 0。
- `./scripts/test_with_guard.sh internal/module/datasource_v2/pdf_extract.go` -> exit 0。
- `gofmt -w internal/module/dashboard/dag_snapshot.go internal/module/datasource_v2/pdf_extract.go` -> exit 0。
- `mcp__lsp.file diagnostics internal/module/dashboard/dag_snapshot.go internal/module/datasource_v2/pdf_extract.go` -> 0 diagnostics。
- `./scripts/test_with_guard.sh internal/module/dashboard/dag_snapshot.go internal/module/datasource_v2/pdf_extract.go` -> exit 0。
- `./scripts/test_with_guard.sh $(tr '\n' ' ' < .agent/workflows/20260626-0651-backend-comments/PARTITIONS/P10.files)` -> exit 0。
- `git diff --check -- internal/module/dashboard/dag_snapshot.go internal/module/datasource_v2/pdf_extract.go` -> exit 0。

## 约束与关注点

- 未提交、未 stage。
- 只修改了当前 `P10.files` 中的 Go 文件和本报告文件。
- `mcp-orch tools unavailable`: 当前会话未暴露 `task_create_dag`、`task_start_node`、`task_update_node`。
- worktree 内 `.agents/skills/注释规范/SKILL.md` 和 `.agent/skills/注释规范/SKILL.md` 不存在；已读取主 checkout 的 `.agents/skills/注释规范/SKILL.md` 并按该规范执行。
