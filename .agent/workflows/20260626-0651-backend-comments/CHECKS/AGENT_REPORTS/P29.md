# P29 历史注释复扫与低样板修复

## 状态

DONE

## 范围

- 生产清单：`.agent/workflows/20260626-0651-backend-comments/PARTITIONS/P29.files`。
- 测试清单：`/tmp/backend-comments-test-P29.files`。
- 追加复扫阶段只做注释文本清理和必要 `gofmt`；未 stage，未 commit。

## 修复内容

- 清理历史计划/tracker 口吻、英文旧注释、旧阶段说明和乱码风险。
- 将低信息样板注释改为说明当前边界、失败语义、并发/生命周期、wire 兼容或持久化角色。
- 为 metrics/logger/history/path/idempotency/codemap 等跨模块结构补充字段或行为边界说明。
- `/tmp` 测试清单中清理了 P22/docs/plans/迁移、Phase、英文 contract/fuzz/guard 注释。

## LSP 工具

- `grep(text_search)`：窄范围搜索历史标签和计划引用，确认测试清单中无 `P22/docs/plans/迁移/Phase/TODO/FIXME` 残留。
- `structure(document_symbol)`：查看 `internal/platform/cachekeepalive/drain_test.go` 的测试结构和 helper 边界。
- `inspect(definition)`：跳转 `Manager` 定义，确认 cache keepalive 测试注释描述的对象边界。
- `xref(references)`：检查 `ProjectKeyFromCwd` 引用，复核 pathutil 注释涉及的跨包入口。
- `file(read_file/diagnostics)`：读取修改后的关键测试函数，并对测试清单运行 diagnostics，结果无诊断。

## 验证

- P29 生产列表逐文件 guard：
  - 命令：`while read f; do ./scripts/test_with_guard.sh "$f"; done < .agent/workflows/20260626-0651-backend-comments/PARTITIONS/P29.files`
  - 结果：37/37 文件 exit 0。
- P29 测试列表逐文件 guard：
  - 命令：`while read f; do ./scripts/test_with_guard.sh "$f"; done < /tmp/backend-comments-test-P29.files`
  - 结果：4/4 文件 exit 0。
- 历史标签/乱码复扫：
  - 命令：生产+测试并集上 `rg "//.*(ADR|docs/plans|docs/迁移|迁移|backlog|cutover|V2|TODO|FIXME|========|�)"`
  - 结果：exit 1，无匹配。
  - `P1` / `Phase` 类剩余命中仅来自必须以函数名开头的 Go doc comment，例如 `check_p1...` 和 `runRatchetPhase`，不是计划/tracker 标签。
- 英文旧注释复扫：
  - 命令：生产+测试并集上搜索英文 doc comment 动词模板。
  - 结果：exit 1，无匹配。
- LSP diagnostics：
  - `cmd/mcp-orch/orchestration/stop_test.go`
  - `internal/archtest/observability_log_event_guard_test.go`
  - `internal/module/memory/parse/parse_test.go`
  - `internal/platform/cachekeepalive/drain_test.go`
  - 结果：no diagnostics。
- 空白检查：
  - 命令：`git diff --check -- <P29 production files> <P29 test files> CHECKS/AGENT_REPORTS/P29.md`
  - 结果：exit 0。

## 备注

- 本轮使用允许的原生子代理派发；未记录持久 mcp-orch DAG。
- `test/fixtures/p21/secrets/check_redaction.go` 保留前序 P29 guard 修复中的行为等价 helper 拆分，用于消除既有复杂度 guard 失败；本次追加复扫未再做语义改动。
- 当前 worktree 仍存在其他非 P29 dirty 文件，本轮未 stage、未 commit、未回退。
