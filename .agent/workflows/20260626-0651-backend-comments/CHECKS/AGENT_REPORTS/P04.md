# P04 后端注释治理报告

## 最终状态

DONE_WITH_CONCERNS

必需验证均已通过：逐文件 guard、P04 生产列表 guard、P04 测试列表 guard、`git diff --check` 均为 exit=0。

concern：补充的最小包验证被 P04 外文件 `internal/module/prompt/enable_when.go` 的既有 error-prone pattern guard 阻断；该文件不在 P04 生产/测试清单内，未越界修复。

## 历史注释复扫与低样板修复

- 已按 `.agent/workflows/20260626-0651-backend-comments/PARTITIONS/P04.files` 复扫生产 Go 文件，清理 `Phase`、`Fxx`、`Pxx`、`ADR-*`、`docs/plans`、`§`、英文旧注释和 `========` 横幅类残留。
- 已重点复核 `/tmp/backend-comments-scan-P04.tsv` 指出的 6 条旧证据：`rpc.go`、`sharedfile_adapter.go`、`stop_helper.go`、`wakeupreclaim/reclaimer.go` 当前均不再命中原注释文本。
- 已处理 `wakeupreclaim/reclaimer.go` 的 `Phase 3.3 / 3A`、`fallback/default` 泛化说明，改为当前独立 lease 回收 runner、显式默认间隔和 fence 边界。
- 已处理 `taskdag/contract.go` 的英文重复块、Fxx/ADR/阶段说明和 `========` 风格横幅，保留当前接口预算、OCC、事务、run 隔离、wakeup wire 兼容边界。
- 已处理 `service.go`、`runtime.go`、`stop_helper.go`、`store/agent/store.go`、`store/sharedfile/module.go` 等低信息或英文旧注释。
- 联合扫描结果：
  - `cat P04.files /tmp/backend-comments-test-P04.files | xargs rg -n --pcre2 '//.*(Phase|ADR[-0-9]*|F[0-9]+|P[0-9]+|docs/plans|迁移|README|§|========)'`：exit=1，无输出。
  - `cat P04.files /tmp/backend-comments-test-P04.files | xargs rg -n --pcre2 '^\\s*//(?!go:)(?!\\s*$)(?!\\s*[-─]+)(?!.*\\p{Han}).*'`：exit=1，无输出。
  - `/tmp/backend-comments-scan-P04.tsv` 原文逐条 `rg -F`：无输出。

## 测试复扫小节

已处理 `/tmp/backend-comments-test-P04.files`：

- `cmd/mcp-orch/orchestration/dag_complete_downstream_test.go`：去掉 Phase/S7.2 标签，补足 NodeFlowStore 路由、状态机校验、兼容写路径边界。
- `cmd/mcp-orch/orchestration/wakeup_reclaim_test.go`：去掉 Phase 标签和英文 fallback 说明，明确 nil context 测试覆盖的是兼容替换路径。
- `internal/archtest/orchestration_no_rpc_shell_export_guard_test.go`：去掉 P22/P4/§ 历史标签，保留 RPC 壳禁止导出的当前架构边界。
- `internal/module/memory/phase4_baseline_invalidation_test.go`：去掉 Phase/Spike 计划说明和英文 helper 段，改为 UI RPC 写入失效契约说明。
- `internal/platform/metrics/metrics_test.go`：去掉 P22/P4/S6b 标签和英文 smoke-test 说明，改为 skill metrics counter 可达性说明。

未改测试语义：未重命名测试、未调整断言字符串、未改变执行路径；仅注释和必要 gofmt。

## 修改文件

生产 P04 Go 文件：

- `cmd/mcp-orch/orchestration/recover.go`
- `cmd/mcp-orch/orchestration/report.go`
- `cmd/mcp-orch/orchestration/report_protocol.go`
- `cmd/mcp-orch/orchestration/reportgc/gc.go`
- `cmd/mcp-orch/orchestration/reportstore/store.go`
- `cmd/mcp-orch/orchestration/retry_strategy.go`
- `cmd/mcp-orch/orchestration/retrypolicy/policy.go`
- `cmd/mcp-orch/orchestration/rpc.go`
- `cmd/mcp-orch/orchestration/runtime.go`
- `cmd/mcp-orch/orchestration/scheduledstart/start.go`
- `cmd/mcp-orch/orchestration/service.go`
- `cmd/mcp-orch/orchestration/service_launcher_bridge.go`
- `cmd/mcp-orch/orchestration/sharedfile_adapter.go`
- `cmd/mcp-orch/orchestration/sharedfilemeta/metadata.go`
- `cmd/mcp-orch/orchestration/sharedfileowner/owner.go`
- `cmd/mcp-orch/orchestration/stop_helper.go`
- `cmd/mcp-orch/orchestration/turn_lifecycle.go`
- `cmd/mcp-orch/orchestration/turncompletionretry/turncompletionretry.go`
- `cmd/mcp-orch/orchestration/wakeup_dispatcher.go`
- `cmd/mcp-orch/orchestration/wakeupreclaim/reclaimer.go`
- `cmd/mcp-orch/runtime.go`
- `cmd/mcp-orch/store/agent/store.go`
- `cmd/mcp-orch/store/commandcard/contract.go`
- `cmd/mcp-orch/store/commandcard/module.go`
- `cmd/mcp-orch/store/commandcard/store.go`
- `cmd/mcp-orch/store/prompt/contract.go`
- `cmd/mcp-orch/store/prompt/module.go`
- `cmd/mcp-orch/store/prompt/store.go`
- `cmd/mcp-orch/store/sharedfile/contract.go`
- `cmd/mcp-orch/store/sharedfile/importer.go`
- `cmd/mcp-orch/store/sharedfile/module.go`
- `cmd/mcp-orch/store/sharedfile/store.go`
- `cmd/mcp-orch/store/taskdag/contract.go`
- `cmd/mcp-orch/store/taskdag/factory.go`

测试 Go 文件：

- `cmd/mcp-orch/orchestration/dag_complete_downstream_test.go`
- `cmd/mcp-orch/orchestration/wakeup_reclaim_test.go`
- `internal/archtest/orchestration_no_rpc_shell_export_guard_test.go`
- `internal/module/memory/phase4_baseline_invalidation_test.go`
- `internal/platform/metrics/metrics_test.go`

报告：

- `.agent/workflows/20260626-0651-backend-comments/CHECKS/AGENT_REPORTS/P04.md`

## LSP 工具使用

- `mcp__lsp.grep(text_search)`：按 P04 生产和测试清单搜索 Phase/ADR/Pxx/Fxx/docs/plans/英文旧注释/fallback 等候选。
- `mcp__lsp.structure(document_symbol)`：查看 `wakeupreclaim/reclaimer.go` 和 `dag_complete_downstream_test.go` 的符号结构，定位 runner 与测试 helper 边界。
- `mcp__lsp.file(read_file)`：精读 `wakeupreclaim/reclaimer.go`、`dag_complete_downstream_test.go`、`wakeup_reclaim_test.go` 等上下文。
- `mcp__lsp.inspect(hover)`：复核 wakeup reclaimer / 测试符号当前 doc 展示。
- `mcp__lsp.xref(references)`：确认 `ProvideWakeupReclaimerRunner` 的 fx 接线引用，避免把 runner 注释写成临时阶段说明。
- `mcp__lsp.file(diagnostics)`：改后对生产/测试文件分批诊断；无 error，`wakeup_reclaim_test.go` 保留一个意图明确的 nil context 测试 warning，`stop_helper.go` 有既有 unusedfunc info。

## 验证结果

逐文件 guard（生产改动文件）：

- 全部 34 个 P04 生产改动 Go 文件 `./scripts/test_with_guard.sh <file.go>`：exit=0。
- 修复过程中新增/复改文件也已就地单文件 guard：`wakeupreclaim/reclaimer.go`、`rpc.go`、`sharedfile_adapter.go`、`stop_helper.go`、`runtime.go`、`store/agent/store.go`、`store/sharedfile/module.go`、`retry_strategy.go`、`turn_lifecycle.go`、`taskdag/contract.go` 均 exit=0。

逐文件 guard（测试清单）：

- `cmd/mcp-orch/orchestration/dag_complete_downstream_test.go` exit=0
- `cmd/mcp-orch/orchestration/wakeup_reclaim_test.go` exit=0
- `internal/archtest/orchestration_no_rpc_shell_export_guard_test.go` exit=0
- `internal/module/memory/phase4_baseline_invalidation_test.go` exit=0
- `internal/platform/metrics/metrics_test.go` exit=0

列表 guard：

```bash
xargs ./scripts/test_with_guard.sh < .agent/workflows/20260626-0651-backend-comments/PARTITIONS/P04.files
```

结果：exit=0。

```bash
xargs ./scripts/test_with_guard.sh < /tmp/backend-comments-test-P04.files
```

结果：exit=0。

diff check：

```bash
git diff --check
```

结果：exit=0。

补充最小包验证：

```bash
./scripts/test_with_guard.sh ./cmd/mcp-orch/orchestration/... ./cmd/mcp-orch/store/... ./internal/archtest ./internal/module/memory ./internal/platform/metrics -count=1
```

结果：exit=1。阻断项为 P04 外文件：

```text
internal/module/prompt/enable_when.go:1 error-prone pattern guard: template-level tags_has must fail closed: missing "Template-level keyword routing is retired"
```

去掉 `internal/archtest` 后重跑相关包仍被同一个仓库级 guard 阻断，说明不是本分区修改引入。

## 剩余风险 / 阻塞

- required checks 全部通过；补充 package guard 被 P04 外 `internal/module/prompt/enable_when.go` 阻断，按范围约束未修。
- LSP diagnostics 中 `wakeup_reclaim_test.go` 的 SA1012 是测试 nil context 兼容路径的意图行为；单文件 guard 与列表 guard 均通过。
- worktree 中存在其它分区/其它 worker 的未提交修改；本报告只覆盖 P04 生产清单和 `/tmp/backend-comments-test-P04.files`。
- 未 stage、未 commit。
- 本轮使用允许的原生子代理派发；未登记持久 mcp-orch DAG 生命周期。
