# P00 Aristotle 注释治理复扫报告

状态：DONE_WITH_CONCERNS

## 历史注释复扫与低样板修复

- 范围：`.agent/workflows/20260626-0651-backend-comments/PARTITIONS/P00.files` 生产清单。
- 处理：清理生产清单中的 `Pre-P22`、`P22 P2`、`LSP-S2` 等历史计划标签；把 LSP cache、adapter、Go root resolver、client、bootstrap、factory、installer、middleware、manager registry/scope 等跨模块入口的英文旧注释和“处理/创建/返回”低样板注释改为当前行为说明。
- 约束：只改注释和必要 `gofmt`，未改运行语义；未通过删除注释或逻辑绕过 guard。
- 复扫：
  - 历史标签注释行扫描：`rg -n -P '^\\s*//.*(Phase|Pre-P[0-9]+|P[0-9]+|F[0-9]+|ADR|docs/plans|docs/迁移|迁移|tracker|LSP-S[0-9]|plan §)' ...`，exit 1，无命中。
  - 英文旧注释扫描：只剩 `//go:build` 与 `//go:embed` 编译/嵌入指令，人工确认不属于治理目标。
  - 低样板扫描：已修复缺少当前边界/失败/生命周期说明的函数级注释；剩余命中均已人工复核为包含边界、失败、兼容或持久化说明的有效注释。

## 测试复扫

- 范围：`/tmp/backend-comments-test-P00.files`。
- 处理：清理测试清单中的历史 Phase/Pxx/LSP-Sx 注释，测试说明改为当前真实代码形态；未改断言逻辑或 fixture 文案。
- 复扫：
  - 测试清单历史标签注释行扫描：exit 1，无命中。
  - 非注释字符串中仍有历史文字的用例 fixture / error message 未改动，原因是本轮要求只改注释，不改运行语义。

## LSP 工具

- `mcp__lsp.file`：`diagnostics` 检查关键变更文件，返回 no diagnostics。
- `mcp__lsp.grep`：复核历史标签文本命中；最终 P00 注释行扫描无命中，宽路径命中均为非 P00 清单或非注释字符串。
- `mcp__lsp.structure`：读取 `cmd/mcp-lsp/multilsp/cache.go` 文档符号，确认缓存 store / key / 方法边界。
- `mcp__lsp.inspect`：hover `lspCacheStore.Enabled`，确认文档注释与当前符号绑定。
- `mcp__lsp.completion`：在 `cache.go` 当前位置触发补全，确认 LSP 索引可用。

## 验证结果

- 逐个改动 Go 文件单文件 guard：37/37 exit 0。
- P00 生产列表 guard：`./scripts/test_with_guard.sh $(tr '\n' ' ' < .agent/workflows/20260626-0651-backend-comments/PARTITIONS/P00.files)`，exit 0。
- P00 测试列表 guard：`./scripts/test_with_guard.sh $(tr '\n' ' ' < /tmp/backend-comments-test-P00.files)`，exit 0。
- LSP diagnostics：exit 0，no diagnostics。
- `git diff --check`：exit 0。

## 改动文件

- `cmd/agent-terminal/frontend.go`
- `cmd/mcp-ida/fx.go`
- `cmd/mcp-ida/main.go`
- `cmd/mcp-lsp/edit/patchmatch.go`
- `cmd/mcp-lsp/edit/patchparse.go`
- `cmd/mcp-lsp/edit/replaceutil.go`
- `cmd/mcp-lsp/edit/seeksequence.go`
- `cmd/mcp-lsp/format/compact.go`
- `cmd/mcp-lsp/format/display.go`
- `cmd/mcp-lsp/format/funcrange.go`
- `cmd/mcp-lsp/format/render.go`
- `cmd/mcp-lsp/fx.go`
- `cmd/mcp-lsp/http_runner.go`
- `cmd/mcp-lsp/installer/installer.go`
- `cmd/mcp-lsp/internal/hiddenexec/process.go`
- `cmd/mcp-lsp/internal/hiddenexec/process_default.go`
- `cmd/mcp-lsp/internal/hiddenexec/process_windows.go`
- `cmd/mcp-lsp/main.go`
- `cmd/mcp-lsp/manager/manager.go`
- `cmd/mcp-lsp/manager/registry.go`
- `cmd/mcp-lsp/manager/scope.go`
- `cmd/mcp-lsp/middleware/budget.go`
- `cmd/mcp-lsp/middleware/budget_hints.go`
- `cmd/mcp-lsp/multilsp/adapter.go`
- `cmd/mcp-lsp/multilsp/bootstrap_doc.go`
- `cmd/mcp-lsp/multilsp/cache.go`
- `cmd/mcp-lsp/multilsp/cache_no_loop_test.go`
- `cmd/mcp-lsp/multilsp/client.go`
- `cmd/mcp-lsp/multilsp/configuration_request.go`
- `cmd/mcp-lsp/multilsp/factory.go`
- `cmd/mcp-lsp/multilsp/go_root_resolver.go`
- `cmd/mcp-lsp/multilsp/go_toolchain.go`
- `cmd/mcp-lsp/multilsp/gomod.go`
- `cmd/mcp-orch/orchestration/wakeup_dispatcher_shard17_batch_test.go`
- `internal/archtest/observability_metric_emit_guard_test.go`
- `internal/module/memory/phase16_invalidation_test.go`
- `internal/platform/cachekeepalive/manager_test.go`
- `.agent/workflows/20260626-0651-backend-comments/CHECKS/AGENT_REPORTS/P00.md`

## 剩余 concern

- 隔离 worktree 中存在大量非 P00 分区的既有 dirty 文件；本轮未 stage、未 commit、未跨分区编辑。
