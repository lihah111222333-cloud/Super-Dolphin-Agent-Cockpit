# P07 后端注释治理报告

## 最终状态

DONE_WITH_CONCERNS

## 修改文件

- `internal/contract/manifest.go`
  - 补充 MCP manifest 构建、HTTP/stdout fallback、PROJECT_ROOT 推导、LSP workspace roots 注入、数据库 env 清理的函数级中文注释。
- `internal/contract/memory.go`
  - 补充记忆作用域/类型、读写请求结果、agent 记忆读写接口、错误包装的类型级和函数级中文注释。
- `internal/contract/orchestration.go`
  - 补充 agent lifecycle、DAG runtime、manual dispatch、run/result DTO、DAG event helper 的中文边界说明，并移除中英重复注释。
- `internal/contract/prompt.go`
  - 补充 prompt assembler 输入、BuildCtx、动态 section、缓存策略、内置 prompt registry、turn context provider 的类型级和函数级中文注释。

## LSP 工具使用

- `grep(text_search)`: 定位 P07 Go 文件中的函数候选。
- `grep(ast_search)`: 定位 contract DTO 结构体候选。
- `structure(document_symbol)`: 查看 `guardlib.go`、`memory.go`、`orchestration.go` 的符号大纲。
- `inspect(hover)`: 查看 `OrchestrationService`、`BuildCtx` 的符号信息。
- `xref(references)`: 检查 `OrchestrationService` 的引用影响面。
- `file(read_file/open_file/diagnostics)`: 精读候选文件并对已改文件运行 LSP diagnostics。

## 验证结果

- `./scripts/test_with_guard.sh internal/contract/prompt.go` -> exit 0
- `./scripts/test_with_guard.sh internal/contract/memory.go` -> exit 0
- `./scripts/test_with_guard.sh internal/contract/orchestration.go` -> exit 0
- `./scripts/test_with_guard.sh internal/contract/manifest.go` -> exit 0
- `./scripts/test_with_guard.sh ./internal/contract -count=1` -> exit 0
- `git diff --check -- internal/contract/prompt.go internal/contract/memory.go internal/contract/orchestration.go internal/contract/manifest.go` -> exit 0
- LSP diagnostics for the four modified files -> 0 diagnostics

## 剩余风险 / 阻塞

- 当前 worktree 存在大量非 P07 既有脏文件；本 worker 未修改、未回退这些文件，最终集成需由主控统一处理。
- `mcp-go-agent-orchestration` lifecycle tools 在本会话不可用，未登记 DAG lifecycle。
- 未运行全仓 `make test` / `make guard`，本次按分区范围只运行已改文件守卫和 `internal/contract` 最小包验证。
