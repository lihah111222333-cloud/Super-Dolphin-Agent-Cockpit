# P14 后端注释治理报告

## 最终状态

DONE_WITH_CONCERNS

## 修改文件

- `internal/module/prompt/dynamic_cache_deps.go`：补充动态 section 缓存依赖、子代理依赖和 LSP 工具依赖说明。
- `internal/module/prompt/enable_when.go`：清理 enable_when/match_when 机器式注释，说明 fail-closed 条件和工具名规范化。
- `internal/module/prompt/env_provider.go`：补充环境快照、主会话/子代理渲染、shell/OS 探测说明。
- `internal/module/prompt/frc_provider.go`：补充 FRC provider 注册名和模型启用条件说明。
- `internal/module/prompt/invalidation.go`：补充 section 缓存失效、provider 通知和 catalog 刷新说明。
- `internal/module/prompt/match_when_support.go`：补充 prompt 内容 section 写入、intent 推断和 draft asset 映射说明。
- `internal/module/prompt/mcp_provider.go`：补充 MCP instructions tracker、delta attachment 和并发状态说明。
- `internal/module/prompt/model_descriptor.go`：补充模型元数据查找和展示文本说明。
- `internal/module/prompt/module.go`：补充 fx 装配、handler 注册和 evaluator provider 说明。
- `internal/module/prompt/output_style_provider.go`：补充输出风格和长度锚点 provider 说明。
- `internal/module/prompt/prompt_hint.go`：补充 override/default prompt hint 读取和 JSON 解码说明。
- `internal/module/prompt/registry.go`：补充 section registry 并发注册和排序快照说明。
- `internal/module/prompt/scratchpad_provider.go`：补充 scratchpad 动态 section 注释。
- `internal/module/prompt/section.go`：清理任务标签残留，补充静态 section 和 tag 合并说明。
- `internal/module/prompt/section_write.go`：补充 section 写入事务和 recall topic 去重说明。
- `internal/module/prompt/service.go`：补充 prompt 持久化服务、scope 校验、归档和缓存失效说明。
- `internal/module/prompt/service_surface.go`：补充 RPC 参数/响应、handler 和 section preview 说明。
- `internal/module/prompt/stub_providers.go`：补充 feature gate 辅助函数说明。
- `internal/module/prompt/system_context_provider.go`：补充 git status 系统上下文、超时和截断说明。
- `internal/module/prompt/template_sections.go`：补充模板 section 合并、scope、recall 去重和 asset 筛选说明。
- `internal/module/prompt/token_budget_provider.go`：补充 token budget backstop、低收益判断和续跑提示说明。
- `internal/module/prompt/types.go`：补充 prompt 包跨 contract 导出别名说明。
- `internal/module/prompt/user_context_builder.go`：补充 user context cache、generation 和运行时上下文合并说明。
- `internal/module/rag/chunker.go`：补充文本分块、token 边界和自然切分点说明。
- `internal/module/skill/approval.go`：清理审批缓存任务编号标签，补充 artifact 审批持久化语义。
- `internal/module/skill/canonical_store.go`：补充 canonical skill record、冲突、策略和扫描过滤说明。
- `internal/module/skill/contract.go`：补充 skill 模块公共契约、mirror reconciliation 和路径校验说明。
- `internal/module/skill/events.go`：补充 skill changed debounce、跨 scope 排队和事件合并说明。

## 单文件守卫结果

命令格式：`./scripts/test_with_guard.sh <file.go>`

- `internal/module/prompt/dynamic_cache_deps.go` exit=0
- `internal/module/prompt/enable_when.go` exit=0
- `internal/module/prompt/env_provider.go` exit=0
- `internal/module/prompt/frc_provider.go` exit=0
- `internal/module/prompt/invalidation.go` exit=0
- `internal/module/prompt/match_when_support.go` exit=0
- `internal/module/prompt/mcp_provider.go` exit=0
- `internal/module/prompt/model_descriptor.go` exit=0
- `internal/module/prompt/module.go` exit=0
- `internal/module/prompt/output_style_provider.go` exit=0
- `internal/module/prompt/prompt_hint.go` exit=0
- `internal/module/prompt/registry.go` exit=0
- `internal/module/prompt/scratchpad_provider.go` exit=0
- `internal/module/prompt/section.go` exit=0
- `internal/module/prompt/section_write.go` exit=0
- `internal/module/prompt/service.go` exit=0
- `internal/module/prompt/service_surface.go` exit=0
- `internal/module/prompt/stub_providers.go` exit=0
- `internal/module/prompt/system_context_provider.go` exit=0
- `internal/module/prompt/template_sections.go` exit=0
- `internal/module/prompt/token_budget_provider.go` exit=0
- `internal/module/prompt/types.go` exit=0
- `internal/module/prompt/user_context_builder.go` exit=0
- `internal/module/rag/chunker.go` exit=0
- `internal/module/skill/approval.go` exit=0
- `internal/module/skill/canonical_store.go` exit=0
- `internal/module/skill/contract.go` exit=0
- `internal/module/skill/events.go` exit=0

## 分区最小包验证

- `./scripts/test_with_guard.sh ./internal/module/prompt ./internal/module/rag ./internal/module/skill -count=1` exit=0

## 主控复核打回修复

- `internal/module/prompt/prompt_hint.go`：将 `resolvePromptHint returns...` 英文 doc comment 改为中文，说明当前 cwd 可见的用户配置 LSP prompt hint 读取范围：用户偏好覆盖值优先，共享文件默认值兜底；未配置、依赖未注入或读取失败时返回空串，不阻断 prompt 组装。
- 同步将 prompt hint key/path 常量组说明改为中文，标明读写两侧需要与 `internal/module/uistate/config_rpc.go` 同步。
- 复核验证：
  - `./scripts/test_with_guard.sh internal/module/prompt/prompt_hint.go` exit=0
  - `xargs ./scripts/test_with_guard.sh < .agent/workflows/20260626-0651-backend-comments/PARTITIONS/P14.files` exit=0
  - `git diff --check` exit=0

## LSP 工具使用

- `grep(text_search)`：定位 P14 Go 文件中的函数/type/const/var 和机器式注释。
- `structure(document_symbol)`：查看 `service.go`、`canonical_store.go`、`dynamic_cache_deps.go` 的符号大纲。
- `inspect(hover)`：确认 `cacheByNameSectionDependency` 当前注释和签名。
- `xref(references)`：确认 `cacheByNameSectionDependency` 被 `sectionInputCacheKey` 使用。
- `file(read_file)`：读取 `sectionInputCacheKey` 函数上下文。
- `file(diagnostics)`：对所有改动 Go 文件做 LSP 诊断。

## 剩余风险 / 阻塞

- LSP diagnostics 没有 error 级诊断；仍有 info/hint 级建议，例如 unused helper、CutPrefix/min/slices.Contains/atomic.Uint64 现代化建议，均非本次注释治理范围，未改业务逻辑。
- 当前 worktree 存在非 P14 脏文件（如 mcp-lsp、mcp-orch、platform、provider 等大量文件，以及 `internal/module/prompt/cache.go`、`internal/module/skill/mirror_*`、`internal/module/skill/rpc.go` 等），本次未编辑、未回退、未 stage。
- Native subagent dispatch used; no persistent mcp-orch DAG ledger recorded.
