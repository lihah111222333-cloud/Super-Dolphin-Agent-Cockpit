# P24 后端注释治理报告

## 最终状态

DONE_WITH_CONCERNS

说明：本次按主控打回只改注释，已完成 P24 生产清单与 `/tmp/backend-comments-test-P24.files` 的历史注释复扫与低样板修复。针对最新打回点，`internal/provider/codexapp/codexmanifest/manifest.go:VerifyManaged` 已改为自然中文，说明托管 Codex manifest 的下载源校验和、包内路径、二进制摘要和 app-server 探测边界。未 stage、未提交、未改业务逻辑。

## 修改文件

- `internal/provider/claudecli/*`：补充 history、image tracking、session/runtime、transport、keepalive、retry 和日志 watcher 的中文注释，强调 wire 兼容、并发锁、失败边界和关闭责任。
- `internal/provider/codexapp/*`：清理 P20/P21/P22/ADR/Phase/docs/plans/迁移 等历史标签，补充 provider home、ServerPool、session runtime、event payload、rollout、orphan sweeper、peer supervisor、pool spawn 和托管 Codex manifest 的当前行为说明。
- `/tmp/backend-comments-test-P24.files` 列出的测试文件：清理历史标签和英文旧注释，改为当前 runtime ownership、worker drain、enqueue-only、sharedfile 磁盘/DB 分离和 automation outputs 禁止字段边界说明。
- 最新打回修正：`internal/provider/codexapp/codexmanifest/manifest.go` 的 `VerifyManaged` 注释去掉低样板/混合英文。

## LSP 使用

- `grep(text_search)`：扫描 P24 生产/测试清单中的历史标签、英文旧注释和低样板候选。
- `file(read_file/diagnostics)`：定点读取 `driver.go`、`image_tracker.go` 等上下文，并获取诊断。
- `structure(document_symbol)`：查看 `image_tracker.go` 符号结构。
- `inspect(hover/definition)`：确认 `markIfNew` 等关键函数签名和注释渲染。
- `xref(references)`：确认 `imageHashTracker` 在 session/turn 路径中的引用范围。

## 验证

| 验证项 | 命令 | exit code |
|---|---|---:|
| 最新打回单文件 guard | `./scripts/test_with_guard.sh internal/provider/codexapp/codexmanifest/manifest.go` | 0 |
| P24 生产清单 guard | `while read f; do ./scripts/test_with_guard.sh "$f"; done < .agent/workflows/20260626-0651-backend-comments/PARTITIONS/P24.files` | 0 |
| P24 测试清单 guard | `while read f; do ./scripts/test_with_guard.sh "$f"; done < /tmp/backend-comments-test-P24.files` | 0 |
| diff 空白检查 | `git diff --check -- <P24生产清单> <P24测试清单> .../P24.md` | 0 |

P24 生产清单 guard 明细：37/37 文件 exit 0。
P24 测试清单 guard 明细：5/5 文件 exit 0。

## 剩余风险/阻塞

- 更宽的最小包验证曾失败在非本次注释改动范围的既有测试/架构问题：bootstrap guard 仍要求历史锚点、`internal/module/skill` 架构依赖、以及若干 thread Codex identity 测试路径差异。未按本次“只改注释”要求处理。
- 当前 worktree 仍有大量非 P24 分区改动，视为其他分区/他人改动；本任务未回退、未 stage、未提交。
