# 架构决策

本目录存放当前仍有参考价值的架构边界、架构骨架和设计审计材料。

- `架构边界/`: 包依赖、边界规则和架构守卫说明。
- `架构骨架/`: Fx、RunGroup、事件、JRPC2、无状态等骨架说明。
- `设计审计/`: 生命周期、设计路径和历史审计结论。

长期契约仍以 `docs/契约/` 为入口；已接受 ADR 仍以 `docs/adr/` 和 `docs/decisions/` 为入口。
