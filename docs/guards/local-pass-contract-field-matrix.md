# Local PASS / scheduler 字段矩阵

本矩阵把 Accepted 远程 CI 契约中的本地主机开发边界落到可复查的生产字段、SQLite 投影和静态守卫。local authority 是开发红绿证据，不是 ECI authority 或 release receipt；任何缺字段、跨 namespace lookup、旧物理 schema、未知 provider 能力或隐式 fallback 都必须 fail-fast。

| 契约字段/材料 | 唯一 producer | 转换与直接消费者 | 身份/权威边界 | 守卫与证据 |
| --- | --- | --- | --- | --- |
| `WorkloadPassNamespace` (`local` / `remote`) | `internal/devtools/gate/local_pass_contract.go`，值由 `internal/devtools/cicontract/local_pass_contract.go` 持有 | `WorkloadPassKey`、local scheduler item 校验、local/remote lookup 分支 | namespace 是存储/query key 的显式前缀；不得进入 `WorkloadPassIdentitySHA256` | `TestLocalPassContractBoundary` 的 key AST 检查；`WorkloadPassKey.Validate/Parse` 必须拒绝空/未知前缀 |
| `WorkloadPassIdentity` 四段摘要 | `internal/devtools/gate/workload_pass_evidence.go` | remote PASS identity hash、local entry 校验、两套 evidence 表 | canonical hash 只含 workload/execution/input/environment；namespace、tree、commit、branch、worktree path 不得进入 hash 或 lookup key | local/remote identity AST guard；remote `TestRemoteCIPassIdentityTreeExclusionGuard` |
| `LocalWorkloadPassEnvironment` correctness fields | `internal/devtools/gate/local_pass_contract.go:LocalWorkloadPassEnvironment` 动态枚举全部字段；sealed receipt 的 `localReceiptEnvironment` 先投影公共 `BaseRunnerSemanticDigest`，`localReceiptRunnerSemanticDigest` 仅对 argv[0]=`super-dolphin-gate` 的 receipt-bound program 派生 per-workload final `RunnerSemanticDigest` | `LocalWorkloadPassEnvironmentDigest` 的 `localWorkloadPassEnvironmentPayload` literal 逐字段直映 `Platform`、`GOOS`、`GOARCH`、`GOAMD64`、`GOARM64`、`CGOEnabled`、`GOEXPERIMENT`、`CC`、`CXX`、`SDK`、`OSBuild`、`GoVersion`、`GoFlags`、`ToolchainClosureDigest`、`BaseRunnerSemanticDigest`、`RunnerSemanticDigest`；ProjectMap self proof 的 canonical payload 仅为 logical `Name`、content `Digest`、build `Version`，再派生 final runner digest；`RunnerSemanticPolicy` 唯一合法派生为 `sha256(environment.RunnerSemanticPolicy)` → `RunnerSemanticPolicyDigest` | schema/domain 是 cicontract framing，不是 producer 字段；HostContext/origin 只绑定公共 base，environment identity 同时绑定 base+final；self 的绝对 path、repo/tree/commit 不得进入 identity；self 的 content/version 漂移必须在 lookup 前拒绝，非 self program 不得吸收该 proof；无默认或 fallback | 动态 producer/payload/mapper missing+stale 双向 guard；`TestLocalRunnerSelfSemanticProofFieldGuard` 动态枚举 self payload 并拒绝未登记/path 字段；sealed-receipt tests 覆盖 self-only、non-self-only、mixed promotion、self-only MISS 与 mixed rollback |
| source tree / commit / path provenance | local materializer/origin 与 remote run audit producer | `LocalWorkloadPassOrigin.SourceTreeSHA`、remote `OriginSourceTreeSHA`/run record | 只用于 provenance、receipt/run audit 和 source replay proof；不得成为 local/remote identity、alias 或 lookup key | tree-bearing identity counterexample；`TestRemoteCIPassIdentityTreeExclusionGuard`；local origin projection digest red/green tests |
| host admission (`30s`, CPU busy `<=70%`, capacity) | `BuildLocalHostAdmissionFromSamples` / `ValidateLocalHostAdmissionObservation` | local scheduler MISS admission；命中 lookup 前不得采样 | 仅用于 local 准入与审计，不进入 PASS key；样本不足/窗口短/容量无效必须拒绝 | frozen constants guard；`TestLocalHostAdmissionRejectsShortWindowAndInsufficientSamples`；hit-before-sampler test |
| default `auto` scale (`64` / `10min` / `5min`) | `cicontract.LocalAutoMissCountLimit`、`LocalAutoDurationLimitMS`、`LocalAutoSingleDurationLimitMS` | `localSchedulerAutoScaleAdmitted` | 只决定执行前 local/remote 选择；不得成为 remote shard/concurrency policy 或 identity 字段 | constant owner + scheduler selector AST guard；oversized/count red tests |
| local authority state/generation | `ci_local_authority_state` 与 `initializeLocalAuthorityStateOnConnection` | `currentLocalAuthorityGeneration`、local origin write/lookup | local generation 独立于 accepted remote generation；clean init 可创建，损坏/摘要漂移/未知字段必须 fail-fast | state source markers (`sql.ErrNoRows`, strict decoder, digest validation)；missing/tampered state tests |
| local origin/execution/evidence rows | `strictLocalWorkloadPassSQLiteSchema` 与 `RecordLocalWorkloadPassBatch` | `localSchedulerPassEntryFromReceipt` 与 production origin 从 sealed receipt 投影 `RunnerSemanticPolicy` / common `BaseRunnerSemanticDigest` 到 origin；每条 execution JSON 保留 per-workload base+final environment；`LookupLocalWorkloadPassEvidence`；FK cascade 只在 `ci_local_*` namespace 内 | v13→v14 只增加 local namespace；origin runner proof 必须等于 entry base runner proof，HostContext digest 只绑定 base，entry final 可以是 self-specific；local rows 不得写 `ci_runs`、`ci_check_receipts`、remote baseline 或 authoritative receipt | additive schema AST/string guard；sealed origin/environment mixed-promotion and rollback tests；remote SQLite authority guard allowlists only four `ci_local_*` tables |
| remote execution scope side table | `RemoteCIExecutionScope` + `ci_remote_run_execution_scopes` | `RemoteCIRunRecord.Scope`、scope store write/load、SQLite schema registration | v14→v15 只新增 one remote side table/indexes；legacy/full no row，subset only typed JSON/digest/count；scope digest excludes tree/path/commit/token and is neither PASS identity nor local alias | `TestLocalPassContractBoundary` v15 guard；`TestRemoteCIExecutionScopeV15AcceptedContract` and counterexamples |
| remote PASS lookup/receipt | remote gate/remoteci coordinator and SQLite authority | `LookupWorkloadPassEvidence`、remote receipt finalizer | remote path 不读 local table/lookup；local PASS 不能提升为 ECI/release PASS | remote source scan for `ci_local_`/local calls; remote finalizer table boundary guard |
| local executor capability closure | `local_executor*.go` and local CLI path | local scheduler `Execute` callback and exact-tree materializer | local functions不得触 agent token、ECI、OSS、ImageCache 或云 provider；唯一例外是 frozen `runLocalRemoteSubset`，且仅在 `prepared.Remote` 非空后握手 token 并转交 remote subset | function-level AST boundary guard; local provider/token counterexamples |
| receipt-bound / execution-only workload IDs | `LocalExecutorReceiptBoundWorkloadIDs` / `LocalExecutorExecutionWorkloadIDs` | production `prepareProductionLocalExecutorReceipt`, dependency discovery, session binding, scheduler receipt coverage | 所有有 canonical mapping 的 eligible 与 mapped-ineligible ID 都必须进入 sealed receipt，只有 `Eligible=true` 可进入 dependencies/session；known-unmapped 无 receipt/identity 且 direct remote | classification matrix tests + `TestProductionLocalExecutorReceiptSeparatesBoundAndExecutableIDs`; mapped-ineligible scheduler hit/miss and known-unmapped tests |
| versioned migration read set | `ledger_store_sqlite_namespace_migration.go` v16 retained-proof backfill | v13/v15 frozen remote authority objects and v16 `ci_retained_workload_pass_proofs` | shared migration may read only its explicit remote inputs and write the v16 additive proof projection; any unknown `ci_*` object is fail-fast | function-level literal allowlist and unknown-table counterexample |
| CLI workload selection | existing `--workload` catalog binding in `test_cli.go`; expanded `--gate-workload`/manifest in local path | `parseAutoTestRunOptions`, `bindOrValidateAutoTestSelectors`, local selection validator | `--workload` semantics remain catalog-owned; new local target must not reinterpret or remove it | CLI option/function marker guard; existing `test_cli_test.go` workload/N/V tests and local selector tests |

## 复查顺序

1. 先确认 namespace 与四段 identity hash 的边界，再读 local origin/provenance。
2. 再确认 local lookup/table 与 remote lookup/receipt 的物理隔离，以及 v13→v14 local 和 v14→v15 scope 仅 additive 的 schema diff。
3. 最后确认 hit-first sampling、auto scale、local failure no-fallback 和 CLI `--workload` 兼容性。

对应架构守卫：

- `TestLocalPassContractBoundary`
- `TestLocalPassContractGuardCounterexamples`
- `TestRemoteCISQLAuthorityBindingsAreCreatedByGateSchema`
- `TestRemoteCIPassIdentityTreeExclusionGuard`
- `TestRemoteCIExecutionScopeV15AcceptedContract`
