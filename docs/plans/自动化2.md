# 自动化（上下文继承与预警 · 自动续接）

> 分支：`上下文继承与预警`
> 范围：让 agent 派发的自动化任务在 token 满 / 状态出错时自我续命，保证任务流程不中断。
> 现状（2026-04-30）：Phase 0–1.5 全部 ship；Phase 1.6 (auto-continue state RPC) + 1.7b 状态模型重构 + 1.7e 偏好切换联动 timer 启停已 ship（commit 见 §8）。Phase 1.7c 严格按文档已对齐（无代码改动）。下一步：Phase 1.7f / 1.8a 持久化接通（用 1.6 RPC）/ 1.8b 后端 classifyLaunchError。
> 暂不启动：Phase 1.7f / 1.8a 依赖 Phase 1.6；Phase 2.x / Phase 3.x 需等 Phase 1.7 + 1.8 收口。

---

## 1. 背景与动机

### 1.1 问题
agent 派发的多轮自动化任务（child agent / typed agent / OwnerThreadID 关系下的 thread）跑着跑着会撞到两类天花板：

- **token 用量到 critical**：上下文窗口满，下一轮 turn 失败
- **status 转 error**：CLI 进程崩 / 网络断 / 内部异常

目前两种情况的处理都依赖**用户手动操作**（点 banner 上的"压缩 / 新建继承"或 thread 行的"恢复"按钮）。**对自动化任务而言这是不可接受的** —— 任务跑到一半要等人工介入，相当于没完成。

### 1.2 设计目标
让"task thread"（runtime config 含 `taskId`）在以下信号触发时**自动**完成续命动作，不打扰用户：

- token level 跨入 `critical`
- status 转 `error`

普通用户对话（无 `taskId`）**完全不在自动化范围内**，行为和今天一样（用户自己点 banner / 恢复按钮）。

### 1.3 边界（明确不做）
- 普通对话不自动触发任何动作（Phase 2.x 才会通过 promote-task RPC 让用户主动开关）
- 不引入新的"silent startThread"模式，焦点处理在调度器内部用 selectedThreadId 比较解决
- 不新增 toast / banner 组件，复用现有 `ContextUsageBanner`

---

## 2. 设计概览

### 2.1 整体架构
```
┌─ token level watcher ─┐    ┌─ status watcher ─┐
│  watch tokenUsageBy-  │    │  watch statuses   │
│  Thread (deep)        │    │  (deep)           │
└──────────┬────────────┘    └──────┬────────────┘
           │ critical 跨阈值         │ → error
           ▼                          ▼
   ┌──────────────────────────────────────┐
   │   useAutoContinue 调度器              │
   │  ── 过滤：仅 task thread (有 taskId) │
   │  ── 闸门：偏好 / 幂等 / 频率限制       │
   │  ── 决策优先级：                      │
   │       compact > continueTaskById      │
   │       recover > continueTaskById      │
   └──────────────────────────────────────┘
           │
           ▼
   ┌─ compactCurrent ─┐  ┌─ recoverThread ─┐  ┌─ continueTaskById ─┐
   │ 已存在            │  │ 已存在           │  │ 1.1 抽出           │
   └──────────────────┘  └─────────────────┘  └────────────────────┘
```

### 2.2 不重复造轮子
现有可复用：
- `compactCurrent`（`useThreadActions.js:282`）：上下文压缩，capability `context_compact` 的 thread 才支持
- `recoverThread`（`thread/recover` RPC）：进程恢复
- `useTaskHandoff.continueTaskById`（1.1 抽出）：task thread 续接，复用现有 handoff 文件链路
- `ContextUsageBanner`：警告 banner，已在 critical 时显示

### 2.3 触发范围（task-only）
判定条件：`threadStore.state.agentRuntimeById[threadId].taskId` 非空

来源（`task_handoff.go::shouldAutoTaskHandoff`）：
- `autoTaskHandoff: true` 显式 opt-in
- `ParentAgentID` 不空（agent 派发的子 thread）
- `OwnerThreadID` 不空
- `AgentType` 不空（typed agent）

**普通对话不在监听范围内**，不会被任何 watcher 关注。

### 2.4 另外两条同时在跑的自动续命路径（不在本计划范围）

本计划画的 useAutoContinue 调度器是"前端 thread 级"那一层（下记为 **F1/F2/F3**）。系统里同时还跑着两条**后端层面**的自动续命路径，与本计划独立，但梳理时容易遗漏：

- **B1 · mcp-orch agent launch retry** — `cmd/mcp-orch/orchestration/service_launcher_bridge.go` `launchWithRetry` + `isRetryableLaunchError`；`maxLaunchRetries=3`，退避 `attempt × 2s`（实际等待 0/2s/4s）；只识别 6 个关键字 + `errors.Is(context.DeadlineExceeded/Canceled)`，**不识别 429 / quota / context_length_exceeded**
- **B5 · codexapp peer 自动重启** — `internal/provider/codexapp/peer_supervisor.go` `superviseOne`；peer 进程 `Wait()` 返回后等 `restartBackoff` 调 `launcher.Launch` 重启；**首次重启失败即 degraded 直到整个进程 shutdown，不再尝试**

另外有 3 条**底层能力存在但没有业务 driver、不在跑**的，不要被代码骗：

- `store/taskdag/store_wakeup.go` 的 `ClaimDueWakeups` / `RetryWakeup` / `ReclaimStaleDispatchingWakeups` —— SQL 与 store 方法已存在，除 store 内部和 test 外缺业务 dispatcher 调用 → **Phase 3A 接线**
- DAG 节点完成自动 spawn 下一节点 —— `CompleteNode` 只更新 DB，不查 DAG 拓扑、不 enqueue 下游 wakeup → **Phase 3B 实施**
- DAG retry 策略 —— 当前 `RetryTaskDagWakeup` SQL 有 `attempt_count < 8` 硬限制，未接 DAG metadata `default_retry` / `fail_fast` → **Phase 3B 实施**（详见 [dag-auto-spawn-next-node](../../../../.multi-agent/memory/projects/users-mac-desktop-agnet-super-dolphin/memory/team/project_dag_auto_spawn_next_node.md)）

全图核对结论沉淀在 team memory `auto-continue-full-map`，再讨论"自动续命"前先看那条避免重核。

---

## 3. 决策调度优先级

> **2026-04-28 review 决策（保留原方案）**：曾考虑"自动化任务直接 fork 跳过 compact"，理由是 fork 是摘要继承设计的核心。
> 重新解耦"普通对话 vs 自动化任务"后回到原方案：**自动化任务无 timeline UX 担忧**（没人在 watch），且
> compact 是单点 RPC，活动部件比 fork 少（不开新进程 / 不重新初始化），失败模式更简单。fork 兜底链路已足够覆盖 compact 失败。
> 直接 fork 的理由（"compact 改 timeline 用户切回奇怪"）属普通对话 UX 视角，不适用本计划范围。

### 3.1 token level → critical（已落地于 1.4c）
1. 如果 `canCompact`（capability 含 `context_compact`）→ 调 `threadStore.compactThread(threadId)`（**底层 RPC，不复用 useThreadActions** —— 那一层是为前台用户写的）
   - 成功：done，不续接
   - 失败：healthcheck（thread 已自然恢复则 skip）→ 否则转步骤 2
2. `continueTaskById(threadId)` fork 续接（带 retry 1 次 + 1.5s 延迟 + retry 前 healthcheck）

### 3.2 status → error（已落地于 1.4c）
1. 调 `threadStore.recoverThread(threadId)`
   - 成功：done，不续接
   - 失败：healthcheck → 否则转步骤 2
2. `continueTaskById(threadId)` fork 续接（同 §3.1 retry / healthcheck 链）

### 3.3 焦点处理（已落地于 1.4b）
- `continueTaskById` 内判断：如果 `selectedThreadId.value === sourceThreadId`，新 thread 走默认 saveActiveThread（焦点跟随）
- 否则传 `skipSaveActive: true`，新 thread **不抢焦点**（用户在别处工作时 task 静悄悄续接）
- `startThread(options.skipSaveActive)` 是 1.4b 在 thread-actions-helpers 加的小开关

### 3.4 失败可见性（已落地于 1.4c + 1.4d）
- 单次自动续接失败 → `logWarn` + 写入 `failedAutoContinueByThread`（前端本地 ref<Map>）；UI 反馈靠现有 thread 卡片 status/token 视觉染色
- 全局保险丝触发 → `logError + window.alert(...)`（一次性，相同 reason 不重弹）
- 1.4d 在 `ContextUsageBanner` 渲染失败提示行 + "一键重试"按钮，调 `retryAutoContinue(threadId)`（绕过自动调度闸门，是用户主动入口）

### 3.5 偏好开关（已落地于 1.5）
- watch 跨入 + taskId 过滤后判 `prefRef.value`：false → 整条链路跳过（连 signal 日志都不打，避免"看似在做事"）
- prev level/status 仍正常更新，开关切换后下次跨入识别正确
- `retryAutoContinue` **不**受 pref 控制（用户主动点重试 = 已知情同意）
- UI 入口：`AutoContinuePrefCard` 挂在 `DagsPage` 顶部（侧边栏 DAG 一级页）；选位理由是 DAG 页面的核心用户 = 自动化任务用户，普通对话用户根本不打开此页，控件对其透明；卡片文案明确"不影响普通对话"消歧义

### 3.6 代码层面识别的设计风险（含实施去向）

> **2026-04-29 复核纠正**：之前此节写的"4-28 fuse_blown 15 次 / 4-29 4 次"是 grep 误判（用户点击含 fuse_blown 字样文本触发的 `chat.fileRef.click.no_ref_node` 事件被误算）。实测 4-28 ~ 4-29 全部日志里前端 `auto_continue.*` 真实事件 = **0 条**——Phase 1.x 自动续命系统建好后**还没在生产里真跑过**。本节按 typical 标准筛选后分类到 Phase 1.7（已规划）/ Phase 1.8（待 1.7 ship 后启动）/ 仅登记（不在续命系统范畴）三档，详见末尾"实施去向汇总"。

**风险 1 · F2 → recover 假成功死循环（已证伪，不实施）**

- 假说：若 `recoverThread` 表面成功但底层 state 未持久化，F2 会反复触发→"假成功"→链路结束→下次 status 同步又 'error'→再次触发，最终 60s/20 跳闸
- 代码核实：`internal/module/thread/lifecycle.go::Resume` 确实有 swallow 模式（`persistThreadState` 失败仍 `publishThreadStarted` + return resumed），日志 "thread: resume persist failed, continuing with event emission" 在生产里出现 14 次属实
- **但**：`thread/recover` RPC 走的是 `internal/module/thread/lifecycle_fork.go::Recover`，**不是** lifecycle.go::Resume；`lifecycle_fork.go::Recover` 已正确返回 persist 错误（行 153-154），**没有 swallow**。所以 F2 路径上不会出现"假成功"
- 结论：bug 不存在于 F2 路径。Resume 的 swallow 是另一个独立设计选择（startThread/startup 流程），不影响自动续命。**不进 Phase 1.8，仅保留登记**

**风险 2 · 用户主动 stop 被 F2 覆盖（typical · 进入 1.8a）**

- 假说：`stopThread` 自身不写 status，由后端推回；若 interrupt 处理过程中 status 瞬间到 'error'，F2 会自动 fork 覆盖用户意图
- 当前无 `manualAbort` 抑制位区分"用户主动停"vs"真错误"
- 生产观察：F2 链路全程 0 触发（前端 `auto_continue.*` 事件不持久化，观察不到 ≠ 不发生）
- 结论：typical 必发场景（用户主动停的语义保护是消费侧基本诉求），**进入 Phase 1.8a 实施**（含共享文件持久化，刷新/多 tab 一致）

**风险 3 · 永久错误未识别 → 重试浪费配额（typical · 进入 1.8b）**

- 假说：401 / 403 / quota_exhausted / payment_required / **context_length_exceeded** 等永久错误，B1 重试 3 次浪费配额；F2 看到 status='error' 也会一直 fork 至 fuse_blown
- 当前 B1 `isRetryableLaunchError` 只识别 6 个 transient 关键字，未做 transient/permanent 分类
- 5 类永久错误：401（鉴权失败）/ 403（权限拒绝）/ quota_exhausted（配额耗尽）/ payment_required（订阅过期）/ context_length_exceeded（prompt 已超长）
- 生产观察：日志 grep 全 0；但 typical 必发——任何调外部 API 系统都得有错误分类，且 context_length_exceeded 会随用户阈值配置变化而出现
- 结论：**进入 Phase 1.8b 实施**

**风险 4 · 429 rate limit 短退避不够（typical · 进入 1.8c）**

- 假说：B1 退避是 `attempt × 2s`（最长 4s），429 通常需要 60s+
- 生产观察：日志 grep 0；但 429 是任何 LLM API 调用都会撞的限流，typical 必发
- 结论：**进入 Phase 1.8c 实施**

**风险 5 · handoff 文件缺失时 fork 失败浪费一次配额（typical · 进入 1.8d）**

- 假说：fork 前不验证 `handoff/tasks/<taskId>.md` 存在性，缺失时 fork 失败但已消耗一次 per-thread 闸
- 生产观察：F2 链路 0 触发；但 fork 前预检文件是基本工程实践，typical
- 结论：**进入 Phase 1.8d 实施**

**风险 6 · thread 还活着但事件源断了（已实证，进入 Phase 1.7）**

- 假说：LLM stream 中断 / 工具回执丢失 / 网络抖动导致后端推送停止；thread 进程没崩，status 仍是 working 类（`thinking` / `responding` / `running` / `editing` / `syncing`）；token 用量不到 critical（F1 等不到），status 也不变 'error'（F2 等不到）→ **静默死锁**
- 代码核实：`stores/thread-sync-helpers.js::handleBridgeEvent` (line 335) 是后端事件唯一入口，所有 thread 相关事件（tokenusage/updated、turn/completed、item/started、item/completed、textdelta、outputdelta、thread patch 等）都过这里且带 `eventThreadId`。可在 watchdog 私有状态中统一记录 `lastBackendEventAtByThread[tid] = now`，任何后端推送都会刷新；stream 真断时该时间戳停止更新；不要新增 thread store runtime key，避免触碰 `THREAD_STORE_RUNTIME_STATE_KEYS` 白名单。
- 生产观察：用户实际遇到，普通对话 thread 工作中静默 1 分钟以上无反馈；用户手动发"继续"消息后 agent 立刻恢复；无 taskId 场景（§2.3 task-only 过滤导致 F1/F2 都不监控这个 thread）
- 结论：**已实证，进入 Phase 1.7**——独立于 useAutoContinue 链路（不受 §2.3 task-only 约束），监所有 thread

**风险 7 · agent 假完成（Phase 3.10 部分覆盖）**

- 现象：thread 回 idle，agent 输出"我做完了 X"但用户判断没真完成
- 之前结论是"不在续命系统范畴"——Phase 3 合并 sharedfile 落盘后**结论可收回**：通过 Phase 3.10 长任务进度协议**部分覆盖**
- 覆盖机制：约定 agent 自宣告完成时写 `_internal/done/<threadId>.md`；watchdog 看到 done.md 才结束监控，没写就视为未完成
- 仍不覆盖：agent **故意写 done.md 但实际没做完**（需要更上层的评估/review 系统）；这是诚信问题不是续命问题

**风险 8 · agent 死循环 / 反复调同一工具（不在续命系统范畴 · 仅登记）**

- 现象：agent 反复调同一工具或在小范围打转
- 续命系统不覆盖：状态正常 + 事件源活跃，watchdog 也不会触发
- 出口：agent 行为监督层 / review 层，**不在本计划范围**

**风险 9 · fuse 自愈静默（typical 可观测性 · 进入 1.8e）**

- 现象：全局 fuse 跳闸时一次性 alert + logError，60s 滑窗自愈时**无任何提示**
- 运维 / 用户不知道当前是否还在保险丝里
- 修法：自愈时打一行 `auto_continue.fuse_recovered` info 日志，几行代码
- 结论：**进入 Phase 1.8e 实施**

**实施去向汇总**

- 风险 1：已证伪，仅登记
- 风险 2/3/4/5/9：→ Phase 1.8 实施（5 条子项 1.8a-e）
- 风险 6：→ Phase 1.7 实施（已规划）
- 风险 7：→ Phase 3.10 部分覆盖（progress.md / done.md 协议）
- 风险 8：不在续命系统范畴，登记给未来 agent 行为监督层引用

### 3.7 抑制规则（消费侧反推 · 待补）

> 当前架构是**白名单触发**——看到信号（5 条触发条件）就续。但消费侧反推后发现真正的缺口是**黑名单抑制**——什么时候不该续。两者缺一不可。

**抑制层 vs §4 闸门**：闸门是"触发了但限频"（per-thread 1 次 / 全局 60s/20），抑制是"看到信号但根本不该触发"。两者目标不同：
- 闸门防失控（spawn 风暴 / 死循环）
- 抑制防误判（永久错误重试浪费配额 / 用户主动停被自动接管）

**该有的抑制（按消费方紧迫度排）**

| 抑制场景 | 影响的消费方 | 现状 | 落到 |
|---|---|---|---|
| 永久错误（401 / 403 / quota_exhausted / payment_required / context_length_exceeded）| A 类（无人值守任务）| 完全不识别，理论上会重试到 fuse_blown | §3.6 风险 3 → Phase 1.8b |
| 用户主动 stop（manualAbort）| C / D 类（用户在前台 / 主动停）| 无 manualAbort 信号，stop 后若 status='error' 理论上会被 F2 续命覆盖 | §3.6 风险 2 → Phase 1.8a |
| 续命链路深度 | A 类 | **已被现有闸隐式覆盖**：per-thread 1 次 + 全局 60s/20，链长占 N 个配额自然受限 | 不补 |
| 任务已 done | 全部 | **已天然满足**：F2 watcher 只看 status='error'，done/idle 不触发 | 不补 |

**明确不做的抑制（避免过度设计）**

- 任务"软失败"自动重试（agent 自己回复"做不了"）：判定太复杂，false positive 后果比 false negative 还大
- per-task 滑窗 / 累计计数：§4.3 已用 YAGNI 排除
- 自动换 provider：跨 provider 状态复杂，先做"识别永久错误并停"，不做"自动切换"
- 任务总时长上限自动停：用户要的是"自动续"，停应该靠预算闸而不是时长

---

## 4. 频率限制（精简版 · 2 层闸）

> **2026-04-28 review 决策**：原计划 7 层闸（per-task 滑窗 / per-task 累计 / recover 冷却 / 用户静默期）属于过度设计 ——
> 真实失控防御 90% 已被 per-thread 1 次 + 全局保险丝覆盖；per-task 闸又被 §4.3 自己证明对多子 agent 场景无效。
> 按 YAGNI 精简：等真出现 corner case 再 case-by-case 加，不预先建仓库。

### 4.1 闸门设计

| 闸 | 维度 | 数值 | 触达后行为 |
|---|---|---|---|
| 偏好开关 | `taskHandoff.autoContinueOnAlert`（在 useAutoContinuePref，不在闸门模块） | bool | false 时整条链路跳过 |
| 单 thread 续接上限 | per source threadId | 1 次 | 静默 skip（reason `thread_already_continued`） |
| 全局保险丝 | 全应用 | 1min 内 20 次 | 静默 skip + `fuseBlown:true`，由 useAutoContinue 决定是否 alert / logError |

### 4.2 状态结构（在 `auto-continue-gating.js` 闭包内）
```js
const continuedSourceThreadIds = new Set();   // per-thread 1 次
const globalLog = [];                          // number[] 全局保险丝时间戳

const GLOBAL_WINDOW_MS = 60 * 1000;
const GLOBAL_WINDOW_MAX = 20;
```

### 4.3 多子 agent 场景与未来扩展
10 个子 agent × per-thread 1 次 = 10 次（每 thread 各自独立），全局保险丝 60s/20 兜底。
如未来出现"主任务反复 spawn 失败子 agent"的级联场景，再加 Phase 4（rootTaskId 分组，多 agent 限频）。
已评估但未启用的 4 层闸（per-task 滑窗 / per-task 累计 / recover 冷却 / 用户静默期）作为扩展点，按需启用。

---

## 5. Step 清单（执行顺序）

### Phase 1.x —— 自动续接核心调度（agent 任务可用）

| Step | 改动 | Commit message | 验证 |
|---|---|---|---|
| **1.1** | `useTaskHandoff` 抽 `buildTaskFromRuntime(runtime)` helper + 加 `continueTaskById(sourceThreadId)`，原 `continueTask` 改 wrapper | `refactor(chat): useTaskHandoff 拆出 continueTaskById` | 新建 `use-task-handoff.test.js` 全过；现有"以此新建任务"按钮仍正常 |
| **1.2** | 加 `settings.taskHandoff.autoContinueOnAlert` 偏好（默认 true）+ `useAutoContinuePref` composable | `feat(prefs): 加自动续接偏好` | prefs 读写测试 |
| **1.3** | 新建 `useAutoContinue` composable：watch 所有 task thread 的 token level + status，**只 logInfo 不调动作**；在 `UnifiedChatPage` 接线 | `feat(chat): useAutoContinue 调度器（仅日志）` | 后台 task thread 拉到 critical，控制台看到 `auto_continue.signal` 日志 |
| **1.4a** | 抽 `auto-continue-gating.js`（精简 2 层：per-thread 1 次 + 全局保险丝）+ 单测 | `feat(chat): 自动续接 §4 闸门模块（精简 2 层）` | 闸门 7 case 单测 |
| **1.4b** | `useTaskHandoff.continueTaskById` 加焦点判断；`startThread` 加 `skipSaveActive` option | `feat(chat): continueTaskById 不抢后台焦点` | use-task-handoff.test.js 加 3 case |
| **1.4c** | `useAutoContinue` 接通真实动作链（compact/recover/fork + retry + healthcheck）+ 接闸门 + 全局保险丝 alert + `failedAutoContinueByThread` + `retryAutoContinue` | `feat(chat): 自动续接接通真实动作链路 + 闸门 + 兜底重试` | 18 case 覆盖每条决策路径 + retry + 闸门 + inflight |
| **1.4d** | `ContextUsageBanner` 加 `failedInfo` / `retrying` props + reason 翻译表 + 一键重试按钮；UnifiedChatPage 接线 | `feat(chat): 自动续接失败一键重试入口` | 失败行渲染 + reason 翻译 + 重试 emit + 双 section 共显 |
| **1.5** | 偏好关闭跳过整条链路 + 单测；新建 `AutoContinuePrefCard` + `DagsPage`，把 toggle 挂在侧边栏 DAG 页顶部 | `feat(chat): 自动续接尊重偏好开关 + 侧边栏 DAG 页顶部 toggle` | 关闭后 critical / status_error 时无任何动作；DAG 页能切换偏好 |
| **1.6** | **前置契约**：新增 auto-continue state 专用 UI RPC（读/写/删），只允许 `_internal/auto-continue/state/<threadId>.json`；写入前做 JSON schema 校验，底层走 sharedfile store Upsert/Delete；不得复用 MCP `shared_file_write`（它是 agent 工具且 `handoff/tasks/` 有系统保护） | `feat(chat): auto-continue state RPC 契约` | 单测：合法 path 可 get/upsert/delete；path traversal / 非 `_internal/auto-continue/state/` prefix 被拒；schemaVersion/threadId 校验失败被拒；删除调用真实 Deleter |

### Phase 1.7 —— Thread Watchdog（事件停滞检测）

> 来源：消费侧实证（§3.6 风险 6）——thread 工作中事件源断了，F1/F2 都看不到。独立于 useAutoContinue 链路，**不受 §2.3 task-only 约束**，监所有 thread。

**架构**：handleBridgeEvent 入口只戳真实后端事件时间 `lastBackendEventAtByThread` → 60s 扫描 → `status ∈ WATCHDOG_WORKING_STATUSES && now - lastBackendEventAt > 180s && now - lastWatchdogPokeAt > 60s` → 分流（task thread 自动戳 "继续" / 普通对话写 stuckByThread 让 banner 渲染按钮）→ 累计 ≥ 5 次仍未完成 → 停止自动戳 + banner 升级"建议人工介入"（1.7f 兜底）

**watchdog 状态模型（开工前固定，不再混用 lastEvent）**：
```js
const WATCHDOG_WORKING_STATUSES = new Set(['thinking', 'responding', 'running', 'editing', 'syncing']);
// starting 暂不纳入，避免 pending launch / 首 turn 初始化阶段误戳；waiting 暂不纳入，避免覆盖用户输入/审批等待；error 交给 F2。
const lastBackendEventAtByThread = new Map(); // 只由 bridge event 更新
const lastWatchdogPokeAtByThread = new Map(); // 只由 watchdog 自动戳/用户手动继续更新，用于节流
const cumulativePokeCountByThread = new Map();
const stuckByThread = new Map();
```
- 偏好关闭时仍记录 `lastBackendEventAtByThread`，只是不触发自动动作；否则开关重新打开后会丢失真实事件基线。
- watchdog 自动发 "继续" 后只更新 `lastWatchdogPokeAtByThread`，**不得覆盖** `lastBackendEventAtByThread`。

| Step | 改动 | Commit message | 验证 |
|---|---|---|---|
| **1.7a** | `thread-sync-helpers.js::handleBridgeEvent` 入口加真实后端事件戳点：`eventThreadId` 非空时 `lastBackendEventAtByThread.set(tid, Date.now())`；状态放在 `useThreadWatchdog` 私有闭包或专用 module，不新增 thread store runtime key，避免触碰 `THREAD_STORE_RUNTIME_STATE_KEYS` 白名单 | `feat(thread): 后端事件 lastBackendEventAt 戳点` | 单测：每收到带 threadId 的 evt 都刷新 lastBackendEventAt；threadId 缺失时不更新；偏好关闭时仍记录真实事件时间 |
| **1.7b** | 新建 `useThreadWatchdog` composable：`setInterval 60s` 扫所有 thread（最坏延迟 = 阈值 + 扫描 = 240s）；触发条件固定为 `WATCHDOG_WORKING_STATUSES` + `now - lastBackendEventAt > 180s` + per-thread poke 节流；分流：有 `runtime.taskId` → 调 `sendMessage(tid, "继续")` + log `thread_watchdog.poke.task_auto`；无 taskId → 写 `stuckByThread.set(tid, ts)` 留给 banner；触发后只更新 `lastWatchdogPokeAtByThread`，不改真实后端事件时间 | `feat(chat): useThreadWatchdog 事件停滞检测 + 双分流` | 单测：working 状态 + 180s 无后端事件 → task 自动戳 `"继续"` / 普通对话只写 stuckByThread；自动戳后 lastBackendEventAt 不变、lastWatchdogPokeAt 更新 |
| **1.7c** | 闸门 + 偏好。**节流**（不是永久锁）：per-thread 60s 内最多 1 次，全局 5min 内最多 10 次保险丝；偏好**单列** `taskHandoff.threadWatchdog`（默认 true），独立于 `autoContinueOnAlert`；偏好只控制是否自动戳/写 stuck，不控制 bridge event 戳点 | `feat(chat): watchdog 闸门 + 单列偏好` | 单测：60s 内同 thread 第二次被节流；5min 全局 10 次后保险丝跳闸；偏好关闭 → 不自动戳、不写 stuck，但 lastBackendEventAt 仍记录 |
| **1.7d** | `ContextUsageBanner` 加 `stuckInfo` props（thread id + 卡住时长）+ "继续"按钮，参考 1.4d `failedInfo` 模式；UnifiedChatPage 接线 `retryStuckThread(tid)` → `sendMessage(tid, "继续")` + 清 stuckByThread | `feat(chat): 普通对话卡住一键继续按钮` | 手工：让普通对话静默 180s → banner 出现"agent 似乎卡住 X 分钟" → 点击 → sendMessage 发出 |
| **1.7e** | UI 入口：`AutoContinuePrefCard` 同位置加 watchdog toggle，或合并卡片（1.7d 时再定 UX）；`DagsPage` 顶部展示 | `feat(chat): watchdog 偏好开关 UI` | 切换开关 → watchdog 启停（关闭时 setInterval 不跑） |
| **1.7f** | 累计上限兜底：watchdog 内 `cumulativePokeCountByThread` Map，per-thread 累计戳 ≥ 5 次后**停止自动戳**；改写 `stuckByThread.set(tid, { kind: 'cumulative_limit', count: n })` 让 banner 升级文案为"已尝试 N 次仍未完成，建议人工介入"+ 一键"再戳一次（force）" / "标记完成（清状态）"按钮；计数重置时机：用户**主动**发任意 turn / 用户点"标记完成" / 同 thread 新 task 派发；当前 `sendMessage` 不支持把 `source` 透传到 `turn/start`，先用 watchdog 私有 `autoPokeRequestIds`/调用路径区分自动戳与用户 turn，除非本步显式扩展 `sendMessage` payload。**state 持久化依赖 Phase 1.6**：累计计数写 `_internal/auto-continue/state/<threadId>.json`（5s 节流），watchdog 初始化时读取，防刷新丢失 | `feat(chat): watchdog 累计上限兜底 + banner 升级 + state 持久化` | 单测：累计 5 次后自动戳停止 + stuckByThread 带 cumulative_limit；用户主动 turn 后计数重置；watchdog 自动戳不重置；点"标记完成"清状态；**刷新页面后累计计数保留** |

**和 useAutoContinue 的独立性**：
- 不共享 state（`lastBackendEventAtByThread` / `lastWatchdogPokeAtByThread` / `stuckByThread` / `inflight` / `failedRef` 都是 watchdog 自己的）
- 不共享闸门（gate 模块新写：节流逻辑而非 1 次锁定）
- 不共享偏好（单列开关）
- 复用：`sendMessage` / `ContextUsageBanner` 渲染框架 / log infrastructure / **Phase 1.6 的 state RPC（与 1.8a 共用 `_internal/auto-continue/state/<threadId>.json`）**

**共享文件 state 文件 schema**（1.7f + 1.8a 共用；依赖 Phase 1.6 专用 RPC）：
```json
{
  "schemaVersion": 1,
  "threadId": "thread_xxx",
  "manualAbortAt": null,
  "manualAbortSource": "ui_stop",
  "watchdogPokeCount": 3,
  "lastWatchdogPokeAt": 1745912345678,
  "lastBackendEventAt": 1745912345678,
  "lastUpdatedAt": 1745912345678
}
```
- 路径：`_internal/auto-continue/state/<threadId>.json`（与 Phase 3 `_internal/` 路径约定一致）
- 写：调 Phase 1.6 `auto-continue/state/upsert`，5s 节流（任一字段变化都批量 flush），写前校验 `schemaVersion/threadId` 与路径一致
- 读：调 Phase 1.6 `auto-continue/state/get`，组件初始化（useAutoContinue / useThreadWatchdog）时回填前端 state
- 清：thread archive / delete 时调 Phase 1.6 `auto-continue/state/delete`；不要写不存在的 `shared_file_delete` 工具名

### Phase 1.8 —— 错误分类 + 抑制规则（待 1.7 ship 后启动）

> 来源：§3.6 风险 2/3/4/5/9——typical 必发场景，按消费侧痛点排优先级。**与 Phase 1.7 先后实施**（1.7 先 ship，1.8 后续），不并行以免叠加风险。

| Step | 优先级 | 改动 | Commit message | 验证 |
|---|---|---|---|---|
| **1.8a** | P0 用户意图保护 | manualAbort 抑制位（风险 2）。**只在用户显式 stop 时写入**：`stopThread` 的 `options.source === 'ui_stop'` 才允许 `manualAbortByThread.set(threadId, ts)` + Phase 1.6 state RPC 持久化；`compactThread` 内部 interrupt / recovery interrupt / 无 source 的系统 stop 不得写 manualAbort，避免误伤 compact/recover。F2 watcher 判 status='error' 之前先检查此位，命中则跳过整条链路并 logInfo `auto_continue.skipped_manual_abort`；下一次 startThread / fork / 用户一键重试时清除该位（同时清共享文件字段）；初始化时读共享文件回填 state，**实现刷新页面 / 多 tab 一致** | `feat(chat): F2 用户主动 stop 抑制位 + state 持久化` | 单测：`source='ui_stop'` 后 status='error' 时 F2 不触发；`compactThread` 内部 stop 不写 manualAbort；手工：点 stop → 不被续命；**刷新页面后抑制位保留**；多 tab 一致 |
| **1.8b** | P1 配额保护 | 永久错误识别 5 类（风险 3）：401 / 403 / quota_exhausted / payment_required / **context_length_exceeded**。**两层各做一份**：(1) 后端 `isRetryableLaunchError` 拆为 `classifyLaunchError` 返回 `transient/permanent/unknown`，permanent 直接跳出重试循环；(2) 前端 `classifyError` 加 `permanent_reason` 字段；命中 → `failedAutoContinueByThread` 写 reason `permanent_<type>`；ContextUsageBanner 加新文案 + 把"一键重试"换成"打开账号设置"指引（具体 UX 1.8 开工前再敲） | `feat(chat): 永久错误识别 5 类 + 抑制 + 用户告知` | 单测：5 种错误命中各自 reason；前端 banner 渲染永久错误文案 |
| **1.8c** | P1 配额保护 | 429 智能退避（风险 4）。**先补错误载体**：provider/launcher error envelope 至少保留 `status_code` / `retry_after` / `provider_error_type`，否则 `launchWithRetry` 只能看到普通 `error` 无法读取 header；`isRateLimited(err)` 识别 429 / `rate limit` / `too many requests`；命中 → `Retry-After` 优先（缺则默认 60s）→ `waitRetryBackoff` 内部按此时长退避而非 `attempt × 2s`；**仍走 `maxLaunchRetries=3` 计数**不另起独立 retry；3 次失败 reason 用 `rate_limited_retry_exhausted`，不是 permanent | `feat(orch): 429 rate limit 智能退避（Retry-After 优先）` | 单测：mock 429 + Retry-After → 退避时长等于 header；mock 无 header → 60s；不是 2s；3 次仍失败后 reason 为 `rate_limited_retry_exhausted` |
| **1.8d** | P1 资源保护 | handoff 文件预检（风险 5）。**后端**在 `task_handoff.go` 里加 `EnsureHandoffExists(taskId)`：fork 路径调起前 stat `handoff/tasks/<taskId>.md`，缺则返回明确错误；前端 `continueTaskById` 捕获该错 → `failedAutoContinueByThread` 写 reason `no_handoff_file` + 不重试 | `feat(handoff): fork 前 handoff 文件预检` | 单测：删除 handoff 文件 → fork 不发起 + reason `no_handoff_file` |
| **1.8e** | P2 可观测性 | Fuse 自愈日志（风险 9）。`auto-continue-gating.js` 在滑窗淘汰旧记录使 globalLog.length 重新 < GLOBAL_WINDOW_MAX 时，打一行 `auto_continue.fuse_recovered` info 日志（带恢复时间戳 + 上次跳闸 reason）；**仅日志不改行为** | `feat(chat): fuse 自愈日志` | 单测：fuse 跳闸 → 60s 后查询 → 看到 fuse_recovered 日志一次（不重复） |

**依赖序**：1.8a 必须先于 1.8b（manualAbort 是基础，永久错误命中要写 failedMap，需要先有 stop 抑制路径不污染数据）；1.8c/1.8d/1.8e 之间无依赖，可并行。

**和 useAutoContinue 的关系**：1.8a 在前端 state 加 manualAbort Map 后，需要修改 `useAutoContinue.js::handleStatusError` 入口；1.8b 修改 `classifyError` 函数；1.8e 修改 `auto-continue-gating.js`。**修改既有模块，不新建独立链路**——与 Phase 1.7 watchdog（独立链路）相反。

### Phase 2.x —— 让普通对话也能 opt-in（路径 1：promote-task）

| Step | 改动 | Commit message | 验证 |
|---|---|---|---|
| **2.1** | 后端 `thread/promote-task` RPC：把指定 thread 的 `autoTaskHandoff` 标 true、补 taskId、`ensureTaskHandoffShell` 初始化 handoff；重复 promote 必须幂等，不覆盖既有 runtime config | `feat(thread): promote-task RPC` | 单测：普通 thread promote 后补 taskId/handoffFile；重复 promote 返回同一 taskId；已有 runtime 字段保留；handoff shell 创建失败返回明确错误；promote 后 F1/F2 能识别 taskId |
| **2.2a** | 前端 thread 配置面板加"作为自动化任务运行" toggle，调上述 RPC（基础入口，常驻）；取消 opt-in 本期不做则 UI 必须只展示单向升级语义 | `feat(chat): 普通对话升级为自动化任务（配置面板入口）` | 组件测试：按钮/开关只在普通 chat thread 可见；点击成功后 runtime 刷新、DAG 页偏好不影响普通对话；失败显示明确错误 |
| **2.2b** | 1.7d banner 卡住卡片加"升级为自动化任务"按钮（机会入口，痛点瞬间转化）；点击调 promote-task RPC 后立即把 thread 转入 F1/F2 监控；banner 同时提示"以后类似情况不用再点继续，会自动续接" | `feat(chat): 卡住时 banner 升级为任务入口` | 手工：普通对话卡住 → banner 出升级按钮 → promote 成功后清 stuckInfo 并进入 task watcher；失败不清 stuckInfo |

Phase 2.x 完成后，普通用户在某个对话里干活到一半决定"这事得多轮"，可以打开开关让它自动续接。开关之后行为和 agent 派发的任务一致。

### Phase 3 —— DAG 自动化 + 协作文件层（合并版）

> 来源：消费侧反思的两个相关缺口——
> (1) thread 内自动续命（Phase 1.7+1.8）做完后节点之间仍 100% 手动启动 = 半自动；
> (2) sharedfile 当前只在 DB，agent 协作要走 MCP 工具，不能用通用 Read/Write/Edit/Glob。
> 合并解决两层。**Source of truth = 磁盘**，DB 退为查询索引（updated_by/时间戳/List 加速）。
> 工程量 10-15 天；技术栈：后端 Go + sqlc + goroutine + 文件系统。
> 详见 team memory [dag-auto-spawn-next-node](../../../../.multi-agent/memory/projects/users-mac-desktop-agnet-super-dolphin/memory/team/project_dag_auto_spawn_next_node.md)。

**架构**：
- CompleteNode → 同事务 enqueue 下游 wakeup → dispatcher goroutine 拾起 → launch_agent → 失败 RetryWakeup 退避；reclaim cron 兜底
- sharedfile write：直接落盘 `<cwd>/.agnet/shared/<path>` (atomic tmp + rename) → store 同步更新 DB 索引
- agent 用通用 Read/Write/Edit/Glob 操作 `.agnet/shared/`；MCP `shared_file_read/write` 走同样路径
- DAG 节点间数据传递走 sharedfile 约定路径（dispatcher 把 prev 节点产出路径塞进下一节点 prompt）

**路径 schema**（5 个 prefix · sandbox 强制约束）：

| Prefix | 用途 | git 默认 |
|---|---|---|
| `handoff/tasks/<taskId>.md` | **系统 task handoff 文件**（现有 `defaultTaskHandoffPath`）；只能由后端 task handoff worker / promote-task 写，MCP `shared_file_write` 继续拒绝覆盖 | commit |
| `handoff/<taskId>/...` | agent/用户协作 handoff 空间；必须明确不包含 `handoff/tasks/` | commit |
| `dag/<dagKey>/<nodeKey>/...` | DAG 节点产出（mode 1）| commit |
| `inbox/<taskId>/...` | 用户给任务的输入（mode 4 异步交互）| commit |
| `reports/<taskId>/...` | agent 给用户/其他 agent 的产出（mode 4）| commit |
| `_internal/...` | 系统私有（1.7f/1.8a state、3.10 progress）| **ignore** |

**Phase 3 gate 化执行**：不再把 dispatcher + sharedfile + progress 一口气并行改。每个 gate 通过 DoD 后再进下一段。

| Step | Gate | 改动 | Commit message | 验证 / DoD |
|---|---|---|---|---|
| **3.1** | 3A dispatcher 基础 | **复用已存在**的 `ClaimDueWakeups` / `RetryWakeup` / `ReclaimStaleDispatchingWakeups` / `FOR UPDATE SKIP LOCKED` SQL；本步只补 mcp-orch dispatcher wiring 和 service 生命周期接线，不重复造 Claim SQL | `feat(orch): wakeup dispatcher 接线` | 单测：dispatcher tick 调 ClaimDueWakeups；无 due wakeup 空跑；claim 后带 claimed_by/lease；不新增重复 SQL/query 名称 |
| **3.2** | 3A dispatcher 基础 | Dispatcher goroutine：mcp-orch 启动时跑 ticker（10s/次），取 wakeup 后通过 launcher 调 `launch_agent`；成功 `MarkWakeupSent` + 后续 turn 绑定；失败调 `RetryWakeup` 或 `FailWakeup` | `feat(orch): wakeup dispatcher 主循环` | 集成：pending wakeup → sent；launcher transient fail → pending + next_retry_at；launcher permanent fail → failed；shutdown 停 ticker |
| **3.3** | 3A dispatcher 基础 | Reclaim cron：单独 ticker（30s/次）调既有 `ReclaimStaleDispatchingWakeups`，把 lease 过期 wakeup 回收为 `pending` | `feat(orch): wakeup lease 过期回收 cron` | 单测：未过期不回收；过期 dispatching 回 pending；被回收 wakeup 旧 fence 不能 MarkSent/Retry |
| **3.4** | 3B 节点控制 | `CompleteNode` 自动 enqueue 下游：节点完成后查 DAG 拓扑找 ready 下游节点（依赖已满足），对每个生成 wakeup；同事务保证一致性；payload 含 prev 节点产出路径 `dag/<dagKey>/<prevKey>/output.json` | `feat(orch): DAG 节点完成自动 enqueue 下游` | E2E：A→B→C 顺序；A→{B,C} 并行；重复 CompleteNode 不重复 enqueue（idempotency_key） |
| **3.5** | 3B 节点控制 | 节点失败重试策略：当前 SQL `RetryTaskDagWakeup` 有 `attempt_count < 8` 硬限制；本步必须把 max attempts 参数化或由 dispatcher 在调用 Retry/Fail 前按 DAG metadata `default_retry` 判断；累计失败 N 次 → 节点标 `failed`，整张图按 `FailFast` 决定是否中断 | `feat(orch): DAG 节点失败重试策略` | 单测：default_retry=0 直接 failed；default_retry=2 第 3 次 fail；fail_fast=true 阻断下游；fail_fast=false 只标当前节点 |
| **3.6** | 3C sharedfile 磁盘落盘 | sharedfile store 改造为**磁盘 source / DB 索引**：write 直接落盘 `<cwd>/.agnet/shared/<path>`（tmp + rename 原子）；read 优先磁盘 fallback DB；List 走 DB 索引（不扫磁盘）；DB 字段不变（path/content/updated_by/时间戳）但 content 大于阈值（如 100KB）时 DB 只存元数据 | `refactor(sharedfile): 磁盘 source + DB 索引模式` | 单测：磁盘/DB 一致；大文件 DB 不存正文；读 fallback DB；atomic rename 失败不污染旧文件 |
| **3.7** | 3C sharedfile 磁盘落盘 | 路径约定 schema + sandbox 边界：write/read 路径必须落在 `<cwd>/.agnet/shared/` 下，path traversal reject；前缀必须命中白名单；`handoff/tasks/` 保持系统保护，agent 工具不得覆盖 | `feat(sharedfile): 路径约定 + sandbox 边界` | 单测：`../`、绝对路径、未知 prefix、agent 写 `handoff/tasks/` 均拒绝；后端系统写 `handoff/tasks/` 仍可用 |
| **3.8** | 3C sharedfile 磁盘落盘 | `.gitignore` 默认策略：startThread / 首次 sharedfile write 时检测 `<cwd>/.gitignore`，ensure `.agnet/shared/_internal/` 在 ignored 列表（缺则追加并 logInfo）；其他 prefix 默认可 commit | `feat(sharedfile): .gitignore 默认策略` | 手工：无 .gitignore 自动创建/追加；已有规则不重复；只 ignore `_internal/` |
| **3.9** | 3D 协作产出 | DAG 节点间数据传递（mode 1）：Phase 3.4 enqueue wakeup 时把 prev 节点产出路径塞进 wakeup payload；dispatcher launch agent 时把路径插入下一节点 prompt（"上游产出在此路径，请 Read 读取"）；agent 用 Read 工具直接读，不需 MCP shared_file_read | `feat(orch): DAG 节点间数据传递（基于 sharedfile）` | E2E：上游写 output.json；下游 prompt 带路径；下游能读取并产出结果 |
| **3.10** | 3D 协作产出 | 长任务进度协议（mode 3 / 部分覆盖风险 7）：约定 agent 接到 watchdog 戳后写 `_internal/progress/<threadId>.md`（追加进度行）；agent 自宣告完成时写 `_internal/done/<threadId>.md`；watchdog 戳前对比 progress.md 增量决定是否升级 1.7f 累计上限；watchdog 看到 done.md 直接结束监控 | `feat(chat): 长任务进度协议 + 风险 7 部分覆盖` | 单测：progress 增量重置/降低升级；done.md 终止 watchdog；缺 progress 仍走累计上限 |
| **3.11** | 3E 收尾 | 端到端测试 + codemap 更新：DAG 顺序/并行/失败 fail-fast/重试成功/数据传递；sharedfile 磁盘/DB 一致性/sandbox/.gitignore；进度协议 progress/done | `test(orch): Phase 3 合并版端到端` | `go test` 相关包 + 前端相关测试全过；更新 wakeup 状态机与 sharedfile 落盘 codemap |

**与 Phase 1.7/1.8 的关系**：
- DAG 节点是 task thread → 节点内由 1.7 watchdog + 1.8 错误分类兜底（节点内出问题节点级解决）
- 节点级失败（launch 不起来 / 重试上限）走 3.5 → 节点 failed → DAG fail-fast
- 1.7f 累计上限 vs 3.10 进度协议**互补**：1.7f 数戳次数，3.10 看实际进展（progress 是否增长）；3.10 看到 done.md 时 1.7f 计数清零（任务真完成了）
- 1.7f/1.8a 的 state 文件落 `_internal/auto-continue/state/`，与 Phase 3 路径约定一致（不需要 migrate）

### Phase 4 —— 多 agent 限频（按需，原 Phase 3.x 内容）

只在 Phase 3 落地后实际看到多子 agent 失控的场景才动。两个候选方向：
- 方向 1：后端给子 thread spawn 时传 `rootTaskId`，前端按 rootTaskId 限频
- 方向 2：前端按 owner_thread_id 链反查找 root，按 root 限频

---

## 6. 工作纪律（针对本计划）

> 反思记忆 `feedback_verify_before_write.md` 已沉淀，复述要点：

1. **每步开工前先核 5 个事实假设**（写在工作笔记里给用户看），等用户点头才写代码
2. **测试 fixture 必须照抄生产数据形状**（`historyMessageToTimelineItem` 教训）
3. **声明完成前做 user-visible 验证**（不只是 unit test 通过），手工触发一遍 happy path 才算 done
4. **小步 commit，每步独立可 revert**
5. UI 行为以 template 里的 `@click` / 事件绑定为准，不能只看 setup export 列表
6. **新加：每次写代码 / commit 前必须 `git branch --show-current` + `pwd` 确认 worktree 与分支**（本次跑偏的教训）

---

## 7. 重要事实速查

| 事实 | 值 | 来源 |
|---|---|---|
| token level 4 档 | `normal/warn/danger/critical` | `format-utils.js::getTokenLevel` |
| 默认阈值 | `[70, 85, 95]`（用户可改） | `format-utils.js:95` |
| thread status 9 种 | `idle/starting/thinking/responding/running/editing/waiting/syncing/error` | `services/status.js:1-11` |
| **没有** `failed` 状态 | 失败用 `error` | 同上 |
| timeline item 形状 | `{ id, kind: 'user'|'assistant', text, ts }`（无 `role` 字段） | `thread-history-ui.js:186-193` |
| runtime 取法 | `threadStore.state.agentRuntimeById[threadId]` | `useThreadCards.js:189` 等多处 |
| compact 调用 | `compactCurrent()` —— `useThreadActions.js:282` | 已存在 |
| recover 调用 | `recoverSelected()` 内调 `recoverThread(threadId)` 或 `thread/recover` RPC | `useThreadActions.js:320` |
| handoff 文件路径 | `handoff/tasks/<taskId>.md` | `task_handoff.go::defaultTaskHandoffPath` |
| handoff 自动同步 | onTurnCompleted → taskHandoffWorker.Enqueue → `sharedFiles.Upsert` | `task_handoff.go:330+` |
| B1 launch retry 上限 | `maxLaunchRetries=3`，`launchRetryBase=2s`（实际等待 0/2s/4s） | `service_launcher_bridge.go:28-44` |
| B1 识别的错误关键字 | deadline exceeded / connection refused / transport unavailable / empty thread id / timed out / i/o timeout + `errors.Is(DeadlineExceeded/Canceled)`；**未覆盖 429 / quota / context_length_exceeded** | `service_launcher_bridge.go:507-528` |
| B5 peer supervisor 行为 | peer `Wait()` 返回 → `restartBackoff` → `launcher.Launch`；首次重启失败即 degraded 不再尝试 | `peer_supervisor.go:284-329` |
| wakeup 三件套（Phase 3 实施） | `ClaimDueWakeups` / `RetryWakeup` / `ReclaimStaleDispatchingWakeups` 已存在；缺的是 dispatcher 业务调用、CompleteNode 下游 enqueue、retry metadata 接线；**不要重复新增 Claim SQL** | `store_wakeup.go` / `task_dag_wakeup_dispatch.sql` / `dag.go` |
| auto-continue state RPC | 当前没有前端 sharedfile upsert 专用 RPC；Phase 1.6 必须先补 get/upsert/delete，路径限制在 `_internal/auto-continue/state/<threadId>.json` | `internal/module/memory/ui_rpc_mutations.go` 仅有 shared-file get/promote/delete |
| stopThread 与 F2 边界 | `stopThread` 自身不写 status，后端推回；**无 manualAbort 抑制位**让 F2 区分主动停 vs 真 error；1.8a 只能在 `options.source === 'ui_stop'` 时写 manualAbort，避免 compact 内部 stop 误判 | `thread-actions-helpers.js:583-625`, `compactThread` 内部调用 stopThread |

---

## 8. 当前进度

- [x] Phase 0：进展段抽取兼容生产 timeline 形状（commit `6765839`）
- [x] Phase 1.1：抽 `continueTaskById`（commit `020533d`）
- [x] Phase 1.2：偏好开关（commit `eb1eeda`）
- [x] Phase 1.3：调度器骨架（仅日志）（commit `b8e9669`）
- [x] Phase 1.4a：§4 闸门模块（精简 2 层）（commit `2102b9a`）
- [x] Phase 1.4b：continueTaskById 焦点判断（commit `2fdead9`）
- [x] Phase 1.4c：接通真实动作链 + 闸门 + retry/healthcheck + failedMap（commit `0650fca`）
- [x] Phase 1.4d：ContextUsageBanner 一键重试入口（commit `fb9b39d`）
- [x] Phase 1.5：偏好关闭跳过整条链路 + DagsPage 顶部 toggle（commit `42376cf`）
- [x] Phase 1.6：auto-continue state RPC 契约（commit `d603aaba`）—— `ui/auto-continue/state/{get,upsert,delete}`，路径 `_internal/auto-continue/state/<threadId>.json`，threadId 严格匹配 path 段 + schemaVersion=1 + 32 case 单测。1.7f / 1.8a 持久化已解锁。
- [x] Phase 1.7b：watchdog 状态模型重构（commit `a7a5d3fe`）—— `lastEventTsByThread` 改名 `lastBackendEventAtByThread`（语义：只由 bridge event 写）；删 `useThreadWatchdog.js:93` 重置污染（gate 私有 `lastPokeTsByThread` 已节流）；42 处 rename 跨 5 文件，23 case 单测全过。
- [x] Phase 1.7c：偏好关闭语义校准（无代码改动，事实核查显示已 100% 对齐 §1.7c：偏好单列 / bridge event 戳点不受控 / scan 内部 pref check 时机正确 / lastBackendEventAt 仍记录）。
- [x] Phase 1.7e：偏好切换联动 timer 启停（commit 待补）—— `useThreadWatchdog` 加 `watch(prefRef, ..., { flush: 'sync' })` 自动 start/stop；start() 加 pref=false guard 避免空轮询；2 个新 case + 旧 17 case 全过。
- [ ] Phase 1.7a：handleBridgeEvent 入口 lastBackendEventAt 戳点
- [ ] Phase 1.7b：useThreadWatchdog 事件停滞检测 + 双分流
- [ ] Phase 1.7c：watchdog 闸门 + 单列偏好
- [ ] Phase 1.7d：普通对话卡住一键继续按钮
- [ ] Phase 1.7e：watchdog 偏好开关 UI
- [ ] Phase 1.7f：watchdog 累计上限兜底（防永远戳完不成的任务）
- [ ] Phase 1.8a：F2 manualAbort 抑制位（待 1.7 ship 后启动）
- [ ] Phase 1.8b：永久错误识别 5 类 + 用户告知
- [ ] Phase 1.8c：429 智能退避（Retry-After 优先）
- [ ] Phase 1.8d：handoff 文件预检
- [ ] Phase 1.8e：fuse 自愈日志
- [ ] Phase 2.1：promote-task RPC
- [ ] Phase 2.2a：配置面板 toggle（基础入口）
- [ ] Phase 2.2b：banner 卡住升级按钮（机会入口）
- [ ] Phase 3.1：复用既有 ClaimDueWakeups，补 dispatcher 接线（[A] dispatcher 基础）
- [ ] Phase 3.2：wakeup dispatcher 主循环（[A]）
- [ ] Phase 3.3：wakeup lease 过期回收 cron（[A]）
- [ ] Phase 3.4：DAG 节点完成自动 enqueue 下游（[B] 节点控制）
- [ ] Phase 3.5：DAG 节点失败重试策略（[B]）
- [ ] Phase 3.6：sharedfile store 改造（磁盘 source + DB 索引）（[C] 落盘）
- [ ] Phase 3.7：sharedfile 路径约定 + sandbox 边界（[C]）
- [ ] Phase 3.8：sharedfile .gitignore 默认策略（[C]）
- [ ] Phase 3.9：DAG 节点间数据传递（基于 sharedfile）（[D] 协作）
- [ ] Phase 3.10：长任务进度协议（progress.md/done.md）（[D]，覆盖风险 7）
- [ ] Phase 3.11：Phase 3 合并版端到端测试（[E] 收尾）
- [ ] Phase 4：多 agent 限频（按需启动）

每个 Step 完成后回来勾选并补充实际 commit hash。
