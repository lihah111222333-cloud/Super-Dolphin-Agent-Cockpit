# 架构违规修复 — 全仓审查与加固

> 执行日期: 2026-04-28
> 范围: `super-agent-v3` 全仓 `internal/` 目录
> 提交: `79112d8`, `863c5ac`
> 影响: 21 文件, +208 / -32

---

## 一、背景与动机

在 V3 平台化迁移过程中，多个模块引入了大量 worker goroutine 模式（P22 P2 Finding 系列），但部分新增的 `go method()` 调用缺乏 panic 防护。Go 的 goroutine panic 会导致整个进程 crash，对长期运行的 agent 系统是致命风险。

同时，此前的 `framework_audit.md` 审计文档标注了多个维度的违规项，需要逐一验证并修复。

本次审查目标：
1. **全量扫描** 所有裸 goroutine，确保 panic recovery 覆盖率 100%
2. **验证** fx 生命周期配置、RPC 契约、数据层安全等架构约束
3. **修复** 所有可操作的违规项，并 **验证性关闭** 已实现但被错误标记的项

---

## 二、审计方法

### 2.1 静态分析流程

```
1. grep 扫描全仓 `go [a-z]` → 定位所有 goroutine 启动点
2. 交叉检查每个启动点是否存在 recover() / SafeGo 包装
3. AST 级别验证: archtest 套件 (TestSessionPrivateRuntimeAllowlist)
4. go build / go vet / go test 全量验证
```

### 2.2 覆盖范围

| 维度 | 检查项 |
|------|--------|
| Goroutine 安全 | `go func()` / `go method()` 无 recover |
| fx 生命周期 | StartTimeout / StopTimeout / OnStop 顺序 |
| RPC 契约 | stub 方法 / Middleware 签名 / 错误映射 |
| 数据层 | DBQuery READ ONLY / AILog keyword 下推 |
| 信号管理 | headless 双 signal 入口 |

---

## 三、发现清单

### 3.1 🔴 P0 — 影响进程稳定性（已全部修复 ✅）

#### P0-1: 17 处裸 goroutine 无 panic recovery

以下 goroutine 启动方法级调用在审计时无任何 `recover()` 或 `SafeGo` 包装：

| # | 模块 | 文件 | 函数 | 风险等级 |
|---|------|------|------|----------|
| 1 | memory | `team_sync_coordinator.go` | `go c.runWorker()` | 致命 — 长跑 worker |
| 2 | memory | `auto_dream_scheduler.go` | `go s.runWorker()` | 致命 — 长跑 worker |
| 3 | memory | `nested_ingest_worker.go` | `go w.runWorker()` | 致命 — 长跑 worker |
| 4 | memory | `extract_runtime.go` | `go func()` 后台提取 | 致命 — 状态泄漏 |
| 5 | thread | `agent_launched_worker.go` | `go w.runWorker()` | 致命 — 长跑 worker |
| 6 | thread | `task_handoff_worker.go` | `go w.runWorker()` | 致命 — 长跑 worker |
| 7 | rpc | `approval.go` | `go m.dispatchApproval(...)` | 高 — 审批卡死 |
| 8 | claudecli | `session_log_watcher.go` | `go w.pollLoop()` | 高 — token 追踪丢失 |
| 9 | mcpserver | `server.go` | `go s.readLoop(results)` | 致命 — MCP 断开 |
| 10 | bootstrap | `reconnect.go` | `go c.reconnectLoop(rootCtx)` | 致命 — 永久断连 |
| 11 | bootstrap | `heartbeat.go` | `go c.runHeartbeat(hbCtx)` | 高 — lease 续约丢失 |
| 12 | bootstrap | `lifecycle.go` | `spawnCallback` | 高 — 回调 panic |
| 13 | mcpserver | `http_transport.go` | `go server.Serve(listener)` | 致命 — HTTP 服务崩溃 |
| 14 | codexapp | `peer_supervisor.go` | `go s.superviseOne(...)` | 高 — wg 死锁 |
| 15 | codexapp | `session_runtime.go` | `go r.runHealthLoop()` | 高 — 会话监控失效 |
| 16 | codexapp | `session_runtime.go` | `go r.runRecoveryWorker()` | 高 — 恢复机制失效 |
| 17 | codexapp | `transport_process.go` | `go func() { cmd.Wait() }` | 中 — channel 未关闭 |

**影响分析**: 任何一个 panic 都会导致整个 agent 进程退出。对于 #1-6 的长跑 worker，panic 后 worker 消失且无日志报告，`doneCh` 永远不会关闭，导致 `Stop()` 超时或死等。

#### P0-2: fx.StartTimeout / fx.StopTimeout 未设置

```go
// 审计时状态
func newFXApp(options ...fx.Option) *fx.App {
    base := []fx.Option{
        Module,
        fx.Invoke(BindRuntime),
        // Caller-side start/stop deadlines are applied where App.Start/App.Stop are invoked.
    }
    // ...fx 使用内部默认 15s，但 platformconfig.StartupTimeout = 30s
}
```

**影响**: 虽然调用侧 (`runApp`, `RunDesktop`) 通过 `platformconfig.WithTimeout` 手动设置了 context 超时，但 fx 内部的超时使用默认值 15s，与配置的 30s 不一致。如果 fx 内部有独立超时判断路径（如 lifecycle hook 超时），会提前触发。

#### P0-3: MapCapabilityError 缺失

`contract.CapabilityError` 到 JSON-RPC 错误码的映射散落在各个 handler 中，无标准化函数。

---

### 3.2 🟡 P1 — 影响契约合规性

#### P1-1: `review/start` RPC 返回 ErrNotImplemented（已修复 ✅）

```go
// 审计时状态
"review/start": reviewStartHandler(),

func reviewStartHandler() handler.Func {
    return rpc.ThreadHandler(func(ctx context.Context, p threadIDOnlyParams) (any, error) {
        return nil, rpc.ErrNotImplemented("review/start is not yet implemented")
    })
}
```

**问题**: 注册了路由但返回自定义 `-31006` 错误码。客户端无法区分"方法存在但未实现"和"方法不存在"，容易产生误导性的集成错误。

#### P1-2: DBQuery READ ONLY（验证为已实现 ✅）

审计文档标注 "READ ONLY 加固未完成"，但实际代码已有三层防护：

| 层级 | 机制 | 位置 |
|------|------|------|
| 应用层 | `validateQueryText()` — SELECT/WITH 前缀限制 + DML 关键字正则黑名单 | `executor.go:123-137` |
| 表级 | `validateAllowedTables()` — 白名单表引用 + 危险函数正则检测 | `executor.go:170-191` |
| 事务层 | `OpenReadOnlyRows()` — `pgx.TxOptions{AccessMode: pgx.ReadOnly}` | `tx.go:38-43` |

**结论**: 审计文档的标注是错误的，三层防护已完整实现。

#### P1-3: AILog keyword 搜索未下推（验证为已实现 ✅）

审计文档标注 "keyword 搜索未下推"，但 sqlc 生成的查询已通过参数化 SQL 在数据库层过滤：

```go
// store/ailog/store.go
func (s *store) List(ctx context.Context, filter ListFilter) ([]AILog, error) {
    rows, err := s.q.ListAILogSystemLogs(ctx, sqlc.ListAILogSystemLogsParams{
        Keyword:    filter.Keyword,  // ← 已下推到 SQL
        LimitCount: filter.Limit,
    })
}
```

`ListAILogsByCategory` 同样支持 keyword 参数。

**结论**: 审计文档的标注是错误的，keyword 已在 SQL 层过滤。

#### P1-4: Headless 双 signal 入口（验证为已收敛 ✅）

```go
// runner.go:161
err := platformrunner.RunGroup(runCtx, p.Runners, platformrunner.GroupOptions{
    EnableSignals: false,  // ← RunGroup 不监听信号
})
```

信号由 fx 的 `app.Done()` (`<-chan os.Signal`) 独立管理，两个入口不产生竞争。

#### P1-5: RPC Middleware 签名偏差（P2 推迟）

- 契约定义: `type Middleware func(jrpc2.Handler) jrpc2.Handler`
- 代码实际: `type Middleware func(handler.Func) handler.Func`

影响低（内部使用），需要评估 `jrpc2` 中间件生态兼容性后再统一。

---

### 3.3 🟢 P2 — 低优先级（暂不处理）

| # | 项目 | 详情 |
|---|------|------|
| 1 | 19 个 fx 悬空 Provider | 5 bus emitter + 14 store 注册无消费者 |
| 2 | 9 个 fx Optional 依赖 | thread(6) + turn(2) + app(1) |
| 3 | 9 个零发布事件 | TurnStalled / TurnResumed 等已注册但从未发布 |
| 4 | RPC 方法覆盖率 41.56% | V2 154 方法仅 64 个同名对齐 |
| 5 | 20+ 处 `_ =` 残留 | 均为 file.Close / os.Remove 等低风险场景 |
| 6 | threadbinding V2 操作集 | 无独立 store，嵌入 thread service |

---

## 四、修复实施

### 4.1 Commit `79112d8` — 主修复批次

**标题**: `harden: panic recovery on all worker goroutines + fx timeout guards + MapCapabilityError`

**19 files changed, +202 / -26**

#### 4.1.1 Panic Recovery 模式

对所有 17 处裸 goroutine 采用统一的 defer/recover 包装：

```go
// 标准模式 — worker goroutine
go func() {
    defer func() {
        if rec := recover(); rec != nil {
            logger.Error("module: recovered worker_name panic", "panic", rec)
        }
    }()
    w.runWorker()
}()
```

```go
// 特殊模式 — peer_supervisor (需要保持 wg 平衡)
go func() {
    defer func() {
        if rec := recover(); rec != nil {
            s.logger.Error("peer_supervisor: recovered superviseOne panic",
                "peer", name, "panic", rec)
            wg.Done()  // ← 确保 panic 后 wg 不死锁
        }
    }()
    s.superviseOne(ctx, name, h, &wg)
}()
```

```go
// 特殊模式 — approval dispatch (需要释放 pending)
go func() {
    defer func() {
        if rec := recover(); rec != nil {
            m.logger.Error("rpc: recovered approval dispatch panic",
                "call_id", pending.callID, "panic", rec)
            m.failPending(pending, errors.New("approval dispatch panicked"))
        }
    }()
    m.dispatchApproval(ctx, bridge, server, pending, method, params)
}()
```

```go
// session_runtime.go — 可复用 helper
func (r *SessionRuntime) recoverWorkerPanic(name string) {
    if rec := recover(); rec != nil {
        r.logger.Error("session: recovered worker panic",
            "worker", name,
            "session_id", r.sessionID,
            "panic", rec,
            "stack", string(debug.Stack()),
        )
    }
}
```

#### 4.1.2 fx Timeout 护栏

```go
func newFXApp(options ...fx.Option) *fx.App {
    base := []fx.Option{
        Module,
        fx.Invoke(BindRuntime),
        fx.StartTimeout(platformconfig.StartupTimeout),   // 30s
        fx.StopTimeout(platformconfig.ShutdownTimeout),    // 30s
    }
    base = append(base, options...)
    return fx.New(base...)
}
```

同时应用于 `newDesktopFXApp`。

#### 4.1.3 MapCapabilityError

```go
// internal/platform/rpc/errors_helper.go (新增)
func MapCapabilityError(err error) *jrpc2.Error {
    var capErr *contract.CapabilityError
    if !errors.As(err, &capErr) {
        return nil
    }
    return &jrpc2.Error{
        Code:    jrpc2.Code(CodeCapabilityGate), // -31004
        Message: capErr.Error(),
    }
}
```

#### 4.1.4 archtest 适配

- `session_private_allowlist.go` — 更新 Symbol 从 `runHealthLoop` → `safeRunHealthLoop`、`runRecoveryWorker` → `safeRunRecoveryWorker`
- `server.go` — 提取 `startReadLoop()` 方法降低 `Run()` 圈复杂度从 CC=11 到 CC=9，满足 CodeSizeGuard 上限 CC≤10

---

### 4.2 Commit `863c5ac` — RPC 存根清理

**标题**: `rpc: remove review/start stub returning ErrNotImplemented`

**2 files changed, -6**

```diff
 // internal/module/turn/rpc.go
 return rpc.HandlerMapResult{Handlers: handler.Map{
     "turn/start":         turnStartHandler(...),
     "turn/steer":         turnSteerHandler(...),
     "turn/interrupt":     turnInterruptHandler(...),
     "turn/forceComplete": turnForceCompleteHandler(...),
-    "review/start":       reviewStartHandler(),
     "approval/respond":   approvalRespondHandler(approver),
 }}
```

```diff
 // internal/module/turn/rpc_helpers.go
-func reviewStartHandler() handler.Func {
-    return rpc.ThreadHandler(func(ctx context.Context, p threadIDOnlyParams) (any, error) {
-        return nil, rpc.ErrNotImplemented("review/start is not yet implemented")
-    })
-}
```

移除后，客户端调用 `review/start` 将收到标准 `MethodNotFound (-32601)`。

---

## 五、验证

### 5.1 编译 & 静态分析

```
$ go build ./internal/...     ✅ 零错误
$ go vet ./internal/...       ✅ 零警告
```

### 5.2 测试结果

| 包 | 结果 | 耗时 |
|----|------|------|
| `internal/module/memory` | ✅ PASS | 1.9s |
| `internal/module/memory/agent` | ✅ PASS | 1.2s |
| `internal/module/memory/nested` | ✅ PASS | 0.8s |
| `internal/module/memory/parse` | ✅ PASS | 2.4s |
| `internal/module/memory/retrieval` | ✅ PASS | 1.7s |
| `internal/module/memory/shared` | ✅ PASS | 1.9s |
| `internal/module/memory/team` | ✅ PASS | 3.3s |
| `internal/module/thread` | ✅ PASS | 2.7s |
| `internal/module/turn` | ✅ PASS | 0.7s |
| `internal/module/turn/observation` | ✅ PASS | 1.1s |
| `internal/platform/rpc` | ✅ PASS | 3.4s |
| `internal/provider/codexapp` | ✅ PASS | 11.6s |
| `internal/provider/claudecli` | ✅ PASS | 4.6s |
| `internal/mcpserver/common` | ✅ PASS | 4.1s |
| `internal/mcpserver/common/bootstrap` | ✅ PASS | 7.3s |
| `internal/app` | ✅ PASS | 4.1s |
| `internal/archtest` | ✅ PASS | 2.8s |

### 5.3 archtest 专项

| 测试 | 结果 | 说明 |
|------|------|------|
| `TestCodeSizeGuard` | ✅ PASS | 全部函数 CC ≤ 10 |
| `TestSessionPrivateAllowlistIntegrity` | ✅ PASS | 6/6 entry 验证通过 |
| `TestSessionPrivateRuntimeAllowlist` | ✅ PASS | 零未授权 goroutine |
| `TestBindRuntimeSafeGoHasSessionPrivateAllowlistEntry` | ✅ PASS | BindRuntime SafeGo 入口追踪 |

---


## 六、后续工作（P2 — 下个迭代）

### 6.0 ✅ 已完成 — goroutine panic recovery 补全（原 P0 遗漏）

> 在审查本文档时发现 4 个长跑 worker + 1 个 per-event goroutine 遗漏了 panic recovery，
> 与 P0-1 的 17 项同构但未被覆盖。已在同一迭代中修复并通过测试。

| # | 文件 | 修复 | 风险等级 |
|---|------|------|----------|
| 18 | `session_recovery_worker.go:118` | `Start()` → defer/recover 包装 | P0 — doneCh 死等 |
| 19 | `session_recovery_worker.go:225` | `drainPending()` per-event → defer/recover + inflight.Done() 保证 | P1 — wg 死锁 |
| 20 | `push_worker.go:106` | `Start()` → defer/recover 包装 | P0 — doneCh 死等 |
| 21 | `dispatch_worker.go:89` | `Start()` → defer/recover 包装 | P0 — doneCh 死等 |
| 22 | `config_fanout_worker.go:105` | `Start()` → defer/recover 包装 | P0 — doneCh 死等 |

验证:
```
go build ./internal/...                           ✅ 零错误
go vet ./internal/module/thread/...               ✅
go test ./internal/module/thread/...              ✅ PASS 0.9s
go test ./internal/platform/rpc/...               ✅ PASS 0.7s
go test ./internal/platform/hooks/...             ✅ PASS 0.6s
go test ./internal/platform/mcpcontrol/...        ✅ PASS 0.6s
go test ./internal/archtest/...                   ✅ PASS 3.4s (全部 archtest 通过)
```

---

### 6.1 待处理 P2 清单

| # | 项目 | 详情 | 预估 | 风险 | 状态 |
|---|------|------|------|------|------|
| **P2-A** | RPC Middleware 签名统一 | 当前 `type Middleware func(handler.Func) handler.Func`（L17）而非标准 `func(jrpc2.Handler) jrpc2.Handler`。内部仅自消费（`ThreadScope`/`CapabilityGate`/`Logging`/`Validate`/`CapabilityErrorMapper` 五个），不影响外部兼容性 | 1d | 🟢 低 — 纯内部类型 | ✅ Won't Fix — handler.Func 满足 jrpc2.Handler 接口 |
| **P2-B** | fx 悬空 Provider 清理 | 全仓 84 个 `fx.Provide`，`TestAppModuleGraphIsClosed` 通过（图闭合验证）。`fx.ValidateApp` 仅检查依赖存在性，不检测未消费 provider。手动审计需逐一追踪返回类型到 `fx.In` 消费点，预估 2-3h | 0.5d | 🟢 低 — 仅启动开销 | ⏸ 延后 — 无安全风险 |
| **P2-C** | 零发布事件修复 | **`TurnStalled`**: 有 6 个订阅者（eventsurface/observation/insight/trajectory/memory×2）但仅在 `subscribers_test.go:143` 构造发布，生产代码**从未 Publish**。**`TurnResumed`**: 有 2 个订阅者（eventsurface/uistate）但**全仓零构造**。其余 7 个待确认 | 1d | 🟡 中 — 死代码膨胀 + 功能缺失 | 需规格 |
| **P2-D** | threadbinding V2 操作集 | binding 操作嵌入 `thread/service` 和 `binding_registration.go`，无独立 store 抽象层。`stop_test.go` 使用 `stubThreadBindingStore` 覆盖了 12 个方法 | 2d | 🟢 低 — 功能可用 | 未启动 |
| **P2-E** | RPC 方法覆盖率提升 | V2 154 方法仅 64 个同名对齐 (41.56%)。需冻结 V2 方法规格后逐批迁移 | 3d | 🟡 中 — 阻塞客户端迁移 | 需规格 |
| **P2-F** | 测试空白区补齐 | 基线已建立（总覆盖率 71.4%）。< 60%: `platform/bus` 58.4%, `memory/shared` 58.9%, `platform/mcpcontrol` 59.4%。60-65%: `memory/team` 60.5%, `codexapp` 62.1%, `platform/rpc` 62.7%, `memory/retrieval` 64.7% | 2d | 🟡 中 — 回归风险 | 基线完成，待补测试 |
| **P2-G** | MapCapabilityError 接入 | ✅ 注入 `CapabilityErrorMapper()` middleware 到 `baseThreadHandler`，运行时 `contract.CapabilityError` 现正确映射为 `-31004 CodeCapabilityGate` | 0.5d | 🟡 中 — 错误码不一致 | ✅ 完成 |
| **P2-H** | 低风险裸 goroutine 覆盖 | ✅ 6 处已加固：`wails/lifecycle.go` (shutdown+quit)、`http_server.go` (Serve)、`claudecli/transport.go` (wait)、`codexapp/transport_process.go` (collectStderr+watchProcess)、`rpc/server.go` (serveConn) | 0.5d | 🟢 低 — 防御性加固 | ✅ 完成 |

### 6.2 P2-C 详细证据 — 零发布事件调研

#### TurnStalled — 有订阅无发布

**订阅者 (6 个)**:

| # | 订阅者 | 位置 | 用途 | 影响分析 |
|---|--------|------|------|----------|
| 1 | `eventsurface/bind.go:132` | RPC push 到 UI | `turn/stalled` 推送给前端 | 前端从未收到此事件 |
| 2 | `observation/subscribers.go:36` | 观测记录 | 调用 `contract.RecordTurnStalled` | 观测数据缺失 stall 维度 |
| 3 | `insight/collector.go:62` | 洞察收集 | 记录 stall 事件 | 洞察分析不含 stall |
| 4 | `trajectory_collector.go:404` | 轨迹收集 | `c.onTurnStalled` 标记轨迹终止 | 轨迹依赖 TurnCompleted/Interrupted |
| 5 | `memory/module.go:513` | 记忆层 — 后台提取 | `onTurnTerminated(ev.ThreadID, ev.TurnID)` — 作为 turn 终止信号触发记忆提取 | 记忆提取仅由 TurnCompleted/Interrupted 触发 |
| 6 | `memory/module.go:593` | 记忆层 — 上下文清理 | `ContextProvider.onTurnTerminated` — 清理 turn 级上下文缓存 | 上下文清理仅由 TurnCompleted/Interrupted 触发 |

**发布者**: **仅 `subscribers_test.go:143`**（测试构造） — 生产代码**从未** `event.Publish(d, turndto.TurnStalled{...})`

**event_map.go 注册**: L55 `typedEventType[turndto.TurnStalled](): publishEvent[turndto.TurnStalled]` — 表明该事件**设计上**应由 provider raw event 翻译而来，但 `translateCommonRawEvent` 中**无对应 case**

#### TurnResumed — 有订阅无构造

**订阅者 (2 个)**:

| # | 订阅者 | 位置 | 用途 |
|---|--------|------|------|
| 1 | `eventsurface/bind.go:135` | RPC push 到 UI | `turn/resumed` 推送 |
| 2 | `uistate/projector.go:31` | UI 状态投影 | `applyTurnResumed` 恢复 UI 状态 |

**构造点**: **全仓零结果** — `TurnResumed{}` 从未被创建

**event_map.go 注册**: L56 — 同 TurnStalled，设计存在但翻译逻辑未实现

#### 决策选项

| 方案 | 内容 | 工作量 | 风险 | 适用场景 |
|------|------|--------|------|----------|
| **A. 补发布逻辑** | 在 `translateCommonRawEvent` 中添加 `isStalledRawType` / `isResumedRawType` 分支，将 provider 原始事件翻译为 typed event | 0.5d | 🟡 需确认上游 provider (Claude/Codex) 是否发出对应 raw event type。若上游不发，补翻译逻辑也无效 | 上游 provider 确实会发出 stalled/resumed raw event |
| **B. 清理死代码** | 移除 6+2 个订阅回调、2 个 DTO、2 个 event_map 注册、2 个 eventsurface 方法常量 | 1d | 🟢 最安全 — 删除从未执行的代码路径。但如果未来需要，需要重新实现 | 确认上游 provider 不会发出对应事件 |
| **C. 保留预留** | 不修改代码。记录为已知技术债。订阅者代码不会执行，不影响运行时性能（`kelindar/event` 分发是 O(1)） | 0 | 🟢 零变更零风险。但死代码持续增加认知负担 | 不确定未来需求 |

> **建议**: 先确认 Claude CLI 和 Codex app-server 的 raw event 类型列表。如果上游确实无 `stalled` / `resumed` 事件，选 **B** 清理。如果不确定，选 **C** 暂留。

---

### 6.3 P2-D 详细调研 — threadbinding Store 独立化

#### 现状分析

`binding.Store` 接口已在 `internal/store/binding/contract.go` 定义，包含 **11 个方法**：

| 方法 | 语义 |
|------|------|
| `GetByProviderThread` | 按 provider+threadID 查找 binding |
| `Upsert` | 创建/更新完整 binding |
| `DeleteByAgentID` | 按 agentID 删除 |
| `UpdateSessionUUID` | 更新 session UUID |
| `SetArchived` | 设置归档状态 |
| `GetByAgentID` | 按 agentID 查找 |
| `BindAgentThread` | 绑定 agent ↔ thread |
| `UnbindAgentThread` | 解绑 |
| `ListAgentThreadBindings` | 列出所有绑定 |
| `GetThreadByAgent` | 查 agent 对应 thread |
| `UpdateAgentCwd` | 更新 agent CWD |

**已有独立 store 包**: `internal/store/binding/` 包含 `contract.go`、`store.go`、`types.go`

**消费者** (跨 5 个模块):
- `cachekeepalive/manager.go` — 使用 `GetByAgentID`, `BindAgentThread`, `UnbindAgentThread` 等
- `toolbridge/proxy.go` — 使用 `GetThreadByAgent`
- `unified/session_resolver.go` — 使用 `GetByProviderThread`, `GetByAgentID`
- `thread/service` — 通过 `binding_registration.go` 调用 `Upsert`, `DeleteByAgentID`, `SetArchived` 等
- `mcpcontrol/handlers.go` — 查询 binding scope

**测试覆盖**: `cachekeepalive/manager_test.go` 实现了 `bindingStoreStub` (12 方法)，`unified/session_resolver_test.go` 实现了 `stubBindingLookup`

#### 决策选项

| 方案 | 内容 | 工作量 | 风险 |
|------|------|--------|------|
| **A. 维持现状** | `binding.Store` 已是独立接口+独立包，消费者通过 DI 注入。当前架构已满足 SOLID 的接口隔离 | 0 | 🟢 |
| **B. 精炼接口** | 按消费者拆分为更小的 reader/writer 接口（如 `BindingReader`、`BindingWriter`、`ThreadAgentMapper`），减少各消费者的依赖面 | 1d | 🟡 需同步更新 5 个消费者的 DI 声明 |

> **建议**: 选 **A**。`binding.Store` 已独立存在于 `internal/store/binding/` 包中，11 个方法是数据库实体的自然映射，不存在过度耦合。文档中"P2-D: 无独立 store 抽象层"的原始描述与代码实际不符，store 层已到位。

---

### 6.4 P2-E 详细调研 — RPC 方法覆盖率

#### V3 方法清单（86 个生产方法，排除 test-only）

| 域 | 方法数 | 方法列表 |
|----|--------|----------|
| `thread/*` | 27 | start, stop, list, resume, fork, recover, handoff, archive, unarchive, delete, read, resolve, messages, loaded/list, name/set, config/get, config/set, model/set, clear, personality/set, approvals/set, compact/start, rollback, undo, mcp/list, skills/list, realtime/start, realtime/stop |
| `turn/*` | 3 | start, steer, interrupt, forceComplete |
| `approval/*` | 1 | respond |
| `ui/*` | 17 | state/get, sidebar/get, preferences/get, preferences/set, projects/get, projects/add, projects/remove, code/locate, code/open, code/save, dashboard/get, log, memory/get, memory/agent/get, memory/agent/save, memory/agent/delete, memory/entry/get, memory/entry/upsert, memory/entry/delete |
| `skills/*` | 14 | list, create, local/read, local/write, local/delete, remote/list, remote/read, remote/write, remote/export, candidate/list/pending, candidate/get, candidate/approve, candidate/reject, config/read, config/write, match/preview, summary/write |
| `skill/*` | 2 | list, expand |
| `dashboard/*` | 7 | query, prompts, skills, agent/detail, system/info, dags, logs |
| `config/*` | 1 | read |
| `prompts/*` | 3 | list, write, delete |
| `memory/*` | 1 | consolidate |
| `cronjob/*` | 4 | create, delete, get, list, update |
| `feedback/*` | 1 | record |
| `command/*` | 1 | exec |
| `tools/*` | 1 | lsp |
| **合计** | **~86** | |

#### V2 → V3 对齐分析

原始文档称"V2 154 方法仅 64 个同名对齐 (41.56%)"。但这里需要区分：

1. **V2 独有方法** — 可能已被 V3 的不同 URL 路径替代（如 V2 `model/list` → V3 `thread/config/get`）
2. **V3 新增方法** — V2 不存在的域（如 `cronjob/*`, `skills/candidate/*`, `tools/lsp`）
3. **真正缺失** — V2 有但 V3 确实未实现的功能

#### 决策选项

| 方案 | 内容 | 工作量 | 前提 |
|------|------|--------|------|
| **A. 冻结 V2 规格** | 导出 V2 完整方法列表，逐一标注 → V3 等价方法 / 废弃 / 待迁移 | 0.5d | 需获取 V2 方法注册表 |
| **B. 客户端驱动** | 以实际前端调用为基准，前端不调用的 V2 方法视为废弃 | 1d | 需审计前端 RPC 调用点 |
| **C. 增量补齐** | 每当前端报 method not found 时逐一补齐 | 0 (按需) | 适合已上线系统 |

> **建议**: 选 **B** 或 **C**。V3 的 86 个方法已覆盖核心业务域。在没有 V2 完整规格的情况下，以前端实际调用为基准进行对齐最高效。

---

### 6.5 P2-G 详细分析（已完成 ✅）

CapabilityError 在代码中有两个独立的处理路径：

```
路径 A (请求前 — 已生效 ✅):
  CapabilityGate middleware → HasCapability 检查 → -31004 CodeCapabilityGate

路径 B (运行时 — 已修复 ✅):
  provider 方法返回 contract.CapabilityError
    → thread/command.go 包装为 friendlyCapabilityError
    → CapabilityErrorMapper middleware 拦截
    → MapCapabilityError(err.Error()) 保留友好消息
    → -31004 CodeCapabilityGate
```

**已完成**: `CapabilityErrorMapper()` 注入 `baseThreadHandler` 链最外层 (commit 4d866da, 5fb2159)

---

## 七、审计修正

本次审查发现 `framework_audit.md` 中以下标注与代码实际不符：

| 原标注 | 实际状态 | 修正 |
|--------|----------|------|
| "DBQuery READ ONLY 加固未完成" | 三层防护已完整实现 | 标记为 ✅ |
| "AILog keyword 搜索未下推" | sqlc 参数化 SQL 已在 DB 层过滤 | 标记为 ✅ |
| "headless 双 signal 入口未收敛" | EnableSignals=false + fx 独立管理 | 标记为 ✅ |
| "P2-D: 无独立 store 抽象层" | `internal/store/binding/` 已包含完整 Store 接口 (11 方法) + 实现 | 标记为 ✅ |

建议后续审计采用代码验证而非文档推断，避免 false positive。

---

## 八、附录 — 修复文件总览

### Commit `79112d8` (19 files) — P0 主修复批次

| 文件 | 变更类型 |
|------|----------|
| `internal/app/app.go` | fx.StartTimeout / fx.StopTimeout |
| `internal/archtest/session_private_allowlist.go` | 重命名 safe wrapper symbols |
| `internal/mcpserver/common/bootstrap/heartbeat.go` | +panic recovery |
| `internal/mcpserver/common/bootstrap/lifecycle.go` | +panic recovery (spawnCallback) |
| `internal/mcpserver/common/bootstrap/reconnect.go` | +panic recovery |
| `internal/mcpserver/common/http_transport.go` | +panic recovery (Serve) |
| `internal/mcpserver/common/server.go` | +startReadLoop 提取 + panic recovery |
| `internal/module/memory/auto_dream_scheduler.go` | +panic recovery |
| `internal/module/memory/extract_runtime.go` | +panic recovery + 状态转换 |
| `internal/module/memory/nested_ingest_worker.go` | +panic recovery |
| `internal/module/memory/team_sync_coordinator.go` | +panic recovery |
| `internal/module/thread/agent_launched_worker.go` | +panic recovery |
| `internal/module/thread/task_handoff_worker.go` | +panic recovery |
| `internal/platform/rpc/approval.go` | +panic recovery + failPending |
| `internal/platform/rpc/errors_helper.go` | +MapCapabilityError (新增) |
| `internal/provider/claudecli/session_log_watcher.go` | +panic recovery |
| `internal/provider/codexapp/peer_supervisor.go` | +panic recovery + wg.Done |
| `internal/provider/codexapp/session_runtime.go` | +recoverWorkerPanic + safe wrappers |
| `internal/provider/codexapp/transport_process.go` | +panic recovery + channel 保护 |

### Commit `863c5ac` (2 files) — RPC 存根清理

| 文件 | 变更类型 |
|------|----------|
| `internal/module/turn/rpc.go` | -review/start 路由注册 |
| `internal/module/turn/rpc_helpers.go` | -reviewStartHandler 函数 |

### Commit `4d866da` (13 files) — P2-G/H 修复

| 文件 | 变更类型 |
|------|----------|
| `internal/platform/rpc/handler.go` | +CapabilityErrorMapper middleware |
| `internal/platform/rpc/factory.go` | 注入 CapabilityErrorMapper 到 baseThreadHandler |
| `internal/platform/rpc/handler_test.go` | +TestThreadHandlerMapsRuntimeCapabilityErrorToGateCode |
| `internal/platform/rpc/server.go` | +serveConn panic recovery |
| `internal/ui/wails/lifecycle.go` | +shutdown/quit panic recovery |
| `internal/ui/wails/http_server.go` | +srv.Serve panic recovery |
| `internal/provider/claudecli/transport.go` | +tr.wait() panic recovery |
| `internal/provider/codexapp/transport_process.go` | +collectStderr/watchProcess panic recovery |
| `internal/module/thread/session_recovery_worker.go` | +Start/drainPending panic recovery |
| `internal/platform/rpc/push_worker.go` | +Start panic recovery |
| `internal/platform/hooks/dispatch_worker.go` | +Start panic recovery |
| `internal/platform/mcpcontrol/config_fanout_worker.go` | +Start panic recovery |

### Commit `5fb2159` (1 file) — MapCapabilityError 消息修复

| 文件 | 变更类型 |
|------|----------|
| `internal/platform/rpc/errors_helper.go` | 使用 err.Error() 保留友好消息 |
