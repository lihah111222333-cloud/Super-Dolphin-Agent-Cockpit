# P8.5 Lifecycle Hooks 实现文档审查报告

> 审查时间：2026-03-23
> 审查方式：Claude Agent 初审 + 主 Agent 复审交叉验证
> 审查对象：p8.5-execution-plan.md、p8-lifecycle-hooks.md、mcp-service-convention.md + 现有代码
> 前序会话 UUID：ea3ad84e-7b52-422d-bc46-cff9da3ea9f9

---

## 总评：⚠️ 有条件通过

文档整体设计严谨，Before/During/After 三阶段模型完备，`ctl/hook/*` 5 方法与 v1 基线 9 方法的分层策略合理。但存在 **3 个严重问题**（文档与代码矛盾、hooks 基础设施代码空白、持久化方案缺失）和 **5 个中等问题**（命名不一致、selector 未实现、重入防护缺失、archtest 空白、reconnect 后订阅恢复），需修复后方可进入编码阶段。

---

## 1. 逐项审查

### 1.1 完整性 ✅

文档覆盖了 hooks 的全部生命周期阶段，无遗漏：

| 生命周期阶段 | Hook 点 | 覆盖 |
|---|---|---|
| 会话/进程启动前 | `agent.session.start` | ✅ |
| Turn 开始前 | `agent.turn.before` | ✅ |
| 工具调用前 | `agent.tool.before` | ✅ |
| 任务启动 | `agent.task.starting` | ✅ |
| 状态跃迁 | `agent.state.change` | ✅ |
| 周期进度 | `agent.turn.progress` | ✅ |
| 工具调用后 | `agent.tool.after` | ✅ |
| Turn 完成 | `agent.turn.after` | ✅ |
| 任务声称完成 | `agent.task.completing` | ✅ |
| Turn 失败 | `agent.turn.failed` | ✅ |
| 进程退出 | `agent.process.exit` | ✅ |

5 个 hook 扩展方法（subscribe / before / check / after / resolve）均有完整请求/响应语义。`escalate → pending_hook_review → resolve` 流程闭环。

**小问题**：
- `agent.task.starting` 与 `agent.turn.before` 区分不够清晰，建议补充使用场景说明
- 缺少 **agent 暖启动/恢复场景** 的 hook 点（进程重连后恢复到中间状态时是否触发 Before？）

### 1.2 一致性 ⚠️

存在 3 处实质性矛盾，均经代码验证确认。

#### 矛盾 ① — LeaseKey 序列化形式（🔴 严重）

p8.5-execution-plan.md §1.6（第 267 行）明确规定：

> "所有 `ctl/register` 之后的方法都把 `LeaseKey` 作为两个顶层 JSON 字段展开：`instance_id` 和 `generation`；不再出现 `lease: { ... }` 嵌套对象。"

**实际代码**（`internal/dto/mcp/protocol.go` 第 65/84/95/107/160 行）：

```go
type HeartbeatRequest struct {
    Lease LeaseKey \`json:"lease"\`  // ← 嵌套对象
}
type ContextRequest struct {
    Lease LeaseKey \`json:"lease"\`  // ← 同上
}
// EventNotify, LogNotify, ApprovalRequest, ReportRequest 均如此
```

**复审确认**：✅ 矛盾确实存在，5 处全部使用嵌套形式。

**建议**：以代码为准（方案 a），修改文档承认嵌套形式 `{"lease":{"instance_id":"...","generation":1}}`。理由：嵌套对象语义更清晰，Go struct tag 自然映射，且已在生产路径中。

#### 矛盾 ② — 文件命名不一致（🟡 中等）

p8.5-execution-plan.md §2（第 286-290 行）和 §6.1（第 601-604 行）定义：

| 文档中的文件名 | 实际代码文件名 | 说明 |
|---|---|---|
| `tool_registry.go` | `registry.go` | 功能一致 |
| `tool_callback.go` | `handlers.go` | 功能一致 |
| `lease.go` | 不存在（LeaseKey 定义散在 registry.go 中） | 未单独拆出 |
| `router.go` | `router.go` ✅ | 一致 |
| `errors.go` | `errors.go` ✅ | 一致 |

**实际文件列表**（`internal/platform/mcpcontrol/`）：
```
errors.go  fanout.go  handlers.go  handlers_test.go  module.go
peers.go  registry.go  registry_support.go  report_handlers.go
resolution.go  router.go  sweeper.go
```

**复审确认**：✅ 命名不一致。实际多了 `fanout.go`、`peers.go`、`registry_support.go`、`report_handlers.go`、`resolution.go`、`sweeper.go` 等文档未提及的文件。

**建议**：以代码为准更新文档，并补充文档中遗漏的文件清单。

#### 矛盾 ③ — Selector 三维交集实现不完整（🟡 中等）

p8.5 §4.2 和 §1.2（第 131 行）规定 `selector = intersection(subscription, capability, scope)`。

**实际代码**：
- `router.go:16` — `NotifyConfigChanged` 只用 `snapshotTargets(r.bySubscription, topic)`
- `registry.go:203` — `NotifyByCapability` 只用 `snapshotTargets(r.byCapability, capability)`

两个维度有独立方法，但**没有三维交集查询方法**。

**复审确认**：✅ 缺少 `intersectTargets(subscription, capability, scope)` 组合方法。

### 1.3 可实现性 ⚠️

协议层设计清晰可编码，但有 4 个实现断层。

#### 断层 ① — `internal/platform/hooks/` 完全不存在（🔴 严重）

p8-lifecycle-hooks.md §3.3（第 572 行）定义 6 个文件（registry.go, dispatcher.go, merge.go, points.go, manager.go, resolver.go），`internal/contract/hooks.go` 也不存在。

**复审确认**：✅ 代码空白。`lsp_grep` 搜索 `platform/hooks` 只在文档中有引用，无任何 `.go` 文件。

#### 断层 ② — Hooks 代码放置存在双重定义

两份文档对 hooks 代码放置有微妙冲突：
- **p8-lifecycle-hooks.md §3.3**：hooks 核心逻辑全部在 `internal/platform/hooks/`（6 文件）
- **p8.5-execution-plan.md §6.1**：`ctl/hook/subscribe` 和 `ctl/hook/resolve` handler 在 `mcpcontrol/tool_callback.go`，`ctl/hook/before|check|after` fanout 在 `mcpcontrol/router.go`

**问题**：merge 逻辑、pending review 管理、TTL 管理、决策收敛——核心 hooks 逻辑在 `hooks/` 还是 `mcpcontrol/`？

**建议明确**：
```
mcpcontrol/handlers.go    → 协议接线（subscribe/resolve handler）
mcpcontrol/router.go      → hook callback fanout（before/check/after 发送）
platform/hooks/           → 核心逻辑（merge、pending review、TTL、决策收敛）
contract/hooks.go         → 接口定义（HookManager）
```
`mcpcontrol` → `contract/hooks.go` ← `platform/hooks/`，无循环依赖。

#### 断层 ③ — `pending_hook_review` 持久化方案缺失（🔴 严重）

`escalate` 是核心语义，但 `pending_hook_review` 存内存还是 DB 未定义。无 SQL schema、无 store 接口。

**风险**：进程重启后 pending review 丢失 → 任务永久挂起。

**建议**：补充 `hook_pending_reviews` DB 表 schema + `HookReviewStore` 接口 + 启动恢复流程。

#### 断层 ④ — `dispatch_intent` 缺少持久化定义

p8.5 §1.5（第 244-254 行）定义了 `dispatch_intent` 最小字段但无 SQL schema、无 `DispatchStore` 接口。

### 1.4 安全性 ✅

权限控制、超时和错误处理设计充分。

| 维度 | 状态 |
|---|---|
| `AllowedTools` 白名单交集合并 | ✅ |
| `DeniedTools` 黑名单并集合并 | ✅ |
| Turn 级白名单不能放宽 Session 级限制 | ✅ |
| `session_token` 校验 | ✅ |
| Before/After 默认 fail-closed | ✅ |
| During 默认 fail-open（`continue`） | ✅ |
| 所有 callback 带 `deadline_ms` | ✅ |
| `escalate` TTL 到期默认 `reject` | ✅ |
| `ctl/context` scope 白名单固定 4 个 | ✅ |
| `dispatch_intent` 幂等键去重 | ✅ |
| MCP 不能主动 `SubmitTurn` | ✅ |
| `agent.process.exit` 只能由核心层生成 | ✅ |

**小建议**：
- 补充每个 hook topic 的最大订阅方数量限制（防 fanout 爆炸）
- 补充 hook response payload 的最大大小限制

### 1.5 可测试性 ✅

| 设计点 | 测试友好度 |
|---|---|
| `Peer` 接口（Notify/Callback/Close） | ✅ 可直接 mock |
| `HandlerDeps` 全部 `optional:"true"` | ✅ fx 注入可选替换 |
| `ContextProvider` / `EventSink` / `LogSink` 接口 | ✅ 已分离 |
| `ToolRegistry` 实现 `contract.ToolRegistry` 接口 | ✅ 可 mock |
| Registry 纯内存实现 | ✅ 无外部 IO |
| Sweeper 独立 goroutine + context cancel | ✅ lifecycle 可控 |

**待补充**：
- hooks 扩展的测试接口定义（`HookSubscriber` / `HookDispatcher` / `HookResolver`）
- 标准集成测试 fixture（多订阅方 + 冲突合并 + escalate/resolve 完整流程）

### 1.6 与现有架构的兼容性 ⚠️

整体兼容性好，一个潜在风险。

| 基础设施 | 集成方式 | 风险 |
|---|---|---|
| `jrpc2` TCP server | 复用 `rpc.Server`，`handler.Map` 注入 | 低 |
| `event.Dispatcher` | `handlers.go` 已用 `event.Publish` | 低 |
| `fx.Lifecycle` | Sweeper 已正确注册 start/stop | 低 |
| `ApprovalManager` | 通过 `contract.ApprovalBroker` 隔离 | 低 |
| bootstrap client | 独立包，只依赖 `jrpc2+dto+stdlib` | 低 |

**潜在风险**：hooks 包与 mcpcontrol 包的依赖方向未在 archtest 中约束。如果 hooks 包需要 lease 校验或 peer fanout 能力，可能反向依赖 mcpcontrol 形成循环。

### 1.7 边界情况 ⚠️

主要场景覆盖，3 个重要场景缺失。

**已覆盖**：

| 场景 | 策略 |
|---|---|
| 多订阅方冲突决策 | 固定优先级合并（deny > wait > modify > allow） |
| Hook 超时 | Before/After fail-closed, During fail-open |
| 订阅方断连 | `subscriber_lost` + TTL 到期默认 `reject` |
| 重连后 pending hook 恢复 | 按订阅关系重新 dispatch |
| 同一 `hook_call_id` 冲突 resolve | `ERR_HOOK_RESOLVE_CONFLICT` |
| Lease stale 时的 hook 请求 | `ERR_LEASE_STALE` |

**未覆盖**：

#### ❌ 7a — Hook 重入/递归

场景：`agent.tool.before` 消费方自己又调用 tool → 触发另一个 `agent.tool.before`。
风险：无限递归或死锁。
建议：定义 `max_hook_depth=3`，超过直接 `deny`。

#### ❌ 7b — Fanout 部分失败

场景：3 个订阅方中 2 个返回 `allow`，第 1 个超时。
建议：
- Before/After：超时方视为 `deny`/`reject`（fail-closed），参与正常合并
- During：超时方视为 `continue`（fail-open），参与正常合并
- 标注 `partial_failure: true` + 失败方 lease 列表
- 连续 3 次 callback 失败 → 自动取消订阅 + `subscriber_lost`

#### ❌ 7c — `ctl/shutdown` 与 pending hook 竞态

场景：`ctl/hook/after` 等待外部决策时核心发起 `ctl/shutdown`。
建议：
1. 标记所有关联 pending review 为 `cancelled`
2. Before/After 按 fail-closed 落终态
3. During 直接丢弃
4. 不再等待 `ctl/hook/resolve`
5. completion report 标记 `shutdown_interrupted_hooks: true`

---

## 2. 复审补充发现

以下是主 Agent 复审时发现的 Claude 初审未覆盖的问题。

### 2.1 Reconnect 后 hook 订阅恢复策略未定义（🟡 中等）

p8.5 §3 定义了 reconnect 后重发 `ctl/register` + `resume_from_generation`，但未提及 `ctl/hook/subscribe` 的恢复：

- reconnect 成功后，旧 `LeaseKey` 失效，关联的 hook subscription 是否自动迁移到新 lease？
- 还是要求工具侧重新发送 `ctl/hook/subscribe`？

**建议**：明确为 "reconnect 后工具侧必须重新 subscribe"，bootstrap client 的 reconnect 流程自动重发上次的 subscription 集合。

### 2.2 `constants.go` 缺少 hook 方法常量（🟢 轻微）

当前 `internal/dto/mcp/constants.go` 只有 v1 基线 9 个方法的常量，无 `ctl/hook/*` 常量：

```go
// 现有（9 个）
MethodRegister, MethodHeartbeat, MethodContext, MethodEvent,
MethodLog, MethodApproval, MethodReport, MethodShutdown, MethodConfigChanged

// 缺失（5 个）
MethodHookSubscribe = "ctl/hook/subscribe"
MethodHookBefore    = "ctl/hook/before"
MethodHookCheck     = "ctl/hook/check"
MethodHookAfter     = "ctl/hook/after"
MethodHookResolve   = "ctl/hook/resolve"
```

### 2.3 `LeaseID` 残留字段未标记 Deprecated（🟢 轻微）

以下位置仍保留已废弃的 `LeaseID` 字段：
- `internal/contract/mcp_control.go:13`
- `internal/dto/mcp/protocol.go:31,52,110,163`
- `internal/platform/mcpcontrol/registry.go:24,107`
- `internal/platform/mcpcontrol/registry_support.go:198`

文档中已用 `LeaseKey` 替代 `LeaseID`，但代码未标注 `// Deprecated`。

### 2.4 p8-lifecycle-hooks.md §6.4 DAG SQL 重写范围偏大

§6.4 列出 10 个需要重写的 SQL 接口，涉及 `taskdag`、`wakeup` 核心路径。这不是 hooks 实现的前置依赖，但混在同一轮会膨胀范围。

**建议**：hooks 核心实现为 Phase 1 独立交付；DAG SQL 适配为 Phase 2。

---

## 3. 关键问题清单（按严重度排序）

| # | 严重度 | 问题 | 建议修复 | 复审确认 |
|---|---|---|---|---|
| 1 | 🔴 严重 | **LeaseKey 序列化矛盾**：文档说顶层展开，代码用 `json:"lease"` 嵌套 | 以代码为准修改文档 | ✅ 5 处验证 |
| 2 | 🔴 严重 | **`internal/platform/hooks/` 不存在**，`contract/hooks.go` 也不存在 | 编码前先创建接口定义 | ✅ grep 确认 |
| 3 | 🔴 严重 | **`pending_hook_review` 持久化方案缺失** | 补 DB 表 + store 接口 + 恢复流程 | ✅ 无 SQL/接口 |
| 4 | 🟡 中等 | **Selector 三维交集未实现** | 补充 `intersectTargets` 方法 | ✅ 只有单维度 |
| 5 | 🟡 中等 | **文件命名不一致** | 以代码为准更新文档 | ✅ 12 vs 5 文件 |
| 6 | 🟡 中等 | **Hook 重入防护缺失** | 补充 `max_hook_depth` | ✅ 两份文档均无 |
| 7 | 🟡 中等 | **hooks/mcpcontrol 依赖方向无 archtest** | 补 archtest 约束 | ✅ |
| 8 | 🟡 中等 | **Reconnect 后 hook subscription 恢复策略未定义** | 明确 "必须重新 subscribe" | ✅ 复审新增 |
| 9 | 🟢 轻微 | **`dispatch_intent` 无 SQL schema** | 补表定义和接口 | ✅ |
| 10 | 🟢 轻微 | **`constants.go` 缺 hook 常量** | 补 5 个 `MethodHook*` | ✅ 代码验证 |
| 11 | 🟢 轻微 | **`LeaseID` 残留未标 Deprecated** | 标 `// Deprecated` | ✅ 9 处 |
| 12 | 🟢 轻微 | **`task.starting` 与 `turn.before` 区分不明** | 补使用场景说明 | ✅ |

---

## 4. 实现建议

### 4.1 hooks 与 mcpcontrol 的依赖关系图

```
┌──────────────────────────┐       ┌────────────────────────┐
│  platform/mcpcontrol     │       │  platform/hooks        │
│                          │       │                        │
│  handlers.go             │──┐    │  registry.go           │
│   ctl/hook/subscribe     │  │    │  dispatcher.go         │
│   ctl/hook/resolve       │  │    │  merge.go              │
│                          │  ├───▶│  resolver.go           │
│  router.go               │  │    │  manager.go            │
│   hook before/check/after│──┘    │  points.go             │
│   fanout                 │       │                        │
└──────────────────────────┘       └────────────────────────┘
            │                                  │
            ▼                                  ▼
      ┌──────────────────────────────────────────┐
      │         contract/hooks.go                │
      │   HookManager interface                  │
      │   HookDispatcher interface               │
      │   HookResolver interface                 │
      └──────────────────────────────────────────┘
```

### 4.2 `contract/hooks.go` 最小接口草案

```go
type HookManager interface {
    Subscribe(ctx context.Context, lease mcp.LeaseKey, req HookSubscribeRequest) (HookSubscribeResponse, error)
    DispatchBefore(ctx context.Context, topic string, payload HookPayload) (BeforeDecision, error)
    DispatchCheck(ctx context.Context, topic string, payload HookPayload) (CheckDecision, error)
    DispatchAfter(ctx context.Context, topic string, payload HookPayload) (AfterDecision, error)
    Resolve(ctx context.Context, req HookResolveRequest) error
    GetPendingReviews(ctx context.Context, agentID string) ([]PendingHookReview, error)
}
```

### 4.3 补充 `constants.go` hook 常量

```go
const (
    MethodHookSubscribe = "ctl/hook/subscribe"
    MethodHookBefore    = "ctl/hook/before"
    MethodHookCheck     = "ctl/hook/check"
    MethodHookAfter     = "ctl/hook/after"
    MethodHookResolve   = "ctl/hook/resolve"

    HookDecisionAllow    = "allow"
    HookDecisionDeny     = "deny"
    HookDecisionWait     = "wait"
    HookDecisionModify   = "modify"
    HookDecisionContinue = "continue"
    HookDecisionWarn     = "warn"
    HookDecisionAbort    = "abort"
    HookDecisionApprove  = "approve"
    HookDecisionReject   = "reject"
    HookDecisionEscalate = "escalate"
)
```

### 4.4 补充 `ctl/shutdown` 与 pending hook 竞态规则

建议在 p8-lifecycle-hooks.md §7 新增：

> **§7.7 `ctl/shutdown` 与 pending hook review 竞态**
>
> 若 `ctl/shutdown` 到达时存在未完成的 `pending_hook_review`：
> 1. 核心层标记所有关联 pending review 为 `cancelled`
> 2. Before/After pending 按 fail-closed 落终态（`deny` / `reject`）
> 3. During pending 直接丢弃
> 4. 不再等待 `ctl/hook/resolve`
> 5. 最终 completion report 标记 `shutdown_interrupted_hooks: true`

### 4.5 补充 fanout 部分失败处理规则

建议在 p8-lifecycle-hooks.md 多订阅方合并规则后新增：

> **部分失败处理**：
>
> 若 N 个订阅方中有 K 个超时或错误：
> - Before/After：超时方视为 `deny`/`reject`（fail-closed），参与正常合并
> - During：超时方视为 `continue`（fail-open），参与正常合并
> - 标注 `partial_failure: true` + 失败方 lease 列表
> - 连续 3 次 callback 失败 → 自动取消订阅 + `subscriber_lost`

### 4.6 实现分期建议

| Phase | 范围 | 估计 |
|---|---|---|
| Phase 1 核心 | `contract/hooks.go` + `platform/hooks/` 6 文件 + `dto/mcp/hook.go` + `constants.go` 补充 + handler 接线 + archtest | ~1.5k LOC |
| Phase 2 集成 | selector 三维交集 + pending review 持久化 + bootstrap hooks.go 消费 | ~1k LOC |
| Phase 3 DAG 适配 | §6.4 列出的 10 个 SQL 接口重写 | ~0.8k LOC |
| Phase 4 测试 | 单测 + 集成测试 fixture + archtest 回归 | ~1.2k LOC |

---

## 5. 进入编码的前置条件

| # | 条件 | 状态 |
|---|---|---|
| 1 | 解决 LeaseKey 序列化矛盾（以代码为准修改文档） | ⏳ |
| 2 | 创建 `internal/contract/hooks.go` 接口定义 | ⏳ |
| 3 | 定义 `pending_hook_review` 持久化策略（DB schema） | ⏳ |
| 4 | 明确 hooks/mcpcontrol 代码放置和依赖方向 | ⏳ |
| 5 | 补充 constants.go hook 常量 | ⏳ |

满足以上 5 项后方可进入编码阶段。

---

## 6. 审查结论

| 维度 | 评分 | 要点 |
|---|---|---|
| 1. 完整性 | ✅ | 11 个 hook 点 + 5 个方法全覆盖，三阶段闭环 |
| 2. 一致性 | ⚠️ | LeaseKey 序列化矛盾（严重）、文件命名/selector 不一致 |
| 3. 可实现性 | ⚠️ | 协议层清晰，但 hooks 基础设施空白、持久化方案断层、代码放置双重定义 |
| 4. 安全性 | ✅ | AllowedTools/DeniedTools、超时、fail-closed 策略充分 |
| 5. 可测试性 | ✅ | 接口抽象和 fx 注入设计测试友好 |
| 6. 架构兼容性 | ⚠️ | hooks 与 mcpcontrol 依赖方向需 archtest 约束 |
| 7. 边界情况 | ⚠️ | 缺 hook 重入防护、fanout 部分失败、shutdown 竞态 |
