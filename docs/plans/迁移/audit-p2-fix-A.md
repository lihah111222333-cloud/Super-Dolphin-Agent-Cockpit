# P2 修复审查 — Agent A
## B6/B7/B8/B12/I7 逐项验证+行号
- `B6 / Blocker` `internal/module/workspace/service.go:211-217` 先从 `store.ListFiles` 取 tracked rows，再调用 `planMerge` / `applyFileUpdates`。`internal/module/workspace/service_helpers.go:73-98` 只遍历传入 `files` 切片，不扫描 `WorkspacePath`；实现只覆盖已记录文件，不覆盖 workspace 中新增但未入库的文件。
- `B6 / Blocker` `internal/module/workspace/service_helpers.go:121-131` 在可 merge 分支只把 `updated.State` 置为 `fileStateMerged` 并把 `SourceSHA256After` 置为 `workspaceHash`；`internal/module/workspace/service_helpers.go:128` 明确保留了 “copy workspace content into sourceRoot” 的 TODO，当前没有真实 workspace→source copy。V2 在 `go-agent-v2/internal/service/workspace.go:537-557` 调用 `copyFileAtomic(...)` 后再回填 `SourceSHA256After`，语义不等价。
- `B6 / OK` Merge result 已返回 `merged/conflicts/unchanged/errors` 和 `files`：`internal/module/workspace/contract.go:50-60` 定义 `MergeRunResult`，`internal/module/workspace/service_helpers.go:73-98` 汇总计数，`internal/module/workspace/service.go:238-240` 返回结果。
- `B6 / OK` 文件状态确实逐条 `UpsertFile`：`internal/module/workspace/service_helpers.go:141-147` 对 merge 更新循环调用 `s.store.UpsertFile`；store 落库实现位于 `internal/store/workspace/store.go:73-89`。
- `B6 / OK` active→merged 状态迁移失败会回滚文件状态：`internal/module/workspace/service.go:223-236` 在 `transitionRunStatus` 失败时调用 `rollbackMergeState`；`internal/module/workspace/service_helpers.go:150-164` 通过 `restoreRunFiles` 将原始 `RunFile` 逐条 `UpsertFile` 回去。
- `B6 / Blocker` V2 最终状态语义不等价。当前实现在冲突或错误时直接保持 `statusActive` 返回：`internal/module/workspace/service.go:219-221`。V2 在 merge 结束后把 `Errors > 0 || Conflicts > 0` 归为 `WorkspaceRunStatusFailed`：`go-agent-v2/internal/service/workspace.go:329-345`。此外 V2 请求与流程还支持 `DryRun`、`DeleteRemoved` 和 workspace walk：`go-agent-v2/internal/service/workspace.go:66-71`, `go-agent-v2/internal/service/workspace.go:348-369`, `go-agent-v2/internal/service/workspace.go:560-575`；当前 `internal/module/workspace/contract.go:39-42` 不具备这些语义。
- `B7 / OK` RPC 参数已补 `updatedBy` / `reason`：`internal/module/workspace/rpc_types.go:10-14`。
- `B7 / OK` `Service.AbortRun` 签名已扩展为 `(ctx, runKey, updatedBy, reason string)`：`internal/module/workspace/contract.go:11-19`，其中方法签名位于 `internal/module/workspace/contract.go:17`。
- `B7 / OK` `AbortRun` 已把 `updatedBy` 和 `reason` 传入 `TransitionRunStatusInput`：`internal/module/workspace/service.go:243-250`；store 合同 `TransitionRunStatusInput` 包含 `UpdatedBy` / `Metadata`：`internal/store/workspace/contract.go:33-39`。
- `B8 / OK` 会创建独立目录，不会直接退回 `sourceRoot`：`internal/module/workspace/service.go:129-147` 生成 `workspacePath` 并在 `internal/module/workspace/service.go:145-147` 显式拒绝 `workspacePath == sourceRoot`；`internal/module/workspace/service.go:58-60` 调用 `os.MkdirAll(run.WorkspacePath, 0o755)` 创建目录。
- `B8 / Warning` 默认目录并非始终 `CWD/.workspace/runKey`。`internal/module/workspace/service.go:135-139` 先取 `req.CWD`，若为空则把 `base` 回退到 `sourceRoot`，最终路径为 `filepath.Join(base, ".workspace", runKey)`。因此实际默认值是 `CWD/.workspace/runKey` 或 `sourceRoot/.workspace/runKey`。
- `B8 / OK` bootstrap copy 与文件记录都存在：`internal/module/workspace/service.go:61-70` 先 `bootstrapFiles` 再 `upsertRunFiles`；`internal/module/workspace/service_helpers.go:20-30` 把源文件复制到 workspace；`internal/module/workspace/service_helpers.go:166-177` 逐条 `UpsertFile`；`internal/module/workspace/service_helpers.go:179-201` 为 run file 回填 baseline/workspace hash 与 state。
- `B12 / OK` service 已注入 workspace bus emitter：`internal/module/workspace/module.go:9-13` 通过 Fx 注入 `*bus.WorkspaceEmitters`；`internal/module/workspace/service.go:34-50` 在 service 中保存 typed emitter。
- `B12 / OK` typed event 定义存在于 `internal/dto/workspace/event.go:6-40`，包括 `WorkspaceRunCreated`、`WorkspaceRunStatusChanged`、`WorkspaceRunMerged`、`WorkspaceRunAborted`。
- `B12 / OK` `CreateRun` 成功后发布 `WorkspaceRunCreated`：`internal/module/workspace/service.go:65-73`，事件封装位于 `internal/module/workspace/service_helpers.go:204-210`。
- `B12 / OK` `UpdateRunStatus` 成功后发布 `WorkspaceRunStatusChanged`：`internal/module/workspace/service.go:190-203`，事件封装位于 `internal/module/workspace/service_helpers.go:213-220`。
- `B12 / OK` `AbortRun` 成功后发布 `WorkspaceRunAborted`：`internal/module/workspace/service.go:243-255`，事件封装位于 `internal/module/workspace/service_helpers.go:232-238`。
- `B12 / Warning` `MergeRun` 只在成功转为 `merged` 后发布 `WorkspaceRunMerged`：`internal/module/workspace/service.go:223-240`，事件封装位于 `internal/module/workspace/service_helpers.go:222-229`。`internal/module/workspace/service.go:219-221` 的 conflict/error 提前返回路径不会发布任何 workspace event。
- `I7 / OK` RPC 参数已补 `state`：`internal/module/workspace/rpc_types.go:27-30`。
- `I7 / OK` service 已把 `state` 透传到 `ListFilesFilter.State`：`internal/module/workspace/service.go:258-263`；store 合同包含 `State` 字段：`internal/store/workspace/contract.go:41-45`；store 查询实现继续透传：`internal/store/workspace/store.go:100-102`。

## 代码守卫
- `OK` 文件行数守卫阈值是 `internal/archtest/guardlib.go:17-24` 的 `MaxFileLines = 400`。`internal/module/workspace/service.go` 末行是 `internal/module/workspace/service.go:323`；`internal/module/workspace/service_helpers.go` 末行是 `internal/module/workspace/service_helpers.go:284`，均未超阈值。
- `OK` 函数长度守卫阈值是 `internal/archtest/guardlib.go:19` 的 `MaxFuncLines = 80`。当前较长函数例如 `internal/module/workspace/service.go:206-241`、`internal/module/workspace/service.go:76-109`、`internal/module/workspace/service_helpers.go:100-132`，都显著低于 80 行。
- `OK` `go build ./...` 成功。
- `OK` `go vet ./...` 成功。
- `OK` `go test ./internal/archtest/... -count=1` 成功，输出为 `ok github.com/anthropic-ai/super-agent-v3/internal/archtest 1.535s`；对应代码守卫测试入口见 `internal/archtest/code_size_guard_test.go:10-24`。

## 结论（Blocker / Warning / OK）
- `Blocker` B6 未达到“真实 merge”与 V2 等价：当前 merge 不执行 workspace→source copy，也不覆盖 V2 的 walk / dryRun / deleteRemoved / final-status 语义。证据：`internal/module/workspace/service_helpers.go:121-131`, `internal/module/workspace/service_helpers.go:128`, `internal/module/workspace/service.go:219-236`, `go-agent-v2/internal/service/workspace.go:329-369`, `go-agent-v2/internal/service/workspace.go:537-557`。
- `Warning` B8 默认目录策略是 `filepath.Join(base, ".workspace", runKey)`，其中 `base` 可能是 `sourceRoot`，不是无条件 `CWD`。证据：`internal/module/workspace/service.go:135-139`。
- `Warning` B12 的 merge 事件只覆盖成功 merged 路径，conflict/error 返回不发事件。证据：`internal/module/workspace/service.go:219-221`, `internal/module/workspace/service.go:223-240`。
- `OK` B7、I7 和代码守卫满足要求。证据：`internal/module/workspace/rpc_types.go:10-14`, `internal/module/workspace/rpc_types.go:27-30`, `internal/module/workspace/service.go:243-250`, `internal/module/workspace/service.go:258-263`, `internal/archtest/guardlib.go:17-24`。

## 互辩
### 对 audit-p2-fix-B 的批判
1. `audit-p2-fix-B.md:12-16` 把 B14 基本判成 `OK`，但遗漏了更硬的 DAG wire 契约缺口。当前 `createDAGParams` / `dagKeyParams` / `dagNodeParams` / `updateNodeParams` 都是 camelCase，且没有像 B13 那样补 `UnmarshalJSON` 的 snake_case 兼容：`internal/sidecar/orch/orchestration/rpc_types.go:42-68,196-200`。返回 DTO 也是 camelCase：`internal/sidecar/orch/orchestration/contract.go:131-168`。V2/现有 tool 规范仍是 snake_case：`go-agent-v2/pkg/toolsdk/tools/providers.go:134-166`。这不是“都有 JSON tag 就 OK”，而是协议名本身不兼容。
2. `audit-p2-fix-B.md:12-16` 还遗漏了 `task_create_dag` 最关键的 `schedule` / `execution` 语义根本没接进来。当前 `CreateDAGRequest` 和 `CreateDAGNodeRequest` 只有 `DagKey/Title/.../Metadata/Nodes` 与 `NodeKey/.../Config`，没有 `schedule`，也没有节点级 `execution`：`internal/sidecar/orch/orchestration/contract.go:99-116`；RPC 参数层同样缺失：`internal/sidecar/orch/orchestration/rpc_types.go:51-68`。而 V2 工具规范把 `schedule` 设为必填，节点允许 `execution` 覆盖：`go-agent-v2/pkg/toolsdk/tools/resource_specs.go:18-21,31-40,49-50`；真实处理也在缺 `schedule` 时直接报错：`go-agent-v2/pkg/toolsdk/tools/resource_dag.go:24-31,59-69`。B 报告把 B14 判成 `OK`，漏掉了比“真实实现存在”更严重的协议缺口。
3. `audit-p2-fix-B.md:20-22` 仅把 `orchestration/report` 仍是写接口记为 `Warning`，这个分级过轻。批次计划明确要求 “`orchestration/report` 改为真实读 lastReport”：`docs/plans/迁移/p2-execution-plan.md:36-38`。但当前 handler 仍然调用 `svc.SetReport` 并返回 `{success:true}`：`internal/sidecar/orch/orchestration/rpc.go:73-77`；底层实现也只是覆写 `lastReport`：`internal/sidecar/orch/orchestration/report.go:32-41`。这不是附加字段或弱对齐问题，而是方法语义仍与目标相反，应至少升级为 `Blocker`。
4. `audit-p2-fix-B.md:23-24` 抓到了 “没有真正通知投递链”，但仍遗漏了更前置的逻辑 bug：当前 `HandleReportEvent` 只要拿到非空 `report` 就 drain requester，不必等 terminal event，见 `internal/sidecar/orch/orchestration/report.go:96-111`。这意味着一次中途进度汇报就能清空 `reportRequesters`，最终完成态再也没人可通知。V2 的 drain 发生在 `maybeAutoReportOrchestrationCompletion` 识别到 terminal completion 之后：`go-agent-v2/internal/apiserver/orchestration_report.go:99-110,129-136`。B 报告没有指出这一点，导致对 B15 风险低估。

### 对 audit-p2-fix-C 的批判
1. `audit-p2-fix-C.md:16` 断言 “workspace 新增事件与 bus sink 对齐……链路是闭合的”，这与 workspace 实现不符，也和我的 workspace 审查结论冲突。`MergeRun` 在 conflict/error 路径会直接 `return result`，不会发任何 workspace event：`internal/module/workspace/service.go:219-221`；只有成功转为 `merged` 才调用 `emitRunMergedEvent`：`internal/module/workspace/service.go:223-240`。因此 event 链并不覆盖全部关键状态。
2. `audit-p2-fix-C.md:16` 把 “sink 订阅了 4 个事件” 当成闭环证据也过度乐观。LSP references 显示这些 workspace 事件在运行时代码里的引用基本只有声明、service emitter、helper 构造和 log sink：`internal/dto/workspace/event.go:6-40` 的引用分别落在 `internal/module/workspace/service.go:36-49`, `internal/module/workspace/service_helpers.go:204-238`, `internal/platform/bus/sink.go:75-79`。没有额外的 UI/projector/状态消费者。也就是说，当前链路更多是“发出后被日志 sink 记录”，不应表述为功能闭合。
3. `audit-p2-fix-C.md:17` 说 “orchestration dag.go/report.go 与 fx 注册完整……无悬空方法”，但这掩盖了更严重的 B14/B15 语义缺口。`orchestration/report` 仍然是写接口：`internal/sidecar/orch/orchestration/rpc.go:73-77`, `internal/sidecar/orch/orchestration/report.go:32-41`，而批次目标要求它改成读 `lastReport`：`docs/plans/迁移/p2-execution-plan.md:36-38`。同时 DAG create 仍缺 `schedule` 和节点 `execution`：`internal/sidecar/orch/orchestration/contract.go:99-116`, `internal/sidecar/orch/orchestration/rpc_types.go:51-68`，与工具规范 `go-agent-v2/pkg/toolsdk/tools/resource_specs.go:18-21,31-40,49-50` 不符。C 报告把这块当成跨模块 `OK`，判断过宽。
4. `audit-p2-fix-C.md:15` 用 handler key 数量做跨模块一致性证据，论据偏弱，还掩盖了功能重复。它把 `skills/remote/list` 和 `skills/remote/read` 都算作独立 surface，但这两个路由都直接调用同一个 `svc.ReadRemote`：`internal/module/skill/rpc.go:68-72`；service contract 里也根本没有 `ListRemote`：`internal/module/skill/contract.go:20-21`。因此 “22 个 key” 并不能证明 skill 面真正增加了 22 个独立能力，C 报告在这里高估了覆盖度。
