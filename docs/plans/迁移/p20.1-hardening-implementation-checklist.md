# P20.1 加固实施清单（Implementation Checklist）

> [P25 Phase 4 close amendment · 2026-04-29]
> 本清单中各 “`ENABLE_SKILL_PROGRESSIVE_DISCLOSURE` 总开关默认 false” 勾选项反映 P20.1 验收阶段补付状态。P25 Phase 4 close 已翻默认为 `true`，并删除 default-switch-guard / pr6-verify 等 hardening 资产。备查者请以 `docs/plans/迁移/p25skill优化/p25skill优化.md` 顶部 Phase 4 close patch 为准。
>
> 创建时间：2026-04-19 | 状态：✅ 可执行清单（hardening 阶段已完，P25 Phase 4 close 后默认已为 `true`）
> 对应设计基线：`docs/plans/迁移/p20.1-skill-progressive-disclosure-hardening.md`
> 关联目录：`docs/plans/迁移/p20/`
> 用途：给施工 / 联调 / 验收同学一份**可直接勾选**的执行清单；如与 P20.1 正文冲突，以正文为准

---

## 0. 使用方式

- 开工前先通读：
  1. `docs/plans/迁移/p20.1-skill-progressive-disclosure-hardening.md`
  2. `docs/plans/迁移/p20/README.md`
  3. `docs/plans/迁移/p20/status-checkpoint-2026-04-19.md`
- 每完成一个子项，直接勾选并补充 PR / commit / 测试编号（若暂不提交，可填本地验证说明）
- **禁止跳过 Gate 0 / Gate 1** 直接做 provider 写端
- 若发现实现与 P20.1 正文不一致，先修文档再施工，禁止“代码先跑偏”

---

## 1. Gate 0：开工前核对（全部必过）

- [ ] 已确认 authoritative 设计基线为 `p20.1-skill-progressive-disclosure-hardening.md`
- [ ] 已确认 `docs/plans/迁移/p20/p20-original-plan.md` 仅作历史留档，不再作为最新施工口径
- [ ] 已确认当前仓库状态：
  - [ ] `SkillRef` DTO 已部分落地
  - [ ] `trust.go` / `approval.go` / `rollout_markers.go` 已有骨架
  - [ ] `skill_catalog_provider.go` / `rpc_skill.go` / `expanded_state.go` 等仍未完整落地
- [ ] 已确认本轮**不做 git 提交 / 推送**（如需后续提交，另开任务）
- [ ] 已确认需要同步更新的文档入口：
  - [ ] `docs/plans/迁移/p20/README.md`
  - [ ] `docs/plans/迁移/p20/p20-original-plan.md`
  - [ ] `docs/doc/codemap/*`（收尾时）

---

## 2. Gate 1：红线项（必须先完成，未过禁止继续）

### 2.1 untrusted 元数据净化
- [ ] `project + unapproved` skill 不再把作者原始 `description/summary` 注入 system prompt
- [ ] body 自动生成 summary 不再直接进入 L1 manifest
- [ ] L1 对 untrusted skill 只显示固定模板 + trust 标记
- [ ] 已补测试：恶意 summary 不进 manifest

### 2.2 rollout trim 正确性
- [ ] 新格式 block 改成 header/footer 成对裁剪
- [ ] footer 缺失时只走 fallback，不再作为正常路径
- [ ] block 后正常文本可保留
- [ ] 已补 golden cases：single / multi / mixed / broken footer

### 2.3 审批粒度升级
- [ ] 审批 key 已从 `(name, hash)` 升级为 artifact 级
- [ ] body 审批与 resource 审批分离
- [ ] resource hash 变化不会误复用 body 批准
- [ ] 审批 UI / 事件能区分 body 与 resource

### 2.4 invalid mode 保守降级
- [ ] `SkillMode` 仅 `""` 视为 legacy Full
- [ ] 非法非空 mode 一律按 `None`
- [ ] 已补 metric + warn log

---

## 3. 工作流 A：安全 / Trust / Approval

### 3.1 数据结构
- [ ] `ApprovalEntry` 新增：
  - [ ] `RepoFingerprint`
  - [ ] `ArtifactKind`
  - [ ] `ArtifactLocator`
  - [ ] `ContentHash`
- [ ] `ApprovalCache.Lookup/Approve/Revoke` 已切到 artifact 语义
- [ ] 旧 `(name,hash)` 缓存仅作兼容读取，不再作主 key

### 3.2 Trust 与定位
- [ ] `validateSkillName()` 继续作为 RPC 入口强校验
- [ ] 已新增 `RepoFingerprint()` 辅助
- [ ] 已新增 `NormalizeArtifactLocator()` 辅助
- [ ] `skill_read_resource` 对 path 做 clean + prefix 校验

### 3.3 事件与观测
- [ ] 审批事件面支持：
  - [ ] `approve_metadata`（若实现）
  - [ ] `approve_body`
  - [ ] `approve_resource`
- [ ] 增加指标：`skill_artifact_approval_miss_total`
- [ ] 增加日志字段：`artifact_kind` / `artifact_locator` / `repo_fingerprint`

### 3.4 验收
- [ ] `skill_read_resource("foo", "../x")` 100% 拒绝
- [ ] 同名 skill 跨 repo 不共享审批
- [ ] body 已批准但 resource 未批准时，resource 仍需单独弹窗

---

## 4. 工作流 B：DTO / Provider Mode 语义

### 4.1 DTO 修订
- [ ] `SkillMode.Effective()` 已切换为“空值 Full / 非法值 None”
- [ ] `SkillMode.Valid()` 仍只接受 `full|summary|none|""`
- [ ] 保持 `Prompt` 字段名不变，不做破坏性 rename

### 4.2 测试
- [ ] `TestSkillRef_LegacyUnmarshalStillWorks`
- [ ] `TestSkillMode_InvalidFallsBackToNone`
- [ ] `TestSkillRef_OldServerReadsNewPayload`
- [ ] provider 侧新增 raw invalid mode 测试

### 4.3 Provider 写端
- [ ] codexapp 在写端使用 `Effective()` 后再决定三分支
- [ ] claudecli 在写端使用 `Effective()` 后再决定三分支
- [ ] 非法 mode 不输出 full block
- [ ] summary block 文案已改成 `skill_expand_body("name")`

---

## 5. 工作流 C：rollout markers / trim

### 5.1 共享解析器
- [ ] `rollout_markers.go` 支持解析新格式 header/footer
- [ ] 成对裁剪逻辑对多 block 可用
- [ ] broken footer 时记录 `skill_trim_corruption_fallback_count`
- [ ] legacy 识别逻辑保留，仅做旧数据兼容

### 5.2 Provider 复用
- [ ] `internal/provider/codexapp/history_rollout.go` 已改用新 shared trim
- [ ] `internal/provider/claudecli/history_trim.go` 已改用新 shared trim
- [ ] 两家 provider 对同一输入得到等价裁剪结果

### 5.3 Golden cases
- [ ] new full
- [ ] new summary
- [ ] new expanded
- [ ] multiple blocks
- [ ] block + normal text after block
- [ ] broken footer fallback
- [ ] legacy + new mixed

---

## 6. 工作流 D：工具拆分（P20.1 最大变更）

### 6.1 RPC / Contract
- [ ] `skill_expand` 不再作为主路径
- [ ] 新增 contract：
  - [ ] `ExpandBody(...)`
  - [ ] `ReadResource(...)`
- [ ] 新增 RPC / tool types：
  - [ ] `skill_expand_body`
  - [ ] `skill_read_resource`
  - [ ] `skill_list`

### 6.2 Service 实现
- [ ] `ExpandBody(name, anchor, maxBytes)` 只处理 `SKILL.md` / H2/H3 切片
- [ ] `ReadResource(name, path, maxBytes)` 只处理 skill dir 资源文件
- [ ] body/resource 都返回 `version/hash + truncated + total_bytes`
- [ ] 不再使用 `section` 同时承载 anchor 与 path

### 6.3 兼容策略
- [ ] 如保留 `skill_expand` 别名：
  - [ ] 无 `section` 转发到 `skill_expand_body`
  - [ ] path 场景明确拒绝并提示改用 `skill_read_resource`
- [ ] 兼容别名标记为过渡期，仅保留一个 release 周期

### 6.4 测试
- [ ] `anchor="#Usage"` 正确切片
- [ ] `path="references/api.md"` 正确读取
- [ ] `path="../secret"` 正确拒绝
- [ ] body/resource 分别命中各自审批缓存

---

## 7. 工作流 E：Resolver / Expanded State

### 7.1 expanded state 模型
- [ ] 新建 `expanded_state.go`
- [ ] key 结构升级为：`name + kind + locator + hash`
- [ ] 同一 skill 不同 anchor/resource 不再互相抑制
- [ ] resume / compact / re-request 时可失效

### 7.2 Resolver
- [ ] 去重键至少使用 `name@version`
- [ ] trigger / force / manual 决策矩阵仍符合 P20 主路线
- [ ] expanded 过的 body/resource 只对相同 artifact 生效

### 7.3 测试
- [ ] `skill_expand_body("foo", "#Usage")` 后，读取 `references/api.md` 不受影响
- [ ] 同一 body hash 未变化时，近 5 turn 不重复注入
- [ ] hash 变化后会重新注入 / 重新审批

---

## 8. 工作流 F：Manifest / Prompt / Catalog

### 8.1 SkillCatalogProvider
- [ ] 新增 `skill_catalog_provider.go`
- [ ] provider 仅渲染**安全投影**，不直接透传 untrusted 未审批作者文案
- [ ] summary 预算统一 ≤160 字符
- [ ] manifest 尾部工具提示已改成：
  - [ ] `skill_expand_body(name)`
  - [ ] `skill_read_resource(name, path)`

### 8.2 缓存
- [ ] manifest cache key 已纳入：
  - [ ] `skillRevision`
  - [ ] `approvalRevision`
  - [ ] `trustRevision`
  - [ ] `tokenBudget`
  - [ ] `progressiveDisclosureFlag`
- [ ] approval 后无需重开 session 即可刷新 manifest

### 8.3 测试
- [ ] approved skill 正常显示 description + summary
- [ ] untrusted 未审批 skill 只显示 redacted 模板
- [ ] approval 后 redacted → normal manifest 立即切换
- [ ] revision 变化时 cache 失效

---

## 9. 工作流 G：Metrics / Rollout / 灰度

### 9.1 指标
- [ ] `skill_invalid_mode_total`
- [ ] `skill_untrusted_manifest_redaction_total`
- [ ] `skill_trim_corruption_fallback_count`
- [ ] `skill_artifact_approval_miss_total`
- [ ] 原 P20 指标仍保持：
  - [ ] `skill_expand_invoke_rate`（如命名调整需同步文档）
  - [ ] `skill_context_tokens_saved`
  - [ ] `skill_injection_decision`

### 9.2 灰度要求
- [ ] Shadow 模式不改实际 prompt，仅做 diff / metrics
- [ ] Dogfood 阶段额外观察：redaction 覆盖率、invalid mode 数量、trim fallback 比例
- [ ] 灰度开关关闭时，`skill_expand_body` / `skill_read_resource` / `skill_list` 仍可用

---

## 10. 文档收口清单

- [ ] `docs/plans/迁移/p20/README.md` 已加入 P20.1 与 checklist 引用
- [ ] `docs/plans/迁移/p20/p20-original-plan.md` 已加入 P20.1 跳转说明
- [ ] `docs/doc/codemap/07-module.md` 已反映 body/resource 工具拆分
- [ ] `docs/doc/codemap/09-provider.md` 已反映 provider 新注入语义
- [ ] `docs/doc/codemap/ai-index.json` 已更新锚点
- [ ] P20.1 正文中的实施状态矩阵已按最新真实情况更新

---

## 11. 最终验收（交付前逐项勾）

### 11.1 功能
- [ ] L1 / L2 / L3 主路径可跑通
- [ ] 工具调用语义清晰：body 与 resource 分离
- [ ] 两家 provider 行为对齐

### 11.2 安全
- [ ] 未审批 untrusted 元数据不会污染模型上下文
- [ ] path escape 全拒绝
- [ ] artifact 级审批命中正确

### 11.3 正确性
- [ ] rollout trim 不误删 block 后普通文本
- [ ] invalid mode 不会失败展开成 Full
- [ ] expanded state 不会错误去重不同 resource

### 11.4 测试
- [ ] `go test ./internal/module/skill/...`
- [ ] `go test ./internal/module/turn/...`
- [ ] `go test ./internal/module/prompt/...`
- [ ] `go test ./internal/provider/...`
- [ ] 关键 golden / integration 全绿

### 11.5 交付说明
- [ ] 记录本轮实现偏差（若有）
- [ ] 记录未做项与后续 P21/P22 承接项
- [ ] 说明本轮**未进行 git 提交 / 推送**

---

## 12. 一句话执行顺序

```text
先过红线（净化 / trim / 审批 / invalid mode）→ 再拆工具 → 再补 resolver/state → 再补 manifest/cache → 最后灰度与文档收口
```

---

## 13. Phase 10 / Phase 11 落地实际状态（2026-04-19）

### 13.1 Phase 10 灰度 + 指标

commit：`00b073f` + audit fix `9f0f4bd`

- [x] `ENABLE_SKILL_PROGRESSIVE_DISCLOSURE` 总开关默认 false
- [x] `SKILL_CATALOG_TOKEN_BUDGET` env（token，默认 3000，≤0/非法回落）
- [x] `SKILL_CATALOG_META_INSTRUCTIONS` env（默认 true，Phase 9 行为）
- [x] `skill_catalog` dynamic section spec 常驻列表；flag=off 时 provider 未绑定 → (nil, nil)
- [x] fx 灰度闸门：`RegisterSkillCatalogProviderIfEnabled` 四态都有结构化日志
- [x] `compositeNativeSkillDetector` 聚合 `group:"skill_injection_ports"`
- [x] claudecli / codexapp 按 `prompt.SkillInjectionPortGroupTag` 注入（避免字符串漂移）
- [x] 5 个 counter 入仓（pkg/skillmetrics）：
  - [x] `skill_invalid_mode_total` —— dto/provider/turn.go default 分支
  - [x] `skill_untrusted_manifest_redaction_total` —— SkillCatalogProvider Redacted 入栈
  - [x] `skill_trim_corruption_fallback_count` —— rollout_markers.go footer-missing（legacy 不计）
  - [x] `skill_artifact_approval_miss_total` —— approval.go `LookupArtifact` miss / mismatch
  - [x] `skill_expand_invoke_rate` —— skills_expand.go `ExpandBody` + `ReadResource`
- [x] Audit 补丁（commit `9f0f4bd`）：删死代码 / `NewSkillCatalogProviderFx` nil-cfg 防御 / `legacy` 路径误报修复
- [x] `pkg/skillmetrics` 最小测试覆盖 Inc/Read/Snapshot/Reset
- [x] `SkillCatalogProviderIfEnabled` fx 测试覆盖四态 + 聚合 detector 三种样本

### 13.2 Phase 11 文档 + codemap

- [x] `docs/plans/迁移/p20.1-skill-progressive-disclosure-hardening.md` Phase 10 表格补落地点 + 灰度入口 + fx 接线
- [x] 本文件（checklist）补 §13 Phase 10/11 状态溰溺
- [x] `docs/plans/迁移/p20/status-checkpoint-2026-04-19.md` Phase 表格补 Phase 8/9/10/11 勾选 + commit
- [x] `docs/doc/codemap/07-module.md` §2.3 skill 文件地图补新增文件
- [x] `docs/doc/codemap/11-memory-prompt-thread.md` §2.3 prompt 补 `DynamicSectionSkillCatalog` / `SkillCatalogProvider` 与 env flags
- [x] `make codemap-refresh` 重生 `ai-index.json`
