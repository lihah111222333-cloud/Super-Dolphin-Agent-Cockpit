# P2 修复审查 — Agent C
## I1-I5 逐项验证+行号
- `I1 / timeout`：`internal/module/skill/exec.go:57-59` 通过 `platformconfig.WithRPCRequestTimeout(ctx)` 包装执行；30s 常量来自 `internal/platform/config/timeouts.go:15,19-20` 的 `RPCRequestTimeout = 30 * time.Second`。`internal/archtest/timeout_locality_test.go:13-23,36-41,49-50` 明确把 `context.WithTimeout` 约束到 `platform/config/timeouts.go`，当前落点符合守卫。对照 V2，`go-agent-v2/internal/apiserver/methods_command.go:55-56` 是直接 `context.WithTimeout(ctx, 30*time.Second)`。
- `I1 / cwd fallback`：`internal/module/skill/exec.go:56-59,107-111` 在空 `cwd` 时回退到 `s.projectRoot`。注入链闭合：`internal/platform/config/module.go:5` 提供 config；`internal/app/modules.go:25,32` 同时装入 `config.Module` 与 `skill.Module`；`internal/platform/config/config.go:15-20,24-33` 生成 `ProjectRoot`，值来自 `PROJECT_ROOT` 或 `os.Getwd()`；`internal/module/skill/module.go:20-25` 把 `cfg.ProjectRoot` 传给 `internal/module/skill/service.go:29-35`。对照 V2，fallback 落在 `go-agent-v2/internal/apiserver/methods_command.go:58-62`，其 project root 读取函数定义于 `go-agent-v2/internal/apiserver/methods_ui_projects.go:89-100`。
- `Blocker / I1 / env 等价性`：V3 注入的环境是基础变量 `PATH/HOME/USER/LOGNAME/SHELL/TMPDIR/TMP/TEMP/LANG/LC_ALL/TERM`（`internal/module/skill/exec.go:28-30,122-129`）+ `PWD`（`internal/module/skill/exec.go:114-119`）+ 前缀白名单 `OPENAI_/ANTHROPIC_/CODEX_/DYN_TOOL_/MODEL/LOG_LEVEL/AGENT_/MCP_/APP_/STRESS_TEST_/TEST_E2E_`（`internal/module/skill/exec.go:32-34,132-150`）。这些前缀与 V2 `go-agent-v2/internal/apiserver/methods_config.go:81-100` 一致，但 V2 `command/exec` 还接受调用方传入的 `env map[string]string` 并在 `os.Environ()` 之上做 allowlist overlay（`go-agent-v2/internal/apiserver/methods_command.go:20-24,63-68`）。V3 RPC `internal/module/skill/rpc_types.go:26-30` 与 service contract `internal/module/skill/contract.go:13` 已无 `env` 字段，handler 也只传 `command/args/cwd`（`internal/module/skill/rpc.go:51-53`）。因此“白名单存在”成立，但“与 V2 methods_command.go 等价”不成立。
- `I2`：`internal/module/skill/service.go:29-35` 在构造函数里固定 `http.Client{Timeout: 15 * time.Second}`，满足要求。
- `Warning / I3`：`cardByKeyHandler` 仍只覆盖 3 个路由，定义在 `internal/module/skill/rpc.go:12-16`，引用位点是 `internal/module/skill/rpc.go:45,48,50`。新增 `cardCreateHandler` / `cardUpdateHandler` / `cardRunHandler` 定义在 `internal/module/skill/rpc.go:18-34`，分别用于 `internal/module/skill/rpc.go:46-49`；`command/card/list` 仍是直接 handler（`internal/module/skill/rpc.go:44`）。因此 card factory 实际从 `3/7` 扩到 `6/7`，不是计划里的 `7/7`；带特殊参数整形的 handler 没有被强塞进 `cardByKeyHandler`，而是拆成专用 adapter。
- `I4`：P7 TODO 同时存在于 `internal/module/skill/module.go:12-14`（事件驱动 auto-match 尚未接线）和 `internal/module/skill/skills_match.go:64-69`（configured matches 仍是 config-read 占位实现）。
- `I5`：`skills/list` 已有语义注释 `internal/module/skill/rpc.go:54-56`；thread 侧对称注释位于 `internal/module/thread/rpc.go:68-70`。两边都明确区分“扫描本地已安装 skill”与“thread 绑定的 active skills”。

## I6/I8 文档
- `I6/I8` 已写入 `docs/plans/迁移/p2-execution-plan.md`：表格条目在 `docs/plans/迁移/p2-execution-plan.md:55-56`，补充标注在 `docs/plans/迁移/p2-execution-plan.md:76-79`。

## 跨模块一致性
- `handler.Map` 总 key 数：按批次 A/B/C 三个主模块统计，`skill` 22 个（`internal/module/skill/rpc.go:44-85`），`workspace` 8 个（`internal/module/workspace/rpc.go:15-22`），`orchestration` 15 个（`internal/sidecar/orch/orchestration/rpc.go:17-78`），合计 45 个。若把 I5 只加注释的 `thread` 一并算上，则 `thread` 还有 28 个 key（`internal/module/thread/rpc.go:20-82`）。
- `workspace` 新增事件与 bus sink 对齐：DTO 类型 4 个都在 `internal/dto/workspace/event.go:5-40`，共享 event type 常量在 `internal/dto/shared/event.go:30-33`；service 注入 4 个 emitter 在 `internal/module/workspace/service.go:34-50`；成功路径分别发布 created/statusChanged/merged/aborted，调用点是 `internal/module/workspace/service.go:72,202,239,254`，构造 payload 的 helper 在 `internal/module/workspace/service_helpers.go:204-246`；sink 侧也订阅了同样 4 个事件 `internal/platform/bus/sink.go:75-80`。这一链路是闭合的。
- `orchestration dag.go/report.go` 与 fx 注册完整：`internal/sidecar/orch/orchestration/service.go:29-38,78-87` 的 service 已持有并接收 `dagStore`；`internal/sidecar/orch/orchestration/module.go:15-23` 同时提供 `NewService`、`func(s *service) Service { return s }` 和 `NewOrchestrationHandlers`；接口面在 `internal/sidecar/orch/orchestration/contract.go:19-27` 暴露 `GetState/GetReport/RememberReportRequest/HandleReportEvent/CreateDAG/GetDAG/ListDAGs/UpdateNodeStatus`；RPC handler 实际调用这些方法 `internal/sidecar/orch/orchestration/rpc.go:49-71`；实现分别落在 `internal/sidecar/orch/orchestration/report.go:32-122` 和 `internal/sidecar/orch/orchestration/dag.go:14-79`。注册完整，无悬空方法。
- `skill projectRoot` 注入链闭合：`config.Module` 提供 `config.New`（`internal/platform/config/module.go:5`），应用装配引入 `config.Module` 与 `skill.Module`（`internal/app/modules.go:23-36`）；`config.New` 生成 `ProjectRoot`（`internal/platform/config/config.go:15-20`），`skill.newService` 取 `cfg.ProjectRoot`（`internal/module/skill/module.go:20-25`），`skill.NewService` 保存为 `projectRoot`（`internal/module/skill/service.go:29-35`），`ExecCommand` 在空 `cwd` 时消费该字段（`internal/module/skill/exec.go:56-59,107-111`）。链路闭合。

## 编译守卫
- `go build ./...` 通过。
- `go vet ./...` 通过。
- `go test ./internal/archtest/...` 通过。
- LSP diagnostics 复跑后为 0 条：`internal/module/skill/*`、`internal/module/thread/rpc.go`、`internal/sidecar/orch/orchestration/*`、`internal/module/workspace/*`、`internal/dto/workspace/event.go`、`internal/dto/shared/event.go`、`internal/platform/bus/sink.go`、`internal/platform/config/*` 均无 diagnostics。
- 当前 worktree 变更文件未发现超过 400 行的文件。关键上界：`internal/sidecar/orch/orchestration/service.go` 381 行，`internal/provider/codexapp/session.go` 356 行，`internal/module/workspace/service.go` 322 行，`internal/module/workspace/service_helpers.go` 283 行，`internal/sidecar/orch/orchestration/dag.go` 256 行，`internal/sidecar/orch/orchestration/report.go` 228 行；archtest 上限定义见 `internal/archtest/guardlib.go:17-25`。

## 结论（Blocker / Warning / OK）
- `Blocker`：I1 仍未达到 V2 等价。`command/exec` 的 whitelist 已补，但 caller-supplied `env` overlay 能力缺失，证据见 `internal/module/skill/rpc_types.go:26-30`、`internal/module/skill/contract.go:13`、`internal/module/skill/rpc.go:51-53` 对照 `go-agent-v2/internal/apiserver/methods_command.go:20-24,63-68`。
- `Warning`：I3 工厂扩展未到 `7/7`。当前状态是 `cardByKey 3/7 + 专用 handler 3/7 + list 直连 1/7`，证据见 `internal/module/skill/rpc.go:12-50`。
- `OK`：I2/I4/I5/I6/I8、workspace 事件链、orchestration DAG/report 注入链、projectRoot 注入链、build/vet/archtest、LSP diagnostics、`≤400` 文件守卫均成立，证据见本报告对应条目。

## 互辩
### 对 audit-p2-fix-A 的批判
1. `audit-p2-fix-A.md:15-20` 把 `B12` 的 `OK` 证据停在 `module.go/service.go/event.go`，证据链不完整。workspace typed event 真正闭环还依赖共享 event type 常量 `internal/dto/shared/event.go:30-33`，以及 sink 端订阅 `internal/platform/bus/sink.go:75-80`。LSP references 也显示 `WorkspaceRunCreated` / `WorkspaceRunAborted` 不只被 emitter 使用，还被 sink 消费，见 `internal/dto/workspace/event.go:6,31` 的引用到 `internal/module/workspace/service_helpers.go:205,233` 与 `internal/platform/bus/sink.go:76,79`。A 没核这两段，就把 `B12` 判成 `OK`，跨模块证据不足。
2. `audit-p2-fix-A.md:20,33-34` 把 merge conflict/error 路径“不发事件”降成 `Warning`，优先级偏轻。代码里 `internal/module/workspace/service.go:219-221` 直接把 `result.Status` 留在 `statusActive` 后返回，既不会进入 `transitionRunStatus` 的 `internal/module/workspace/service.go:223-234`，也不会走成功路径的 `s.emitRunMergedEvent(...)`（`internal/module/workspace/service.go:239`）；而当前唯一显式状态事件发送点是 `internal/module/workspace/service.go:190-204` 的 `UpdateRunStatus`。这意味着失败 merge 对 bus 观察者完全不可见，比普通 `Warning` 更接近 `Blocker`。
3. `audit-p2-fix-A.md` 漏掉了更实质的 `CreateRun` 一致性风险。`internal/module/workspace/service.go:65-70` 先 `s.store.UpsertRun`，再调用 `s.upsertRunFiles`；`internal/module/workspace/service_helpers.go:166-177` 会逐条 `UpsertFile`，任一文件失败就直接返回。这里没有事务，也没有回滚 run row、已写 file row 或已创建的 workspace 目录（目录创建与 bootstrap 发生在 `internal/module/workspace/service.go:58-63`）。A 抓到了 B6/B8 的功能差异，却漏掉了这个更基础的原子性问题。

### 对 audit-p2-fix-B 的批判
1. `audit-p2-fix-B.md:20,41` 把 `orchestration/report` 仍是写接口只算 `Warning/非阻断`，这和计划本身冲突。`docs/plans/迁移/p2-execution-plan.md:38` 明写 `orchestration/report 改为真实读 lastReport`；但当前 handler 仍在 `internal/sidecar/orch/orchestration/rpc.go:73-77` 调 `svc.SetReport`，其实现只是写 `lastReport`（`internal/sidecar/orch/orchestration/report.go:32-42`）。真正的读接口 `GetReport` 已经存在于 `internal/sidecar/orch/orchestration/report.go:55-64`，却没有被 `orchestration/report` 路由使用。B 把这个判轻了，真实优先级应当是 `Blocker`。
2. `audit-p2-fix-B.md:9,41` 只把“空 `eventType` 也能成功”列成 `Warning`，优先级失真。`internal/sidecar/orch/orchestration/report.go:91-96` 只校验 `agentID`，不校验 `eventType`；但 requester 是否会被 drain 依赖 `internal/sidecar/orch/orchestration/report.go:109-110` 的 `isTerminalReportEvent`，而这个判定又完全依赖 `internal/sidecar/orch/orchestration/report.go:201-207` 对 `eventType` 的解析。空 `eventType` 不只是 schema 漂移，而是直接削弱完成通知链。V2 在 `go-agent-v2/internal/apiserver/methods_orchestration.go:190-193` 把 `event_type` 为空视为硬错误。B 却把返回 key mismatch 放成 `Blocker`，把这个语义断裂降成 `Warning`，轻重倒置。
3. `audit-p2-fix-B.md:12-16` 对 `B14` 的 `OK` 结论只证到模块内部，没有证到应用装配可达性。`orchestration` handler 真正生效不仅要有 `internal/sidecar/orch/orchestration/module.go:15-23` 的 `NewOrchestrationHandlers`，还要 `internal/app/modules.go:23-36` 把 `orchestration.Module` 装进应用，并由 `internal/platform/rpc/module.go:33-49` 收集 `group:"rpc_handlers"` 后统一 `server.Register(...)`。B 的证据链停在 module/store 层，没有把这条跨模块 reachability 链闭合。
