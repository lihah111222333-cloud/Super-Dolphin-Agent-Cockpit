# P24 记忆系统优化

## 文档目的

记录 Super-Dolphin 记忆子系统从 Phase 1 → Phase 2.x 的硬化路线图，标记每个子阶段的状态与产出。当前位置：Phase 2.1.AB.7（cache-root-threading）已落地（worktree 未 commit），AB.5/AB.6/AB.7 共 8+7+1 项代码改动 + 9 项文档修订完成；下一步选项见文末「下一步候选」。

参考资料：
- 路线图来源：Claude 记忆系统对照分析 + Super-Dolphin 现状审计
- ~~`internal/module/memory/PHASE_TRACKER.md`~~ — AB.5 后已删除；slug 现仅以源码 `TODO(tracker:<slug>)` 反查，本文档为唯一索引

## 术语表

下文混用术语在此固定；函数名 / op 名 / JSON 字段名仍保留英文：

| 术语 | 中文 | 含义 |
|---|---|---|
| readback | 写后读 | 写入磁盘后立即重读以确认落盘并合成 RPC 响应；下文叙述统一用「写后读」 |
| harness | 运行壳 | 决定 entrypoint 注入与 overlay 抑制的运行时身份（generic / claude_code / codex） |
| sentinel | 哨兵错误 | `errors.Is` 可识别的导出错误值，用于 RPC 边界归一化 |
| slug | 锚点 | 跨文件追踪 backlog 的短键（源码 `TODO(tracker:<slug>)`） |
| redact | 脱敏 | RPC 边界把含路径错误替换为 sentinel，原 cause 仅入日志 |
| oracle | 信号 | 攻击者借响应差异区分两种内部状态（NotFound vs Containment 即典型 oracle） |
| pathsafe | 路径安全 | 内存读单一防御性原语 `SafeReadEntrypoint(root, rel)`（`memshared` 包） |
| containment | 围栏 | EvalSymlinks 后路径仍在 root 内的强制断言 |
| fold | 合入 | 把审查 Should-fix 合到下一轮一并修复，避免单独阶段 |
| frontmatter | 头部元数据 | `---` 围栏内的 YAML/键值，约束 mode/scope 等 ACL 字段 |
| scope ACL | 作用域访问控制 | private / team 边界，private 不可触 `team/*` |

## 路线总览

```
Phase 1   止血 + harness 打通                      ✅ 完成
Phase 1.5 双注入修复                                ✅ 完成
Phase 1.6 Invalidation 补全                         ✅ 完成
Phase 1.7 Harness 标识冻结                          ✅ 完成

Phase 2.0   Config.Harness 字段化                   ✅ 完成
Phase 2.0.1 parse 包抽离 + 并发契约                 ✅ 完成
Phase 2.0.2 多 BOM / fence 统一 + frontmatter 加固  ✅ 完成
Phase 2.0.3 fence scanner 上移 + Harness 文档去重   ✅ 完成

Phase 2.1.A    Path 旁路收口（5 站点）              ✅ 完成
Phase 2.1.B    Nested loader 围栏                   ✅ 完成
Phase 2.1.AB.1 SafeReadEntrypoint 错误返回 + sentinels ✅ 完成
Phase 2.1.AB.2 isUserVisibleNotFound + 路径 redact  ✅ 完成
Phase 2.1.AB.3 全 6 RPC 边界 redact + logger 注入   ✅ 完成
Phase 2.1.AB.4 耐久记忆 redact + 写后读语义修正     ✅ 完成（Should-fix 在 AB.5 fold 落地）
Phase 2.1.AB.5 design fold + 8 项错误修复            ✅ 完成（worktree 未 commit）
Phase 2.1.AB.6 五路复审 punch list（P0/P1 + 小清理）  ✅ 完成（worktree 未 commit；小清理 C op 重命名推迟）
Phase 2.1.AB.7 cache-root-threading（NestedRuntime 持 cacheRoot + SafeReadEntrypoint 收口）  ✅ 完成（worktree 未 commit）

Phase 2.1.C  HTML stripper 重写 + 三份去重           ✅ 完成（AB.5 去重 + 2.1.C 状态机 + CDATA + fixed-point + fuzz）

Phase 2.1.D  项目 CLAUDE.md 不信任化（含威胁模型）   ⬜ 未启动

Phase 2.2    Tristate feature gate + opaque cache token ⬜ 已规划
Phase 2.3    Per-turn TurnExtractor + 并发交叉测试      ⬜ 已规划
```

---

## ✅ Phase 1 系列：harness 打通 + 立即止血

**目标**：解决 MEMORY.md 双注入、harness 切换时 invalidation 不完整、Phase 1.7 harness 标识在运行时被环境变量篡改等问题。

**关键产出**：
- `MemoryHarness` 枚举（generic / claude_code / codex）+ overlay 抑制
- `MemoryEntrypointProvider` 单一注入路径（去除 turn-fallback 双注入）
- nested loader 不再候选 AutoMem / TeamMem 的 MEMORY.md
- `writeDetectedIntent` / `deleteIntent` / `RunConsolidation` / `launchAutoDreamTask` / `consumeTurnTracking` 全部接 invalidation
- prompt section 排序固定：`memory(120) < memory_entrypoint(122) < agent_memory(123) < memory_context(125)`

---

## ✅ Phase 2.0 系列：parse 包合并 + 并发契约

**目标**：消除三份重复的 frontmatter 解析代码，确认 SectionInvalidator 并发安全。

**关键产出**：
- 新 `internal/module/memory/parse` 包：`SplitFrontmatter` / `IsFence` / `StripUTF8BOM` / `ScanFrontmatterHeader`
- 多 BOM 循环 strip（防 `﻿﻿---` 绕过 frontmatter）
- `IsFence` 容忍尾部空白（`---\t`、`--- `）
- `contract.SectionInvalidator` 并发契约文档化
- `var _ contract.SectionInvalidator = (*service)(nil)` 编译期断言
- `TestServiceInvalidateSectionsIsConcurrentSafe`（16×200 goroutine）
- `Config.Harness` 字段在 `NewConfig()` 时冻结
- 删 `index.go` 与 `retrieval/entry_read.go` 的本地 fence 扫描器，统一调 `parse.ScanFrontmatterHeader`

---

## ✅ Phase 2.1.A + 2.1.B：path hardening + nested 围栏

**目标**：所有内存入口读取走单一防御性原语，nested CLAUDE.md 候选 + 加载双层围栏。

**关键产出**：
- `memshared.SafeReadEntrypoint(root, path)` — EvalSymlinks(root + path) + ContainsPath + Stat（拒目录） + ReadFile，单调用
- 5 个旁路站点切换：`entrypoint_provider`、`agent.loadAgentMemoryEntrypoint`、`ui_rpc.scanUIAgentScope`、`ui_rpc_mutations.readUIAgentMemory`、`nested.loadStandardClaudeMdSource`
- `nested.appendClaudeMdCandidate` 在 `EvalSymlinks` 后强制 `ContainsPath(BaseDir)` 校验
- `extractNestedReadToolPaths` 加 `IsAbs + Clean(path)==path` 门（cache-root 收口归后续 phase）
- 删 `index.go::readMemoryEntryHeader` 死代码

---

## ✅ Phase 2.1.AB.1：API 升级 + 测试加固

**目标**：消化 2.1.A+B 审查的 Should-fix。

**关键产出**：
- `SafeReadEntrypoint` 签名 `(content, info, bool)` → `(content, info, error)`，4 sentinel：
  - `ErrSafeReadNotFound`（`%w` 包 `os.ErrNotExist`）
  - `ErrSafeReadContainment`
  - `ErrSafeReadIsDir`
  - `ErrSafeReadBrokenLink`
- 删 `path.go::safeReadEntrypoint` wrapper（合并到 memshared）
- 7 个 memshared 直测：happy path / NotFound（文件 + 根） / Containment / IsDir / dangling-symlink / Permission / 多 BOM 边界

---

## ✅ Phase 2.1.AB.2：UI 边界 redact 起步

**目标**：消化 2.1.AB.1 审查的 High（`readUIAgentMemory` ENOENT 折叠）+ Should-fix。

**关键产出**：
- 新增 `isUserVisibleNotFound` predicate + `errAgentMemoryReadFailed` sentinel（AB.2 时落在新文件 `ui_error_policy.go`，AB.5 #7 后并入 `ui_rpc.go`）
- `readUIAgentMemory` 切换到 switch + sentinel：NotFound + Containment 折叠为空内容（关 oracle），其它错误 default → log + 返 sentinel
- `ErrSafeReadBrokenLink` 三处 wrap 改双 `%w`（Go 1.25 多重包裹）
- 3 新 memshared 测试：symlinked-root / parent-dir-symlink-escape / symlink-loop（multi-Unwrap 不变量）
- 新文件 `PHASE_TRACKER.md` 5 条 backlog（注：该文件后阶段在用户要求下被删除，详见下文 AB.5 复审 punch list P1 #5；slug 语义仍以代码内联 `TODO(tracker:<slug>)` 为准）。

---

## ✅ Phase 2.1.AB.3：全 RPC 边界 redact + logger 注入

**目标**：消化 2.1.AB.2 审查的 Med（仅 1 站点 redact 不够）+ Should-fix（slog.Default → threaded logger）+ Should-fix（slug-ify TODO）。

**关键产出**：
- 新增 3 sentinels：`errAgentMemoryScanFailed/SaveFailed/DeleteFailed`
- 通用 helper `redactRPCError(logger, op, public, cause, attrs...)`
- `memoryHandlerDeps` 加 `Logger *slog.Logger`（fx optional）
- 4 层签名 threading：`buildUIMemorySnapshot → loadUIAgentMemoryScopes → scanUIAgentScope → readUIAgentMemory`
- 6 个 agent-memory RPC 边界全部 redact
- `PHASE_TRACKER.md` 每条加 slug；inline TODO 改 `TODO(tracker:cache-root-threading)`

---

## ✅ Phase 2.1.AB.4：耐久记忆 redact + 写后读修正

**状态**：代码已落地、`go vet` 与全量测试通过、3 方审查仅 design 报告回来（correctness/security 在 orchestration 中丢失或 failed）。Design Should-fix 三条已在 AB.5 fold 落地。

**已落地**：
- 新增 4 sentinels：`errDurableMemoryReadFailed/ScanFailed/SaveFailed/DeleteFailed`
- `errorBearsFilesystemPath(err)` 分类器（本轮后在 AB.5 跳接 allowlist 模型）
- `redactIfPathBearing(logger, op, public, err, attrs...)` —— validation 错误直通，路径错误才 redact
- 4 个 durable RPC 边界包装：`loadUIMemoryScope`（私有 + Team） / `getUIMemoryEntry` / `upsertUIMemoryEntry` / `deleteUIMemoryEntry`
- **`saveUIAgentMemory` 写后读语义修正**：`os.WriteFile` 成功后若 `readUIAgentMemory` 失败，合成最小 `UIAgentMemoryDetail{Path, Content: req.Content, UpdatedAt: time.Now().UTC()}` 返回 nil err（关 post-write oracle；AB.5 加上 `WriteCommittedReadbackFailed` 标志）
- `PHASE_TRACKER.md` 顶部加 "Phase 2.1 rollup" 6 段总结

---

## ✅ Phase 2.1.AB.5：design Should-fix fold + 8 项错误修复（worktree 未 commit）

**触发**：用户要求轻量复审与修复；5 路并行 reviewer 代码 + 文档审查（Explore agent × 5）后，亲自读代码验证区分真实 bug 与误报，在本轮 fold AB.4 两条 Should-fix。

**范围**：为避免范围扩散，`safe-read-toctou`（`os.OpenRoot` 重写）与 `cache-root-threading`（签名变更）仍留在 PHASE_TRACKER Open。

**落地 8 项**：
1. **`parse` 包扩展为多份工具单一来源**——加 `JSStringLength` / `UTF16CodeUnits` / `TruncateAtCodeUnitLimit` / `StripHTMLComments`。
2. **truncate 工具去重**：`memory.truncate.go` 与 `agent/agent_type.go` 三份 `truncateAtCodeUnitLimit` / `jsStringLength` / `utf16CodeUnits` 合并到 `parse.*`；顺手修 `lastNewline > 0` 边界 → `>= 0`。
3. **`stripHTMLComments` 4 份合并**：`extract.go` / `nested/claudemd_parse.go` / `nested/claudemd_sources.go` / `retrieval/entry_read.go` / `entrypoint_provider.go` / `parser_test.go` 全部改调 `parse.StripHTMLComments`；4 份本地实现与 helper 全删（原"行首只剩行 / 多行块 / 行内不剩"语义保留）。这是 Phase 2.1.C "3-copy dedupe" 子项的提前落地；"重写状态机 + nested/CDATA + fuzz" 仍留给 2.1.C。
4. **`errorBearsFilesystemPath` 黑名单 → allowlist**（AB.4 Should-fix #2）：引入 `publicValidationError` 类型 + `publicValidationErr(msg)` helper；`redactIfPathBearing` 逆转语义，默认 redact，仅 publicValidation 直通。`ui_rpc_mutations.go` 6 处需直通的 validation 错误改用 `publicValidationErr`（行 + 实际文案）：
   1. 行 203 `现有 durable memory 暂不支持改名；如需改名请删除后重建`
   2. 行 206 `现有 durable memory 暂不支持改类型；如需改类型请删除后重建`
   3. 行 389 `memory service is not configured`
   4. 行 409 `unknown memory target %q`
   5. 行 448 `path is required`
   6. 行 451 `private durable memory cannot access team/ paths`

   与 AB.6 小清理 A 的「9 处一致化」**无重叠**：这 6 处已包，那 9 处是另外仍裸 `errors.New` 的位置（详见 AB.6 数字口径表）。删除冗余 `io/fs` import。
5. **`saveUIAgentMemory` 加 `WriteCommittedReadbackFailed` 标志**（AB.4 Should-fix #1）：`UIAgentMemoryDetail` 增加 `WriteCommittedReadbackFailed bool` JSON 字段。写后读失败时置 true 并 `redactRPCError` 记 Warn 日志（op=`agent_memory_save_readback`）。下游 KAIROS / consolidation 可辨识快照字段是否需回查磁盘。
6. **`agent.Config` 补文档**：明确不存在 `Harness` 字段是有意为之（agent-memory 加载与 harness 无关、无 overlay 闸门）；同时约定未来若加该字段必须与 `memory.Config.Harness` 一样不可变。
7. **`isUserVisibleNotFound` 拆分**：原实现把 NotFound 与 Containment 折叠，掩盖攻击信号。拆为 `isUserVisibleNotFound`（仅 NotFound）+ `isContainmentRejection`；`readUIAgentMemory` Containment 路径调 `redactRPCError` Warn（op=`agent_memory_read_containment`）后再返空内容。
8. **`pathsafe` 双 `%w` 权限透传单测**：新增 `TestSafeReadEntrypointBrokenLinkPermissionUnwrapsCause`，通过 symlink 指向 chmod 000 目录触发 EvalSymlinks EACCES，同时断言 `errors.Is(err, ErrSafeReadBrokenLink)` 与 `errors.Is(err, os.ErrPermission)` 都通过双 `%w` 连锁。

**验证**：`go build ./...` · `go vet ./internal/module/memory/...` · `go test -count=1 ./internal/module/memory/...` 均通过。7 个 memory 子包全部 ok。

**净代码量**：净减 ≈ 200 行重复 + 5 处行为/安全修正。

**本轮未动（后阶段范围）**：
- `safe-read-toctou`：`os.OpenRoot` 重写，需改 `SafeReadEntrypoint` 签名并审订十余处调用。**注**：此 slug 暂无源码 `TODO(tracker:...)` 锚点，仅由本文档记录；恢复时需先在 `pathsafe.go` 的 `SafeReadEntrypoint` 上方补 `TODO(tracker:safe-read-toctou)` 注释。
- ~~`cache-root-threading`~~：**已在 Phase 2.1.AB.7 落地**，详见下文同名章节。

---

## ✅ Phase 2.1.AB.6：五路复审 punch list（已落地）

**状态**：AB.5 原 8 项落地 ✅，**改动悬在 worktree 未 commit**（用户约定「非用户指令不自动 commit」）。五路 reviewer 复审抽出 **2 P0 + 3 P1 + 3 小清理**（合计 8 项），本节即 AB.6 punch list。

> **AB.6 落地小结**：8 项中已落地 7 项（2 P0 + 3 P1 + 2 小清理 A/B），小清理 C「op 重命名」经互审证实**外部无消费者**，价值/成本比低，**推迟到后续阶段**或不做。下方 P0/P1 描述保留为历史叙述。

**数字口径收口**（避免后人混淆）：

| 来源 | 数字 | 含义 |
|---|---|---|
| AB.5 落地项 #4 | 6 处 | 已包成 `publicValidationErr` 的 6 处（见上节行号清单） |
| AB.6 P1 #4 | 4 处 | `buildUIWriteRequest` 内裸 `errors.New` 待包（行 425/434/437/440） |
| AB.6 小清理 A | 9 处 | 全文件待包总数（含 P1 #4 的 4 处 + 5 处其他位置） |

**P0/P1 与小清理的关系**：P1 #4 是小清理 A 9 处中的 4 处子集；其余 5 处（agentType ×3、`UISharedFile` path ×2）作为小清理一并修。

**P0 错误**：
1. **`scanUIAgentScope` 未对 Containment 分流**（`ui_rpc.go` 行 232-234）。AB.5 只在 `readUIAgentMemory` 拆开了 NotFound/Containment，但 UI scan 路径调 `SafeReadEntrypoint` 后直接 `continue`，containment 攻击信号在 scan 路径仍被静默吞。**违背 AB.5 落地承诺**。

   **改造模板**（参考 `readUIAgentMemory` switch 模式；变量名按 `scanUIAgentScope` 实际作用域：`logger` / `entryPath`，**不要写成 `deps.Logger` / `rel`**）：
   ```go
   raw, info, err := memshared.SafeReadEntrypoint(root, entryPath)
   switch {
   case err == nil:
       // happy path：沿用原 236-243 行 strip BOM + append items 逻辑
   case isUserVisibleNotFound(err):
       continue
   case isContainmentRejection(err):
       redactRPCError(logger, "agent_memory_scope_scan_containment",
           errAgentMemoryScanFailed, err,
           "scope", string(scope), "agent_type", dir.Name())
       continue
   default:
       return nil, redactRPCError(logger, "agent_memory_scope_scan",
           errAgentMemoryScanFailed, err,
           "scope", string(scope), "agent_type", dir.Name())
   }
   ```
   注：新增 op `agent_memory_scope_scan_containment` 落地后，需同步加入小清理 C 的 8 op 清单表。
   **验证**：`go test ./internal/module/memory/... -run TestScanUIAgentScopeContainment`（需新增）。
2. **`isUserVisibleNotFound` 上方 doc 注释过时**（`ui_rpc.go` 行 293-306），还在说"NotFound 与 Containment 都折叠"，与代码新行为相反，会误导后来阅读者。

   **新注释模板**：
   ```go
   // isUserVisibleNotFound returns true only for ErrSafeReadNotFound (i.e.
   // the entrypoint genuinely does not exist on disk). Containment
   // rejections — post-EvalSymlinks paths that escape the scope root —
   // are now classified by isContainmentRejection and surfaced via
   // Warn-level redactRPCError so the attack signal is preserved instead
   // of silently folded into "no entry to display". See AB.5 落地项 #7.
   ```

**P1 错误**：
3. **`UIAgentMemoryDetail.WriteCommittedReadbackFailed` 三态语义不足**。现为 `bool + omitempty`，false 被省略。
   - **代码事实**：当前 `saveUIAgentMemory` 只在写后读失败路径显式赋 `true`（`ui_rpc_mutations.go:324`）；`readUIAgentMemory` 与 save 成功路径都不赋值，都是零值 false 被 omit。代码注释仅定义了 `true=失败`，false 与「缺失」的语义未被代码区分。
   - **真正受影响的消费者**：不是前端——前端可按 RPC 方法名（`ui/memory/agent/save` vs `get`）区分调用上下文；受影响的是下游 KAIROS / consolidation 离线读取快照场景（只看 JSON、不知原始 RPC）。
   - **改造路径**：字段改 `*bool` 三态——nil = 写后读未发生（只读路径，字段缺省）、true = 写后读发生但失败（需重读）、false = 写后读发生且成功（快照可信）。**save 成功路径须显式赋 false**（否则仍是 nil、与只读路径不可区）；代码注释同步补三态语义定义。
   - **下游消费点全量审查**（接手者必查）：改 `*bool` 是 wire-level breaking change，全仓搜反序列化点：
     ```bash
     rg -n 'WriteCommittedReadbackFailed' --type go
     ```
     实测现状仅 `ui_rpc_mutations.go:99`（字段定义）、`:324`（赋 true）两处。若 KAIROS / consolidation 的 Go 端读结构体存在，必须同步改 `*bool` 并把「字段缺省」判定从 `false` 改为 `nil`，否则会出现「缺省被解释成 false」的隐性回归。前端 TS 端 `boolean | undefined` 改 `boolean | null | undefined`。
   - **测试**：表驱动覆盖三态 JSON 序列化 + 反序列化（nil 不出现 / true 出现 / false 出现）。
4. **`buildUIWriteRequest` 4 处 validation errors 未包 `publicValidationErr`**（`ui_rpc_mutations.go`）：

   | 行 | 文案 |
   |---|---|
   | 425 | `type must be one of user\|feedback\|project\|reference` |
   | 434 | `name is required` |
   | 437 | `description is required` |
   | 440 | `content is required` |

   这些错误沿调用链会被 `redactIfPathBearing` 吞成 `errDurableMemorySaveFailed`，用户看不到实际原因。

   **反向回归测试**（AB.5 反转 allowlist 后必须补，否则未来新增 validation 错误漏包不会被测出）：构造一个会触发 `publicValidationErr` 的 validation 错误（如缺 name），断言 RPC 返回的错误字符串与 publicValidationErr 一致；再构造一个含路径的 IO 错误，断言返回的是 sentinel 而非原 cause。
5. **代码注释中的 `PHASE_TRACKER.md` 引用为死链**：`nested/nested_runtime.go:351` 仍指向已删除的 `internal/module/memory/PHASE_TRACKER.md`。

   **修法**：把 `nested_runtime.go:346-351` 的 `TODO(tracker:cache-root-threading)` 注释末尾 `See internal/module/memory/PHASE_TRACKER.md.` 改为 `See docs/plans/迁移/p24记忆优化/p24记忆优化.md.`，或直接删除该句。
   **兜底**：全仓 grep 死链作收口检查：
   ```bash
   rg -n 'PHASE_TRACKER' --type go
   ```
   实测当前**仅 `nested_runtime.go:351` 一处**（`shared/pathsafe.go` 并无该引用，第七轮指控为脏数据，本轮改正）。

**小清理（可顺手）**：
- A. **9 处** validation `errors.New` 一致化改 `publicValidationErr`：
  - 3 个 `agentType is required`（`ui_rpc_mutations.go` 行 255 / 285 / 514）
  - 2 个 `path is required`（行 336 / 356）
  - 4 个独立 validation（`buildUIWriteRequest` 内部）：`type must be one of...`（行 425）/ `name is required`（行 434）/ `description is required`（行 437）/ `content is required`（行 440）
  - （其余 4 个系统配置错误不需包装：行 332/352/367 共 3 个 `shared file store is not configured` 变体 + 行 490 `memory service is not configured`）
- B. `parse_test.go` 补 **AB.5 新增的 4 个导出函数**的直接单测：`JSStringLength` / `UTF16CodeUnits` / `TruncateAtCodeUnitLimit` / `StripHTMLComments`（对应 AB.5 落地项 #1）+ CRLF 边界 + 未闭合注释 EOF 场景。parse 包总导出 8 函数中其余 4 个（`SplitFrontmatter` / `IsFence` / `StripUTF8BOM` / `ScanFrontmatterHeader`）已被 `parser_test.go` 间接覆盖，本轮不重复。
- C. 统一 op 命名风格。**8 个现存 op 清单**（grep `redactRPCError` 实测）：

  | 文件:行 | op 现名 | 风格 |
  |---|---|---|
  | ui_rpc.go:185 | `agent_memory_scope_root` | 资源_位置 |
  | ui_rpc.go:219 | `agent_memory_scope_scan` | 资源_动作 |
  | ui_rpc_mutations.go:264 | `agent_memory_delete` | 动作 |
  | ui_rpc_mutations.go:290 | `agent_memory_ensure_dir` | 动作_对象 |
  | ui_rpc_mutations.go:300 | `agent_memory_save` | 动作 |
  | ui_rpc_mutations.go:315 | `agent_memory_save_readback` | 动作_阶段 |
  | ui_rpc_mutations.go:544 | `agent_memory_read_containment` | 动作_原因 |
  | ui_rpc_mutations.go:549 | `agent_memory_read` | 动作 |

  **目标方案**：统一为 `agent_memory_<action>[_<qualifier>]` 形式。`_failed` 后缀冗余（所有 op 都是错误归因）。**AB.6 范围内一并落地**的重命名映射：
  - `scope_root` → `list_scope_root`
  - `scope_scan` → `list_scope`
  - `save_readback` → `save_writeback_read`（保留写后读语义）
  - `read_containment` → `read_rejected_containment`
  - 其余 4 个（`delete` / `ensure_dir` / `save` / `read`）保持不变

  P0 #1 新增的 `agent_memory_scope_scan_containment` 一并按新规范落位，无需再改。

**误报列表（不要修）**：
- **Reviewer E** ："redactRPCError / redactIfPathBearing / publicValidationErr 核心实现搜不到"——是其检索失败，这 3 个函数均在 `ui_rpc.go`（行 361 / 384 / 400）。
- **上轮中的"pathsafe 单测 panic 安全 / defer 链"项**：`t.Cleanup` 在 panic 时 Go testing 框架保证执行，但在 testing 外层 defer 阶段执行，晚于用户函数体内的 `defer`。cleanup 为 LIFO、`t.TempDir` 是首注册，所以其 `RemoveAll` 实际在所有用户 cleanup 之后才执行（即是在 `chmod 0o755` 恢复权限之后才删 TempDir）。风险实存但等级低：`_=` 忽略 chmod 失败 + OS 在进程退出时清理 /tmp 作兜底。本轮未作为 P1 修；后阶段如出现实际 CI 失败可补 defer 链。

**验证**（已落地后）：`go build ./...` + `go vet ./internal/module/memory/...` + `go test -count=1 ./internal/module/memory/...` 全绿。新增测试：`TestJSStringLengthCases` / `TestUTF16CodeUnitsCases` / `TestTruncateAtCodeUnitLimitCases` / `TestStripHTMLCommentsCases` 共 4 个表驱动单测，覆盖 AB.5 新增 4 个 parse 导出函数。

**实际落地清单（AB.6 完成）**：
- ✅ P0 #1 `scanUIAgentScope` switch 四分支分流 Containment（`ui_rpc.go:232-251`）+ 新增 op `agent_memory_scope_scan_containment`
- ✅ P0 #2 `isUserVisibleNotFound` doc comment 改写（`ui_rpc.go:293-303`）
- ✅ P1 #3 `WriteCommittedReadbackFailed` 改 `*bool` 三态（`ui_rpc_mutations.go:99/324/327` + 新 helper `boolPtr`）；save 成功路径显式赋 `false`
- ✅ P1 #4 `buildUIWriteRequest` 4 处 + 小清理 A 5 处共 9 处 `errors.New` → `publicValidationErr`（`ui_rpc_mutations.go:255/285/336/356/425/434/437/440/514`）
- ✅ P1 #5 `nested_runtime.go:351` 死链改指 p24 文档；`ui_rpc_mutations.go:527` 同源死注释一并修
- ✅ 小清理 B parse 包 4 函数直测（`parse_test.go` 新增 ≈ 90 行）
- ⏸ 小清理 C op 重命名：互审证实 8 个 op 无外部消费者，价值/成本比低，**推迟**

**净代码量**：生产代码 ~85 行 + 测试 ~125 行 + 文档回写 ~25 行；7 个 memory 子包测试全部 ok。


---

## ✅ Phase 2.1.AB.7：cache-root-threading（已落地）

**触发**：AB.6 落地后用户选「继续推进」，拍板进入候选 3 cache-root-threading 的 scope 调研 + 落地。Scope 报告结论：范围可控（3 代码文件 + 1 文档）。

**原问题**：`extractNestedReadToolPaths` 最终在 `nested_runtime.go` 裸 `os.ReadFile(persistedPath)`，`persistedPath` 来自 bus 事件 `tooldto.ToolCallEnd.PersistedPath`。Phase 2.1.A 只加了「`IsAbs && Clean(path)==path`」防御，没有边界根。伪造 `/etc/passwd`、`/Users/.../id_rsa` 这类绝对路径可以让 `nestedIngestWorker` 同步读盘。

**范围与路径**：

| # | 文件 | 改动 |
|---|---|---|
| 1 | `internal/module/memory/nested/nested_runtime.go` | `NestedRuntime` 加 `toolReadCacheRoot` 字段（`r.mu` 保护）；新增 `SetToolReadCacheRoot(root string)` setter；helper `extractNestedReadToolPaths` / `nestedReadToolRaw` / `readNestedPersistedToolOutput` 改签名接受 `cacheRoot string`；裸 `os.ReadFile` 换为 `shared.SafeReadEntrypoint(cacheRoot, persistedPath)`；`cacheRoot` 为空时 helper 直接返 `"", false`（fail-closed）；删 `"os"` import；删原 `TODO(tracker:cache-root-threading)` 注释段 |
| 2 | `internal/module/memory/factory.go` | `newNestedIngestWorkerProvider` 中调 `p.NestedRuntime.SetToolReadCacheRoot(nestedToolReadCacheRoot())`；新增 `nestedToolReadCacheRoot()` 内部 helper，复制 `turn/tool_result_storage.go::toolResultStorageDir()` 的 path 计算逻辑（`os.UserCacheDir()` / `os.TempDir()` + `super-agent-v3/tool-results`），加同源 lock-step 注释。**不让 memory import turn**，避免反转 `memory.Module → prompt.Module → turn` 的装配依赖方向 |
| 3 | `internal/module/memory/nested/nested_runtime_test.go` | 新增 3 个测试：`TestNestedRuntimePersistedToolReadHonorsCacheRoot`（happy path） / `RejectsOutsideCacheRoot`（伪造外部路径被 SafeReadEntrypoint 拒） / `FailsClosedWhenCacheRootUnset`（未调 setter 时拒读）；原 `TestNestedRuntimeAddsReadToolTriggersFromToolResult` 未动（走 preview 路径） |
| 4 | `docs/plans/迁移/p24记忆优化/p24记忆优化.md` | 本文档本身回写 |

**依赖方向决策**：`memory.Module` 在 `internal/app/modules.go` 中处于底层，`turn` 依赖 `memory`（`memory.Module → prompt.Module → turn`）。让 `memory → turn` 会反转依赖。AB.7 落地时先在 memory 包内复制 path 计算 + lock-step 注释作为务实妥协；**该 follow-up 已在 AB.7.1 落地**，详见 AB.7.1 章节：`internal/platform/toolresults` 公共包替代两份复制，archtest rule13 防未来反转。

**Fail-closed 三道门**：
- Setter 未调 → `r.toolReadCacheRoot == ""` → `readNestedPersistedToolOutput` 首句即拒
- `os.UserCacheDir()` + `os.TempDir()` 都返空 → `nestedToolReadCacheRoot()` 返空 → setter 收空仍为空
- `persistedPath` 不在 cacheRoot 内 → `SafeReadEntrypoint` 返 `ErrSafeReadContainment` → helper 返 `"", false` → fallback preview。仅当伪造 `persistedPath` 且 preview 为空时，tool-read trigger 才全部丢失——这正是安全侧需要的结果

**验证**：`go build ./...` + `go vet ./internal/module/memory/...` + `go test -count=1 ./internal/module/memory/...` 全绿，7 个 memory 子包 ok；3 个新增 `TestNestedRuntimePersistedToolRead*` 测试均 PASS。`internal/archtest` 唯一失败 `TestSqlcBoundary` 与本改动无关（是别的 staged 改动、`cmd/mcp-orch/runtime.go` 越界引 sqlc）。

**净代码量**：生产代码 ~80 行（主要是注释；逻辑变更 ≈ 25 行） + 测试 ~80 行 + 文档回写 ≈ 100 行。

**后续可考虑的 follow-up**：
- ~~抽公共 `internal/platform/toolresults` 包让 turn 和 memory 都从中心读 path~~——**已在 AB.7.1 落地**
- ~~补 `internal/archtest` 架构守卫禁止 memory 包 import turn~~——**已在 AB.7 follow-up commit (rule13) 落地**
- `SetToolReadCacheRoot` 加 sanity log：收到非绝对路径时 Warn（当前静默接受）

---

## ✅ Phase 2.1.AB.7.1：lock-step 复制收口到 `internal/platform/toolresults`

**触发**：AB.7 + 4 路审查（R3 NEEDS-FIX → 转 TRUE-BUT-DEFERRED）+ 用户拍板「抽 internal/platform/toolresults 公共包消除 lock-step 注释」。

**范围与路径**：

| # | 文件 | 改动 |
|---|---|---|
| 1 | `internal/platform/toolresults/path.go`（新建） | 导出 `CacheDir() string`：`os.UserCacheDir()` 失败/空 → `os.TempDir()`；两者都为空返 `""`（fail-closed）；不调 `MkdirAll`（写方自行 mkdir，读方走 SafeReadEntrypoint）。包文档说明「memory 不能 import turn，二者都依赖此包」 |
| 2 | `internal/platform/toolresults/path_test.go`（新建） | `TestCacheDirNonEmptyOnTypicalHost` 锚点测试：正常宿主必返非空绝对路径，且后缀为 `super-agent-v3/tool-results` |
| 3 | `internal/module/turn/tool_result_storage.go` | `toolResultStorageDir` 改调 `toolresults.CacheDir()`；空串返 `errors.New` 而非吞下（**修原潜在 bug**：原写法在双 dir 都空时会调 `MkdirAll("super-agent-v3/tool-results", ...)` 创建相对路径目录） |
| 4 | `internal/module/memory/factory.go` | 删 `nestedToolReadCacheRoot()` 内部 helper（25 行 + 注释 ≈ 9 行）；`newNestedIngestWorkerProvider` 直接 `SetToolReadCacheRoot(toolresults.CacheDir())`；删 `os` / `path/filepath` / `strings` 三个 import |
| 5 | `internal/archtest/dependency_direction_wave3_test.go` | rule13 注释更新：从「lock-step 复制」改为「都通过 internal/platform/toolresults 共享」。规则本身不变（仍禁 memory→turn import） |

**净代码量**：新建 ~50 行（含包文档与测试） + 删除 ~35 行（memory 端 helper） + turn 端 ±5 行 + 注释更新。

**lock-step 注释消除**：原本 turn 与 memory 各持一份算法 + 「保持同源」注释；现在唯一来源是 `toolresults.CacheDir()`，两个调用方共享同一个函数。drift 风险消除。

**bug 修复副产物**：原 `turn/tool_result_storage.go::toolResultStorageDir` 在 `os.UserCacheDir()` 与 `os.TempDir()` 都返空时（极罕见），会通过 `filepath.Join("", "super-agent-v3", "tool-results")` 得到相对路径并 mkdir 当前 cwd 下的 `super-agent-v3/tool-results`。新版统一通过 `CacheDir()` 返空 → `errors.New` 失败，写方安全 fail-closed。

---

## ✅ Phase 2.1.C：HTML stripper 状态机重写 + CDATA + fuzz（slug: `html-stripper-rewrite`）

**状态**：全部落地。去重子项在 AB.5 完成（4 份实现 + 6 个调用点 → 单点 `parse.StripHTMLComments`）；状态机重写 + nested/CDATA + fuzz 本轮完成。

**架构增量：零**。所有改动在 `internal/module/memory/parse` 包内部，`StripHTMLComments` 导出签名不变，8 个上游调用点（AB.5 提前收口后）零改动。

**范围与路径**：

| # | 文件 | 改动 |
|---|---|---|
| 1 | `internal/module/memory/parse/parse.go` | `htmlCommentStripState` 加 `inCDATA bool` 字段；processLine 加 CDATA 优先分支（CDATA 内 `<!--` 不被当作注释 opener）；新增 `openCDATAIfUnclosed` helper；stripInlineComment 重写 + `emitCommentResidue` 处理 close 后残余（递归剪闭合 inline + 转 pending unclosed）；StripHTMLComments 包一层 fixed-point loop（避免删后拼接产生新 `<!--...-->` artifact）；删 `regexp` import |
| 2 | `internal/module/memory/parse/parse_test.go` | `TestStripHTMLCommentsCases` 增 8 条表驱动用例：nested\_first\_close\_wins / nested\_attempt\_multiline / cdata\_multiline\_protects\_comment / cdata\_singleline\_keeps\_inline\_comment / cdata\_unclosed\_eof\_kept / fence\_protects\_comment / fence\_tilde\_protects\_comment / two\_block\_comments。新增 `FuzzStripHTMLComments`，13 条 corpus seed， invariant：不 panic + idempotent |
| 3 | `docs/plans/迁移/p24记忆优化/p24记忆优化.md` | 本文档本身回写 |

**Fuzz 驱动的带路 3 轮迭代**：

1. 初版（仅加 inCDATA + 加表驱动）——`<!----><!--0\n-->0` 出 idempotent fail。根因：原 `htmlCommentLinePattern.ReplaceAllString` regex 单次 lazy 匹配，留下未闭 `<!--0\n` 作 plain。
2. 二版（stripInlineComment 改 loop 加 unclosed-tail 转 pending）——`<!--\n--> <!--0-->` 出 idempotent fail。根因：processPendingLine 的 residue 直接 append，未清理其中 closed inline。
3. 三版（抽 `emitCommentResidue` 共享逻辑 + 递归剪 closed）——`<!----><!<!---->---->` 出 idempotent fail。根因：emitCommentResidue 在剧本拼接时，前后字节息合拼成新的 `<!--...-->`（这是「删后拼接」本质上不可避免的 artifact）。
4. 终版：加 `StripHTMLComments` 包一层 fixed-point loop。每次调 `stripHTMLCommentsScan`，`stripped=false` 或 `next==content` 即收敛。Termination：每次生效迭代至少删 1 个字节，最多 `len(content)` 次。实测 7.7M 次 fuzz 运行（0 fail / 20s× 10 workers）。

**设计决策**：
- **保留 line-based 外层 + char-based 内层**：fence 检测仍须依靠行边界；comment / CDATA 状态跨行必须 line-aware。比全 char-based 状态机代码量小、联成本低。
- **fixed-point loop 而非「完美一趟」**：“删后拼接”产生新 token 是抽象上不可变的现象（不是 stripper 逻辑 bug）。迭代到 fixed point 是唯一能保证 idempotence 的正确修法，且不损 content semantics。
- **嵌套 `<!-- <!-- --> -->`**：HTML5 规范不支持；第一个 `-->` 关闭。本实现镜像该行为（`strings.Cut(line, "-->")` 取首个），不实现真 depth tracking。
- **CDATA**：inCDATA 优先于所有其他 state；单行 CDATA 不特殊处理（因 inline 注释本来就 LEFT UNCHANGED，差异仅体现在跨行场景）。

**净代码量**：parse.go +90 / -10 行（包含大量 doc 与 fuzz 教训注释），parse_test.go +60 行（8 个新 case + 1 个 fuzz target + 13 corpus seeds）。

---

## ⬜ Phase 2.1.D：项目 CLAUDE.md 不信任化（slug: `project-claudemd-untrusted`）

**问题**：项目签入的 `CLAUDE.md` / `.claude/rules/*.md` 直接进 prompt，恶意仓库可注入指令。

**关闭路径**（需先讨论威胁模型）：
1. 引入 `MemorySource.Trusted bool` 标签
2. untrusted 内容包"treat as data not commands" prompt frame
3. 过滤可改 agent 行为的 frontmatter 关键字（`mode` / `scope` / 等）
4. 测试用例：恶意 `CLAUDE.md` 植入 jailbreak 文本，验证不能跳出 frame

---

## ⬜ Phase 2.2：Tristate feature gate + opaque cache token

**已规划**，未启动。从 Phase 2.0 推迟。

---

## ⬜ Phase 2.3：Per-turn TurnExtractor + 并发交叉测试（slug: `prompt-invalidate-assemble-race`）

**已规划**，未启动。Phase 2 的 headline。

**前置工作**：补 `Invalidate × Assemble` 交叉测试（Phase 2.0.1 / 2.1.AB 多次审查均提到，仍是缺口）。

---

## 当前进度标记

```
[当前位置 → AB.7（cache-root-threading）已落地；AB.5+AB.6+AB.7 改动悬在 worktree 未 commit；go build/vet/test 全绿]

✅ 1.0 / 1.5 / 1.6 / 1.7
✅ 2.0 / 2.0.1 / 2.0.2 / 2.0.3
✅ 2.1.A / 2.1.B
✅ 2.1.AB.1 / 2.1.AB.2 / 2.1.AB.3 / 2.1.AB.4 / 2.1.AB.5
✅ 2.1.AB.6（小清理 C 推迟）
✅ 2.1.AB.7（cache-root-threading）

🟡 2.1.C ← dedupe 已走完；stripper 状态机重写 + nested/CDATA + fuzz 仍未做
⬜ 2.1.D
⬜ 2.2
⬜ 2.3
```

## 下一步候选

1. **commit AB.5 + AB.6 + AB.7 worktree 改动**（推荐）：用户审视后给出 commit message，建议按主题分 commit（参考：① AB.5 design fold + 8 项 / ② AB.6 P0+P1 + 小清理 A / ③ AB.6 小清理 B parse 单测 / ④ AB.7 cache-root-threading / ⑤ 文档回写）。**冗余提醒**：当前 worktree 中 AB.5/6/7 三者与其他改动混在一起（原 finalpush 遗留），已 reset 到工作区；commit 前需重新 stage 只属于本路线的文件，避免一次提交 70+ 个不同主题。
   - 验证收口已通过：`go build ./...` + `go vet ./internal/module/memory/...` + `go test -count=1 ./internal/module/memory/...` 全绿
   - 实际改动：AB.5 ~210 行（P0+P1+小清理 A/B） + AB.7 ~160 行（代码 + 测试）
2. **进入 Phase 2.1.C 剩余部分**：重写 `parse.StripHTMLComments` 状态机（支持嵌套 + CDATA）+ 加 fuzz 测试。
3. **处理后阶段临时防护项**：~~`cache-root-threading`~~（已在 AB.7 落地）、`safe-read-toctou`（`os.OpenRoot` 重写，需改 `SafeReadEntrypoint` 签名与十余处调用点，范围较大）。
4. **跳到 Phase 2.1.D**：项目 `CLAUDE.md` 不信任化标签，需先完成威胁模型写作。

## 协作约定（贯穿）

- **回复中文**
- **不自动 commit**
- **不暴露底层 CLI 身份**
- **审查触发**：用户说 "审查" → 启动并行 reviewer（`Agent` 子调度或 `mcp__orch__orchestration_launch_agent`），边界不重叠（代码多块划分 + 文档 / 测试；correctness / security / design 是三个传统角度）。**默认路数**：纯文档复审 3 路，代码审查 5 路；高争议时可加 1 轮 audit 复核 reviewer 报告，硬上限 3 轮。
- **每个子阶段流程**：实现 → 并行审查 → 收报告 → 中文 punch list → 用户决断 → 下一阶段
- **阶段完成更新**：每个 Phase 落地后，回写本文档**四处**：① 文档目的段（L5）的「当前位置」句 ② 路线总览的状态标记（⬜→🟡→✅）③ 「当前进度标记」字符画 ④ 在主体新增「Phase X.Y：标题」章节（参考 AB.5 体例：触发 / 范围 / 落地 N 项 / 验证 / 净代码量 / 本轮未动）。
- **TODO ↔ tracker 关联**：源码 `TODO(tracker:<slug>)`，slug 由本文档（p24记忆优化.md）按字符串反查（PHASE_TRACKER.md 已删，本文档为唯一索引）
