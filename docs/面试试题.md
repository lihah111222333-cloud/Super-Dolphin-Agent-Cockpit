# super-agent-v3 工程师招聘专业笔试/机试题

> **说明：** 本试卷分为代码纠错、编程实战、架构设计与工程素养四个部分。旨在全面评估您的代码基本功、系统思考能力以及对现代工程规范的理解。

---

## 第一部分：代码纠错与基础 (代码能力) - 建议耗时 20 分钟

### 题目 1：Go 并发竞态问题 (Bug 找茬)
**背景：** 我们的调度器（Scheduler）需要在后台记录每个 Job 的最后执行状态。请阅读以下代码，指出其中的潜在问题，并给出修复方案的代码。

```go
package scheduler

import (
	"time"
)

type JobManager struct {
	jobStatus map[string]string
}

func NewJobManager() *JobManager {
	return &JobManager{
		jobStatus: make(map[string]string),
	}
}

func (jm *JobManager) RunJobs(jobIDs []string) {
	for _, id := range jobIDs {
		// 启动异步 Goroutine 执行任务
		go func() {
			// 模拟耗时任务
			time.Sleep(1 * time.Second)

			// 记录执行状态
			jm.jobStatus[id] = "DONE"
		}()
	}
}
```
**【请回答】：**
1. 代码中存在哪些严重 Bug？（至少指出 2 个）
2. 请直接写出修改后的正确代码（提示：考虑并发安全以及 Go 闭包变量捕获问题）。

### 题目 2：Vue 3 响应式陷阱
**背景：** 我们使用无构建的 ESM Vue3 架构。开发人员编写了如下组件代码，发现点击按钮时，控制台打印了新的 count，但页面上的数字没有更新。

```javascript
import { ref } from 'vue'

export default {
  props: {
    initialCount: Number
  },
  setup(props) {
    // 开发者为了方便，直接解构了 props
    let { initialCount } = props
    const count = ref(initialCount)

    const increment = () => {
      count.value++
      console.log("Current count:", count.value)
    }

    return { count, increment }
  },
  template: `
    <div>
      <span>{{ count }}</span>
      <button @click="increment">+1</button>
    </div>
  `
}
```
**【请回答】：**
1. 为什么页面上的数字不会随着点击更新？如果父组件传入的 `initialCount` 发生变化，这个组件能感知到吗？
2. 如何修正上述代码以保持响应式？

---

## 第二部分：编程实战 (代码能力) - 建议耗时 40 分钟

### 题目 3：协议解析提取器
**背景：** 我们的 AI Agent 返回的响应文本往往混杂了解释文字和 JSON 数据。我们需要精准提取出 Markdown 代码块中的 JSON 负载。

**需求：**
请使用 Go 语言实现一个函数 `ExtractToolJSON`。
- **输入:** 一个包含文本和代码块的字符串 `input`。
- **输出:** 提取到的 JSON 字节切片 `[]byte` 和可能产生的 `error`。
- **规则:** 找到包围在 ` ```json ` 与 ` ``` ` 之间的内容；如果存在多个块，只提取第一个；如果找不到，返回自定义错误 `ErrNoJSON`。

**【请编写代码】：**
```go
package parser

import (
    "errors"
)

var ErrNoJSON = errors.New("no json block found")

func ExtractToolJSON(input string) ([]byte, error) {
    // 请在此处填写你的实现
}
```

### 题目 4：依赖注入与生命周期 (Fx)
**背景：** 项目使用 `uber-go/fx` 作为依赖注入框架。我们需要一个简单的数据库连接模块。
**需求：**
请用 Go 代码编写一个 Provider 函数 `ProvideDB`：
1. 接收一个 `Config` 结构体作为参数（假设包含 `DSN string`）。
2. 返回一个 `*sql.DB` 实例。
3. 必须注入 `fx.Lifecycle`，在启动时 (`OnStart`) 测试数据库连接 (`db.PingContext`)，在停止时 (`OnStop`) 安全关闭数据库连接 (`db.Close()`)。

**【请编写代码】：**
```go
// 请填写 ProvideDB 函数实现
```

### 题目 5：数据库事务操作 (sqlc)
**背景：** 我们项目重度使用 `sqlc` 生成类型安全的数据库访问代码。假设 `sqlc` 生成了 `*Queries` 结构体，其中包含 `CreateUser(ctx, arg)` 和 `CreateUserProfile(ctx, arg)` 两个方法。
**需求：**
请用伪代码或 Go 代码演示：如何在一个严谨的数据库事务（Transaction）中，依次调用这两个方法？如果其中任何一个失败，如何确保整个事务回滚（Rollback）？如果发生 panic，事务又该如何安全退出？

**【请编写代码/伪代码】：**
```go
// 请简述或演示事务控制逻辑
```

---

## 第三部分：系统设计与调试 (思维能力) - 建议耗时 25 分钟

### 题目 6：10K 大文件读取导致 OOM 排查
**场景设定：**
昨天晚上，生产环境的 Agent 进程（Go 编写）在读取一个 2GB 的代码仓库日志文件（用于语义分析）时，进程发生 OOM（Out of Memory）并被系统 Kill。
开发人员查看了代码，发现读取逻辑使用了类似 `content, err := os.ReadFile(path)` 的方式。

**【请回答】：**
1. 作为紧急处理负责人，你会使用什么 Go 性能分析工具来获取现场证据？（列举至少 2 个具体工具或指标）
2. 在不增加硬件内存的前提下，你会如何重构这段文件读取和发送到 MCP Server 的逻辑？请简述你的优化思路（不用写完整代码）。

### 题目 7：Agent 任务的中断与恢复设计
**场景设定：**
Agent 正在执行一个长流程：`拉取代码 -> 编译 -> 运行测试 -> 汇总报告`。在这个过程中，用户点击了“强制停止”按钮。

**【请回答】：**
如果在执行到“编译”阶段时收到停止信号，如何设计底层的 Go `context.Context` 传播机制？你会在架构上引入什么设计，使得 Agent 下次启动时能直接从“运行测试”开始，而不需要重新拉取代码和编译？

### 题目 8：Agent 工具调用与自修复机制 (MCP 架构)
**场景设定：**
在基于 MCP (Model Context Protocol) 的架构中，LLM 会向我们的 Go 后端发送工具调用请求。假设 LLM 决定调用 `write_to_file` 工具，但在生成的 JSON 参数中，**漏掉**了必填参数 `TargetFile`。如果后端直接抛出 Error 并断开连接，会导致整个会话失败。

**【请回答】：**
作为后端架构师，你会在 Agent 的编排层（Orchestration）如何设计错误处理和“自修复（Self-Correction）”机制？如何安全、有效地引导大模型在下一轮对话中补全参数，而不是让系统崩溃？

---

## 第四部分：工程素养与规范 (团队能力) - 建议耗时 15 分钟

### 题目 9：关于原子提交 (Atomic Commits)
假设你在本地 `feature-ui` 分支上同时做了三件事：
1. 修复了首页按钮的 CSS 样式错位。
2. 添加了后端 `getUser` 的新 API。
3. 更新了项目根目录的 `README.md` 文档。
现在你运行了 `git add .` 和 `git commit -m "update code"`。

**【请回答】：**
这种提交方式违反了“原子提交”原则。如果是你，你会使用哪些 Git 命令来重组这次提交？请写出你期望的这 3 个 Commit Message 分别是什么样子的。

### 题目 10：代码契约与 Review 冲突
在 Code Review 中，你引入了一个全局变量 `var GlobalCache = make(map[string]interface{})` 来加速数据查询。
资深工程师拒批了你的 MR，指出：“我们项目后端必须是严格的 Stateless（无状态）架构，全局变量会导致横向扩展时的数据不一致。”

**【请回答】：**
作为候选人，如果你的确觉得这个查询太慢需要缓存，接下来你会如何与资深工程师沟通？你会提出什么技术替代方案既满足“无状态契约”，又能提升性能？
