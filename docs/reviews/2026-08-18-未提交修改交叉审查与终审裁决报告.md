# 未提交修改交叉审查与深度收敛治理报告 (2026-08-18)

## 概述

针对当前工作区所有修改与全仓代码架构，系统先后完成了：
1. **第一轮 10 Agent 全域交叉审查**（定位阻断缺陷）；
2. **第二轮 3 Agent 终审裁决与首批修复落地**（已完成 Git Commit 提交，未推送）；
3. **第三轮 3 Agent 全仓深度反碎片化（Deep Convergence）审查**。

经本轮深度收敛审查，精准锁定了全仓残存的 **80 余个微型碎片文件（Micro-Files）与畸形多重否定命名（如 `_nondarwin_nonlinux`）**，并制定了**各模块归一化聚合的终极收敛实施蓝图**。

---

## 一、首轮裁决缺陷修复与落地状态（已提交）

| 缺陷/治理项 | 严重度 | 修复方案与落地点 | 验证与当前状态 |
|:---|:---:|:---|:---:|
| **Windows 目录 `dir.Sync()` 100% 报错** | **P1** | 在 `internal/platform/securefs` 实现统一 `SyncDirectory(path)`，`appupdaterecovery` 与 `mcpcontrol` 直接复用。 | **已修复并提交** |
| **Sanitizer 元数据截断 CPU 100% 死循环** | **P0** | `deleteOneMetadataKey` 返回布尔值，无普通键可删时退出循环，增加无死循环单测。 | **已修复并提交** |
| **PID Registry Windows 权限判定失效** | **P1** | 权限判定下沉至平台实现（Windows 走 SID/DACL，Unix 走 POSIX 0o022），修复测试进程。 | **已修复并提交** |
| **LSP Formatter 0-based 坐标输出违规** | **P1** | 全部 11 个文本渲染函数统一调用 `format.FromLSP()` 转换为 1-based 坐标。 | **已修复并提交** |
| **LSP Manager 空 Root 隐式 `os.Getwd()` 兜底** | **P1** | 彻底移除兜底，空 Root 立即报错（Fail-Fast）。 | **已修复并提交** |
| **非 Windows `CheckExistingOwnerOnly` nil Panic** | **P1** | 补齐 `info == nil` 时的 `os.Lstat` 保护与脱敏报错。 | **已修复并提交** |
| **Refactor Harness 测试 CRLF 导致 Undefined** | **P1** | 标签剥离逻辑兼容 `\r\n\r\n`，修复跨平台测试编译。 | **已修复并提交** |
| **前端门禁 `appRoot` 解析与 CRLF 行宽偏置** | **P1/P2** | `critical-typecheck-guard` 基于模块物理路径定位；`frontend-code-size-guard` 归一化换行符。 | **已修复并提交** |
| **前端 `threadCopyPayload.js` 吞错与兜底** | **P2** | 移除 `try-catch` 吞错与 `codex` 默认 Provider 兜底。 | **已修复并提交** |
| **首批微型平台文件收敛** | **P2** | 清理收敛了 `config/sqlite_packaged_home_*`、`tools/atomic_write_parent_*`、`tools/factory_scope_*` 等 15+ 个微型碎片。 | **已修复并提交** |

---

## 二、第二阶段：全仓深度收敛（Deep Convergence）审查蓝图

针对全仓剩余的微型碎片文件、冗余测试夹具以及多重否定命名，3 个专项子 Agent 已完成全量审查与归一化方案设计，预计将**进一步净消除 52 个微型碎片文件**：

### 1. `cmd/mcp-lsp/installer/` & `multilsp/` 深度收敛（净消除 23 个碎片文件）

| 收敛目标 | 当前碎片文件清单 | 依赖与系统调用分析 | 最小化合并收敛方案 | 净消除数 |
| :--- | :--- | :--- | :--- | :---: |
| **Installer 生产微文件聚合** | `asset_file_type_windows.go` (17行)<br>`asset_file_type_nonwindows.go` (10行)<br>`install_binary_path_windows.go` (36行)<br>`install_binary_path_nonwindows.go` (31行)<br>`windows_catalog_production_branch_windows.go` (10行)<br>`windows_catalog_production_branch_nonwindows.go` (12行) | `asset_file_type` 依赖 Windows `syscall.Win32FileAttributeData`，其余依赖纯路径处理。 | 聚合为包级唯一样板对：<br>1. `installer/platform_windows.go`<br>2. `installer/platform_nonwindows.go` | **-4** |
| **Installer 测试辅助文件就地收敛** | `native_artifact_assert_*_test.go` (2个)<br>`native_artifact_symlink_*_test.go` (2个)<br>`process_failure_*_test.go` (2个) | 纯测试辅助桩，`assertExecutable` 等无需构建标签，使用 `if runtime.GOOS == "windows"` 分支。 | 消除独立测试文件，内联至同名的主测试套件（`native_artifact_test.go`, `native_artifact_symlink_test.go`, `process_failure_test.go`）。在 Windows 下补齐 `os.Stat` 校验（Fail-Fast）。 | **-6** |
| **Multilsp URI 路径逻辑统一** | `file_uri_path_windows.go` (48行)<br>`file_uri_path_nonwindows.go` (26行) | 纯 `net/url` 与 `path/filepath` 字符串处理，无任何 OS 专属系统调用。 | 合并为跨平台单文件 `multilsp/file_uri_path.go`，内部按 `runtime.GOOS == "windows"` 处理盘符。 | **-1** |
| **Multilsp 测试夹具深度收敛** | `document_bootstrap_*_test.go` (2个)<br>`idle_timeout_adapter_*_test.go` (2个)<br>`jsts_dependency_prepare_fixture_*_test.go` (2个)<br>`recycler_rss_limit_*_test.go` (2个)<br>`go_root_resolver_*_test.go` (2个)<br>`typescript_fallback_fixture_*_test.go` (2个) | 纯测试断言与脚本 Fixture 写入差异（`.cmd` vs `.sh`），无底层不可编译符号。 | 消除所有成对的 `*_windows_test.go` / `*_nonwindows_test.go`，直接收敛入主测试文件。 | **-12** |

---

### 2. `internal/platform/toolbridge/` & `schema/` 深度收敛（净消除 19 个碎片文件）

| 收敛目标 | 当前碎片文件清单 | 依赖与系统调用分析 | 最小化合并收敛方案 | 净消除数 |
| :--- | :--- | :--- | :--- | :---: |
| **`toolbridge` 生产与测试聚合** | 生产：`handler_acl_approval_*.go` (2个), `handler_schema_validation_*.go` (2个), `stdio_process_*.go` (2个), `schema_helper_platform_*.go` (2个)<br>测试：ACL/Schema/Stdio/Darwin 等 11 个零碎测试文件 | Windows 依赖 Job Object / HideWindow / Win32 ACL 5/1314；Non-Windows 依赖 `Setpgid` / `SIGKILL`；Darwin 依赖 Bundle 路径映射。 | 1. 生产代码收敛为 4 个标准文件：`platform_windows.go`, `platform_nonwindows.go`, `platform_darwin.go`, `platform_nondarwin.go`；<br>2. 测试代码聚合为 `platform_windows_test.go`, `platform_nonwindows_test.go`, `platform_darwin_test.go`, `platform_nondarwin_test.go`。 | **-10** |
| **`toolbridge/schema` 生产与测试聚合** | 生产：`filesystem_worker_permission_*.go` (2个), `helper_manifest_durability_*.go` (2个), `process_*.go` (2个)<br>测试：7 个零碎测试文件 | Windows 依赖 `FILE_FLAG_BACKUP_SEMANTICS` 目录 Flush、`CREATE_SUSPENDED` 与 `NtResumeProcess`；Non-Windows 依赖 Guardian lease。 | 1. 生产代码收敛为 `platform_windows.go` 与 `platform_nonwindows.go`；<br>2. 测试代码聚合为 `platform_windows_test.go` 与 `platform_nonwindows_test.go`。 | **-9** |

---

### 3. `internal/platform/` 系统层命名与纯逻辑收敛（净消除 10 个碎片文件）

| 收敛目标 | 当前碎片文件清单 | 依赖与系统调用分析 | 最小化合并收敛方案 | 净消除数 |
| :--- | :--- | :--- | :--- | :---: |
| **`appupdatefailure` 伪跨平台隔离重构** | `json_darwin.go` (135行)<br>`state_darwin.go` (53行)<br>`io_darwin.go` (238行)<br>`sidecar_stub_nondarwin.go` (23行)<br>`json_darwin_test.go` / `state_darwin_test.go` | `json_darwin.go` 与 `state_darwin.go` 是**纯 Go 数据结构与 JSON 编解码**，无任何 syscall 却加了 `//go:build darwin`，导致全平台无法执行单测。 | 1. 将 `record`/`state` 移至平台中立的 `sidecar.go`，全平台共享并跑通测试；<br>2. `io_darwin.go` 与 `sidecar_darwin.go` 合并为单一 `sidecar_darwin.go`；<br>3. `sidecar_stub_nondarwin.go` 重命名为规范名 `sidecar_other.go`。 | **-4** |
| **PIDRegistry 多重否定命名规范化** | `find_argument_unsupported_nondarwin_nonlinux.go`<br>`process_identity_nonlinux_nondarwin_nonwindows.go`<br>`cooperative_termination_darwin_linux_platform.go`<br>`cooperative_termination_unsupported_nondarwin_nonlinux.go`<br>`rollback_restart_endpoint_nondarwin_nonlinux_nonfreebsd.go` | 多重否定文件名（`_nondarwin_nonlinux` 等）极其冗长且违反 Go 命名规范。 | 1. 统一收敛重命名为标准的 `_unix.go`（针对 Linux/Darwin 通用实现）与 `_other.go`（针对不支持平台的 stub）；<br>2. 规范 build tag 注解。 | **-6** (畸形命名规范化) |

---

## 三、全仓收敛最终收益核算

- **全仓消除微型碎片文件总数**：**52 个文件**（累计第一轮共消除 **67+ 个碎片文件**）。
- **文件组织范式完全统一**：
  - 无系统调用依赖：统一使用 **单一平台中立文件 + `runtime.GOOS` 逻辑分发**；
  - 强系统调用依赖：统一收敛为 **包级唯一的 `platform_windows.go` / `platform_nonwindows.go`（或 `_unix.go` / `_other.go`）**；
  - 测试用例：**零散测试辅助桩与断言全部内联入主测试套件**。
- **构建门禁与维护认知大幅简化**，彻底消除“每个 5 行函数建一对文件”的历史债务。

---

## 四、二次交叉裁决后的修复闭环（2026-08-18）

本节记录两名 GPT-5.6 Terra 高思考代理交叉裁决后，由 Luna Max 窄任务卡在共享主工作树完成的 RED → Fix → GREEN 证据。未回退远程的七工具入参/出参契约，未提交或暂存文件。

| 裁决项 | RED / 根因 | 修复与防回归 | 当前状态 |
| :--- | :--- | :--- | :---: |
| `cmd/mcp-lsp/tools` 测试基线不能编译 | `formatter_test.go` 缺少 `protocol` import；通用测试与 non-Windows fixture 重复 `structureShellQuote` | 恢复 import；通用 helper 委托平台 fixture；Windows、Linux、Darwin 编译通过 | GREEN |
| Windows `SyncDirectory` 假成功 | Windows 仅 `Stat` 后返回；Win32 5/1314 还是普通错误 | Windows 使用 `CreateFile(FILE_FLAG_BACKUP_SEMANTICS)` → `FlushFileBuffers` → `CloseHandle`；完整保留 flush/close 错误链，并通过 `WrapErrorForPath` 提升为 `WindowsPermissionError`，供 ACL 授权弹窗识别 | GREEN |
| `CanonicalPath` 跨平台测试 | Windows 下 `\\var\\run\\stage` 不是卷限定绝对路径 | 测试改为 `filepath.Join(t.TempDir(), "stage")`，未放宽生产校验 | GREEN |
| Windows stdio MCP 子进程逃逸窗 | `Start` 后才 attach Job，create/open/assign 失败只告警并继续 | 预建 Job，使用 `CREATE_SUSPENDED | CREATE_NO_WINDOW | CREATE_NEW_PROCESS_GROUP`，Start → Open → Assign → `NtResumeProcess`；任一步失败均 kill/wait/close 并返回错误；真实 E2E 连跑 3 次回收孙进程 | GREEN |
| LSP action 不兼容字段被静默忽略 | `handleToolCall` 未执行 manifest schema | `ToolManifest.Schema` 作为 SSOT，组装时用 `jsonschema/v6` 一次编译；handler 前统一验证；7 工具真实 `tools/call` 均返回 content-only `invalid_params`，无 `structuredContent` | GREEN |
| root 环境 cache fail-fast 测试被跳过 | `geteuid()==0` 时 `t.Skip`，chmod 对 root 不构成失败 | 删除缓存目录并在同路径创建普通文件，稳定触发 `ENOTDIR`；WSL root、WSL nobody、Windows 均实际执行 | GREEN |
| Windows symlink 1314 证明空洞 | 普通测试遇 1314 静默 return，导致假 PASS | 普通测试改为显式 `t.Skipf`；新增 `windows && e2e` 强制正向测试，真实 `InstallArtifact` 并断言 `os.Readlink()=="native-lsp"`，权限不足必须失败 | 需要特权 CI |
| CRLF / whitespace 门禁 | `git diff --check` 曾报告 13,416 行、29 文件 | 仅对命中文件执行 CRLF → LF，逐文件 normalized SHA256 相等；另清理 6 个 EOF 多余空行 | GREEN |

统一验证结果：

- `git diff --check`：零输出。
- Windows：securefs、appupdatefailure、toolbridge、installer、multilsp、tools 与 7 工具 schema 聚焦测试通过。
- 交叉编译：Linux amd64、Darwin amd64 的 `mcp-lsp` / `tools` / `toolbridge` 通过；Windows arm64 的 `mcp-lsp` / `toolbridge` 通过。
- LSP diagnostics：本轮 16 个关键源码和测试文件均为 `OK total=0`。
- Windows Job 生命周期 E2E：`TestStdioWindowsJobKillsDescendantsOnClientClose` 连跑 3 次通过。
- Windows symlink 强制 E2E：当前机器因 typed Win32 1314 明确失败；该失败不是回归绿灯，必须在启用 Developer Mode 或持有 `SeCreateSymbolicLinkPrivilege` 的 Windows CI lane 再取得正向 GREEN。
- 下载策略：发现某 E2E 在 staging 中重复执行 `go install gopls@v0.23.0` 后已终止；本轮未把该测试标为 PASS。已有本地资产时不得重复下载。

非阻断 residual：正常关闭 stdio transport 时会记录 `read |0: file already closed` WARN；进程树回收与测试结果均正确，但后续可单独降低正常关闭路径的日志噪声。

## 五、残余红测收口与平台隔离复核（2026-08-18）

本节是对前述历史收敛蓝图的最终覆盖性裁决：凡行为、fixture 或系统命令存在平台差异，必须使用名称一眼可辨的 `*_windows*` / `*_nonwindows*` 文件并带 `//go:build`；不得为了减少文件数量改成公共测试内的 `runtime.GOOS` 分支。前文关于平台测试可内联的规划不再作为实现依据。

| 红测组 | 根因 | 修复 | GREEN 证据 |
| :--- | :--- | :--- | :--- |
| `cmd/mcp-lsp` 3 项 | 远程契约已将 Vue/TS companion 资源租约冻结为 RSS 2560 MiB、Node heap 2048 MiB；LSP 对外位置为 1-based；纯文本行协议会转义 Windows 反斜杠，旧断言仍使用旧值 | 仅更新过期测试断言，未回退资源、1-based 或 content-only 契约 | 三项精确测试通过；Windows/Linux/Darwin `go test -c` 通过 |
| `tools` 4 项 | WSL 路径误用 `filepath.Clean`；原子写测试仍引用已收敛文件名；TSV fixture 未规范化 Windows 路径；slow deadline 断言错误地在 Windows 冷安装策略下执行 | WSL 路径用 `path.Clean`；原子目录同步按平台落入现有平台文件；TSV 路径规范化；deadline 行为由 `!windows` 与 `windows` 两个 build-tag 测试分别证明 | `go test ./cmd/mcp-lsp/tools -count=1` 通过；Linux/Darwin 编译通过；相关 diagnostics 全零 |
| `multilsp` 3 项 | dirty 显式编辑器快照被错误送入磁盘 bootstrap；重开 diagnostics 后重复推进 epoch 丢弃同步 didOpen publish；通用测试硬编码 `sh` 导致 Windows 失败 | 已绑定 dirty 文档直接复用编辑器快照；移除重复 epoch 推进；transport fixture 拆为 `transport_termination_retry_windows_test.go` 与 `_nonwindows_test.go`；atomic replacement 断言也拆为带 build tag 的平台文件 | 三项精确测试通过；`go test ./cmd/mcp-lsp/multilsp -count=1` 41.921s 通过；`GOPROXY=off` 下 Windows/Linux/Darwin amd64 编译通过 |
| 远程更新再次带入 CRLF | 7 个文件重新触发约 1,480 行 whitespace 报告 | 仅做 CRLF → LF；六个 `cmd/mcp-lsp` 文件由 Luna Max 窄卡计算 normalized SHA256，一致后放行；单个 `scripts` 文件按字节归一化 | 最终 `git diff --check` 零输出，`gofmt -d` 无输出 |

主 agent 独立复验：

- `bootstrapDocumentOpenOnly` 的 LSP 定位、definition、4 个 references、精确函数读取均成功；生产文件及 Windows/non-Windows transport fixture 的 `file(diagnostics)` 均为 `OK total=0`。
- `go test ./cmd/mcp-lsp/tools -count=1 -timeout=180s`：PASS（30.680s）。
- `multilsp` dirty snapshot、同步 diagnostics、transport exit、atomic replacement 精确集合：PASS。
- `cmd/mcp-lsp` 资源租约、1-based inspect、纯文本转义及 schema invalid-params 精确集合：PASS。
- 最终 `git diff --check`：零输出。

仍未伪造为 GREEN 的唯一强制 E2E 阻断是 Windows symlink 权限：当前机器返回 typed Win32 1314。需要由产品 ACL 授权链弹窗申请权限，或在启用 Developer Mode / `SeCreateSymbolicLinkPrivilege` 的正式 Windows lane 运行；不能以 skip、fallback 或放宽安全校验消除。

### LSP 异常复现记录

1. 路径写错导致 `grep(text_search)` 返回 `file_not_found`：

   ```json
   {"action":"text_search","query":"escape","paths":["internal/lineprotocol","cmd/mcp-lsp/tools"],"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin","max_results":100}
   ```

   原始结果：`ERROR code=file_not_found`，目标 `internal/lineprotocol` 不存在。收窄重试：

   ```json
   {"action":"text_search","query":"func Escape","paths":["internal/mcpserver/common/lineprotocol"],"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin","max_results":100}
   ```

   结果：`OK total=1`，定位 `protocol.go:44`。

2. `tool_grep.go` call hierarchy 在非函数位置失败：

   ```json
   {"action":"call_hierarchy","pos":"cmd/mcp-lsp/tools/tool_grep.go:257:6","direction":"incoming","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   原始结果：`ERROR code=tool_error retryable=0`，`textDocument/prepareCallHierarchy: json-rpc error 0: data is not a function`。收窄重试：

   ```json
   {"action":"call_hierarchy","pos":"cmd/mcp-lsp/tools/tool_grep.go:256:2","direction":"incoming","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   结果仍为 `ERROR code=identifier_not_found retryable=0`；该符号随后以 references、grep、inspect 与 file 取得影响面证据，未把 call hierarchy 失败写成 PASS。

3. `transport.wait` call hierarchy 把方法误判为非函数。原始与收窄输入：

   ```json
   {"action":"call_hierarchy","pos":"cmd/mcp-lsp/multilsp/transport_conn.go:454:17","direction":"incoming","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"call_hierarchy","pos":"cmd/mcp-lsp/multilsp/transport_conn.go:454:17","direction":"outgoing","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"references","pos":"cmd/mcp-lsp/multilsp/transport.go:76:6","max_results":5,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"call_hierarchy","pos":"cmd/mcp-lsp/multilsp/transport.go:76:6","direction":"incoming","max_results":5,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"call_hierarchy","pos":"cmd/mcp-lsp/multilsp/transport.go:76:6","direction":"outgoing","max_results":5,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   两个位置的 incoming/outgoing 均返回 `ERROR code=tool_error retryable=0`，消息为 `textDocument/prepareCallHierarchy: json-rpc error 0: transport is not a function`；中间的 references 成功，`OK total=93 showing=5 truncated=1`。因此 call hierarchy 保留为 LSP blocker，不写成 PASS。

4. 两次把搜索词误填入 `grep.action`，封闭 schema 正确拒绝。原始与修正输入：

   ```json
   {"action":"documentDiagnosticEpoch","paths":["cmd/mcp-lsp/multilsp"],"glob":"*_test.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"text_search","query":"documentDiagnosticEpoch","paths":["cmd/mcp-lsp/multilsp"],"glob":"*_test.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"ReopenDocumentForDiagnostics","paths":["cmd/mcp-lsp/multilsp/lifecycle_lease_test.go"],"glob":"*.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"text_search","query":"ReopenDocumentForDiagnostics","paths":["cmd/mcp-lsp/multilsp/lifecycle_lease_test.go"],"glob":"*.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   原始两次均返回 `unsupported grep action ... (valid actions: ast_search, text_search)`；修正后分别为 `OK total=0` 与 `OK total=1`。这是调用错误，不是 schema 回归。

5. `patch_edit(replace_range)` 因 patch 文本把 `tr` 误写为 `t`、转义层级不一致而返回 `patch_no_match`。可稳定复现的最小输入：

   ```json
   {"action":"replace_range","file_path":"cmd/mcp-lsp/multilsp/transport_termination_retry_test.go","patch":"-func definitelyMissingTransportFixture() {}\n+func replacement() {}","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   结果：`ERROR code=patch_no_match retryable=0`，提示先 `file(read_file)` 重读。实际收窄读取使用：

   ```json
   {"action":"read_file","pos":"cmd/mcp-lsp/multilsp/transport_termination_retry_test.go:15","limit":70,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"read_file","pos":"cmd/mcp-lsp/multilsp/transport_termination_retry_test.go:22","limit":65,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"read_file","pos":"cmd/mcp-lsp/multilsp/document_bootstrap_test.go:518:6","limit":25,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   重读后用精确上下文重试得到 `OK ... status=applied persisted=1 lsp_sync=1`。该项是 patch 调用文本错误，不是 LSP 服务端错误。

6. `patch_edit(format)` 对一个 non-Windows 测试文件连续两次超时：

   ```json
   {"action":"format","file_path":"cmd/mcp-lsp/multilsp/test_permissions_nonwindows_test.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   两次原始结果均为 `ERROR code=lsp_timeout retryable=1`、`textDocument/formatting ... context deadline exceeded`。收窄诊断：

   ```json
   {"action":"diagnostics","file_path":"cmd/mcp-lsp/multilsp/test_permissions_nonwindows_test.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   结果 `OK total=0`。format 本身仍为可复现 blocker，未以 diagnostics 成功替代 format PASS。

## 六、平台行为编译隔离与主代理复验（2026-08-18）

本轮按“平台专用行为必须由 build tag 选择，文件名必须一眼可见平台”的裁决继续收敛。公共代码仍允许把 `runtime.GOOS` 当作环境数据或错误文本，但不允许据此选择不同平台行为。

| 范围 | 收敛结果 | GREEN 证据 |
| :--- | :--- | :--- |
| `cmd/mcp-lsp/tools` | `normalizePlatformWorkDir` 拆为 `platform_linux.go`、`platform_other.go`、`platform_windows.go`；Linux 独占 WSL 路径转换，其他平台保留原值，Windows 同时承载冷安装总 deadline 策略；每个公开/包内 helper 都有说明责任边界的中文注释 | LSP grep/definition/references/read/diagnostics 全链通过；三个文件 diagnostics 均 `OK total=0`；`go test ./cmd/mcp-lsp/tools -count=1 -timeout=180s` 通过（29.926s） |
| `cmd/mcp-lsp/installer` | 平台断言、进程失败 fixture、dotnet home 与 symlink 行为分别落在 `platform_windows_test.go` / `platform_nonwindows_test.go`；强制 symlink 证明保留 `windows && e2e` | `GOPROXY=off go test ./cmd/mcp-lsp/installer -count=1 -timeout=180s` 通过（8.467s）；`-tags=e2e` 仅编译通过，未下载 |
| `cmd/mcp-lsp/multilsp` | URI、shell fixture、atomic replacement、Win122 bootstrap 恢复与 TypeScript module 搜索均由清晰的平台文件和 build tag 选择；公共 manager/fallback 不再用 `runtime.GOOS` 选择行为 | `GOPROXY=off go test ./cmd/mcp-lsp/multilsp -count=1 -timeout=180s` 通过（约 71s）；相关平台文件 diagnostics 全零 |
| 跨平台编译 | 对 tools、installer、multilsp 分别编译 Windows ARM64、Linux AMD64、Darwin AMD64 | 9/9 项 `GOPROXY=off go test -c` 全部 exit 0 |
| 主入口与平台安全 | 复验七工具 handler 契约、Vue/TS 租约、inspect 位置、模糊 patch 错误、CanonicalPath、目录 flush、stdio 生命周期 | `cmd/mcp-lsp` 精确集合通过；securefs、appupdatefailure、toolbridge 定向集合通过 |

最终 `rg runtime.GOOS` 仅剩以下允许项：adapter/recycler 把 GOOS 当作 LSP 环境或显式算法输入，测试核对环境值，unsupported-platform 错误打印平台名，以及防回归 guard 自身。未发现公共实现继续通过 `runtime.GOOS` 选择平台行为。`git diff --check` 在本节复验前为零输出；最终交付前仍须再次执行。

### 本轮新增 LSP 异常复现

7. Windows URI helper 使用重构前旧行号，definition 与 references 都被封闭位置校验拒绝：

   ```json
   {"action":"definition","pos":"cmd/mcp-lsp/multilsp/file_uri_path_windows.go:39:6","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"references","pos":"cmd/mcp-lsp/multilsp/file_uri_path_windows.go:39:6","include_declaration":true,"max_results":50,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   两次均返回 `ERROR code=position_out_of_range retryable=0`，第 39 行最大列为 3。收窄调用：

   ```json
   {"action":"document_symbol","file_path":"cmd/mcp-lsp/multilsp/file_uri_path_windows.go","max_results":50,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   定位 `platformFileURIFromPath` 为 `34:6`；以该位置重试 definition 成功，references 为 `OK total=2`。

8. build-tag helper 移动后，gopls/LSP 缓存仍引用已删除的 `bootstrap_recovery_windows.go`。稳定复现：

   ```json
   {"action":"definition","pos":"cmd/mcp-lsp/multilsp/manager_document_lifecycle.go:40:8","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"references","pos":"cmd/mcp-lsp/multilsp/manager_document_lifecycle.go:40:8","include_declaration":true,"max_results":100,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"references","pos":"cmd/mcp-lsp/multilsp/startup_retry_windows.go:141:6","include_declaration":true,"max_results":100,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   前两项错误地返回已删除路径；第三项返回 `ERROR code=tool_error retryable=0`、`no object found for \"shouldRecoverBootstrapDidOpenWin122\"`。对最终 Windows/non-Windows 文件执行 open_file 后问题仍存在；但 document_symbol、read_file、diagnostics 与三平台编译均确认最终定义有效。该缓存/index 问题保留为 LSP blocker。

9. tools 平台拆分时，第一次 patch 使用了过期上下文：

   ```json
   {"action":"replace_range","file_path":"cmd/mcp-lsp/tools/factory.go","patch":" import (\\n \\t\\\"context\\\"\\n \\t\\\"encoding/json\\\"\\n \\t\\\"errors\\\"\\n \\t\\\"path\\\"\\n- \\t\\\"runtime\\\"\\n \\t\\\"strings\\\"\\n \\t\\\"time\\\"\\n@@\\n-// normalizePlatformWorkDir 把 Windows 主机传给 WSL sidecar 的绝对路径转换成挂载路径（Linux 下执行）。\\n-func normalizePlatformWorkDir(workDir string) string {\\n-\\tif runtime.GOOS != \\\"linux\\\" {\\n-\\t\\treturn workDir\\n-\\t}\\n-\\treturn normalizeWSLWorkDir(workDir)\\n-}\\n","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   返回 `ERROR code=patch_no_match retryable=0`、`hunk 2: sequence not found`；按提示重读 `factory.go` 精确窗口后重试成功。

10. 对尚未创建的平台文件直接执行 replace_range：

   ```json
   {"action":"replace_range","file_path":"cmd/mcp-lsp/tools/platform_windows.go","patch":"+//go:build windows\\n+\\n+package tools\\n+\\n+// normalizePlatformWorkDir 保留 Windows sidecar 使用的主机绝对路径。\\n+func normalizePlatformWorkDir(workDir string) string {\\n+\\treturn workDir\\n+}\\n","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   返回 `ERROR code=file_not_found retryable=0`、`GetFileAttributesEx ... platform_windows.go: The system cannot find the file specified`。改用文件创建 patch 后成功，随后读取、格式化与 diagnostics 均通过。

11. 主代理并发发起 7 个 LSP 调用（同一 `platform_linux.go` 的 document_symbol、definition、references、read_file、diagnostics，以及 `platform_other.go`、`platform_windows.go` diagnostics）时，批次超过 120 秒没有任何单项响应。由于调用容器没有返回内部单项结果，不能归因到某一个 action。终止批次后逐项使用以下收窄入参重试，全部成功：

   ```json
   {"action":"document_symbol","file_path":"cmd/mcp-lsp/tools/platform_linux.go","max_results":20,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"definition","pos":"cmd/mcp-lsp/tools/platform_linux.go:6:6","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"references","pos":"cmd/mcp-lsp/tools/platform_linux.go:6:6","include_declaration":true,"max_results":20,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"read_file","pos":"cmd/mcp-lsp/tools/platform_linux.go:1","limit":20,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   {"action":"diagnostics","file_path":"cmd/mcp-lsp/tools/platform_linux.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
   ```

   逐项结果分别为 symbol 1、definition 1、references 2、读取 8 行、diagnostics 0；另外两个平台文件 diagnostics 也为 0。批量并发挂起仍记录为 LSP 编排层 blocker。

12. 交叉编译验证 agent 第一次启动时，LSP MCP 初始化握手在 30 秒超时。复现调度参数：`spawn_agent(task_name=\"verify_crossplatform_compile\", model=\"gpt-5.6-luna\", reasoning_effort=\"max\", fork_turns=\"none\")`。使用相同 Luna Max 窄卡、不同任务名重试后启动成功并完成 9/9 编译；这不是源码测试失败。

## 七、最终E2E与生命周期闭环

### 7.1 42 语言矩阵 probe

最终矩阵以 `GOPROXY=off` 运行 42 种语言 × 7 个工具/小功能命令，全部 PASS，总耗时 `413.263s`。该结果是离线、可复现的 E2E probe，不与后述 15 分钟生命周期 soak 混记。

矩阵收口过程中记录并修复了 6 条 MQL RED 链：

1. 遗漏 `language_id` 时返回 `unsupported`；补入 `language_id` 后，`diagnostics` 为空。
2. 增强 journal 证明确有 pull，闭合资源拉取证据链。
3. Windows URI 的字符串大小写/转义身份比较是根因，改为正确的身份比较后恢复。
4. 平台 tag helper 修复后，平台专用行为回到对应构建分支。
5. workspace symbol 使用 canonical `cpp`，消除语言别名导致的定位偏差。
6. 6 个别名与 42 种语言全绿。

### 7.2 strict Windows 生命周期 soak

strict Windows 15m 首次运行为 RED：line 62 的 `close` 后，RSS observation 返回 `connect refused`，测试运行 `247.020s` 后由 `Ctrl+C` 终止。根因是 broker 未调用 `CREATE_BREAKAWAY_FROM_JOB`，导致首个 sidecar 被 `KILL_ON_CLOSE` job 一并杀死。

增加 Windows-only conditional breakaway 后，先得到短绿 `28.607s`；随后重跑正式 strict Windows 15m 为 PASS，idle 时长 `15m0.0413726s`，总耗时 `927.153s`。这里的首 RED、短绿和正式 PASS 分别保留为 probe/soak 的生命周期证据，不以短绿替代正式 soak。

### 7.3 bundle、资源 cohort 与最终包

- bundle manifest 固化 42 个 ID 以及 SHA、version、profile、schema；generic native Windows writer 已纳入，且本地包已存在时不下载。
- resource cohort 的平台碎片已收敛；最终以 `GOPROXY=off go test -c -tags=e2e` 复验 Windows arm64、Linux amd64、Darwin amd64，3/3 均为 GREEN，产物使用一眼可辨平台的文件名。
- final 包测试 `tools`、`installer`、`multilsp`、`common`、`toolbridge` 均 GREEN；LSP diagnostics 为零；`git diff --check` GREEN。

### 7.4 本轮新增 LSP 错误记录

本轮仍保留以下可精确复现的 LSP 错误，不将其误判为源码测试失败：

- `grep` 传入非法 `path` 字段：

  ```json
  {"action":"text_search","query":"func hasDiagnosticsFile","path":"cmd/mcp-lsp/lsp_binary_multilang_diagnostics_windows_linux_darwin_e2e_test.go","work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin","max_results":20}
  ```

  返回 `ERROR code=invalid_params retryable=0`、`json: unknown field \"path\"`；改用 `paths` 后成功。
- 主 agent 的 `xref` 位置超出行尾：

  ```json
  {"action":"references","pos":"cmd/mcp-lsp/runtime_gopls_root_resource_windows.go:119:6","include_declaration":true,"language_id":"go","max_results":30,"work_dir":"C:\\Users\\mima0000\\Desktop\\wj\\super-dolphin"}
  ```

  返回 `ERROR code=position_out_of_range retryable=0`、`column 6 is beyond end of line 119, max column is 3`；收窄到函数标识符 `:116:6` 后成功。
- Luna Max 窄卡首次启动时 LSP MCP 握手超时；复现调度为 `spawn_agent(task_name="fix_windows_15m_shared_daemon_startup_race", model="gpt-5.6-luna", reasoning_effort="max", fork_turns="none")`，返回 `required MCP servers failed to initialize: lsp: timed out handshaking with MCP server after 29.999999667s`；同一窄卡重试后成功。

以上错误与已完成的诊断零、矩阵 probe、平台编译证据及 strict Windows 生命周期 soak 分开记账，最终裁决以正式 PASS 证据为准。
