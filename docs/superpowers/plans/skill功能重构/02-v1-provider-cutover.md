# V1 Provider Cutover Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cut the old skill injection/runtime dependency path and make Claude/Codex rely on provider-native mirrors generated by V1 foundation.

**Architecture:** Provider startup asks the skill module to reconcile mirrors before launch, then launches the provider with native discovery paths. Codex no longer receives a Super-Dolphin skill manifest prepend or `skill_read_section`; Claude no longer gets `.claude/skills` symlinked to `~/.super-dolphin/skills-cache`.

**2026-05-18 direction correction:** Super-Dolphin does not bundle Claude/Codex CLI for V1. Users install and log in to providers themselves. Default mirrors are project `<repo>/.claude/skills`, `<repo>/.agents/skills`, and personal `~/.claude/skills`, `~/.agents/skills`. Codex identity/config still uses user `~/.codex`; that identity directory is not a skill mirror. Explicit provider homes remain supported as advanced configuration only.

**Tech Stack:** Go 1.25.7, Fx optional ports, provider driver unit tests, toolbridge host tool tests, README and `.gitignore` docs updates.

---

## File Structure

- Modify: `internal/provider/codexapp/driver.go`
- Modify: `internal/provider/codexapp/module.go`
- Modify: `internal/provider/codexapp/driver_test.go`
- Modify: `internal/provider/codexapp/pool_spawn_cmd.go`
- Create: `internal/provider/codexapp/binary_resolver.go`
- Test: `internal/provider/codexapp/binary_resolver_test.go`
- Modify: `internal/provider/claudecli/driver.go`
- Modify: `internal/provider/claudecli/module.go`
- Modify: `internal/provider/claudecli/driver_test.go`
- Modify: `internal/provider/claudecli/config.go`
- Create: `internal/provider/claudecli/binary_resolver.go`
- Test: `internal/provider/claudecli/binary_resolver_test.go`
- Modify: `internal/provider/claudecli/transport.go`
- Modify: `internal/provider/claudecli/transport_config.go`
- Modify: `internal/provider/claudecli/history.go`
- Modify: `internal/provider/claudecli/context_window.go`
- Modify: `internal/util/historyjsonl/history.go`
- Create: `internal/provider/claudecli/provider_home.go`
- Create: `internal/provider/claudecli/provider_home_test.go`
- Modify: `internal/module/thread/lifecycle_helpers.go`
- Modify: `internal/module/thread/lifecycle_helpers_test.go`
- Modify: `internal/module/cliadapter/symlink.go`
- Modify: `internal/module/cliadapter/symlink_test.go`
- Modify: `internal/module/cliadapter/symlink_posix_test.go`
- Modify: `internal/module/prompt/module.go`
- Modify: `internal/module/prompt/assembler_support.go`
- Modify: `internal/module/uistate/config_rpc.go`
- Modify: `internal/module/uistate/builtin_tools.go`
- Modify: `cmd/agent-terminal/frontend/vue-app/stores/builtin-tool-policy.js`
- Modify: `cmd/agent-terminal/frontend/vue-app/stores/thread-actions-helpers.js`
- Modify or delete old modules/tests under `internal/module/skilllibrary`, `internal/module/skillforge`, and `internal/module/fbsd` after production consumers are migrated
- Modify: `internal/platform/toolbridge/host_tools.go`
- Modify: `internal/platform/toolbridge/module.go`
- Delete: legacy `skill_read_section` implementation file
- Modify: `internal/platform/toolbridge/host_tools_test.go`
- Delete or rewrite: `internal/platform/toolbridge/skill_read_section_test.go`
- Modify: `internal/app/modules.go`
- Modify: `internal/app/toolbridge_adapters.go`
- Modify: `internal/contract/skill.go`
- Modify: `internal/contract/toolbridge.go`
- Modify: `README.md`
- Modify: `.gitignore`

This plan is a full cutover. Remove old provider-runtime dependencies on `skilllibrary`, `skillforge`, and `fbsd` in this implementation slice. Existing production consumers in prompt assembly, uistate config/builtin-tools, app module wiring, and old candidate review surfaces must be refactored to the new canonical/proposal services before claiming the old runtime path is gone. Do not leave any old cache/library injection path callable at runtime.

## Task 0: External Provider CLI Preflight

**Files:**
- Modify: provider startup/config validation files after locating current CLI launch paths
- Modify: `internal/provider/claudecli/config.go`
- Modify: `internal/provider/codexapp/pool_spawn_cmd.go`
- Test: affected Claude/Codex provider startup tests

- [ ] **Step 1: Write installed CLI preflight tests**

Cover:

- missing `claude` / `codex` CLI returns a clear setup error
- missing provider login/config returns a clear provider setup error where the provider exposes one
- explicit advanced binary override still wins only when deliberately provided
- driver-level smoke records whether it used a real installed CLI or a fake test double

- [ ] **Step 2: Preserve existing CLI launch path**

Do not add bundled binary discovery in V1. The app manages skills, not provider installation. Keep diagnostics explicit so users know they need to install and log in to Claude/Codex themselves.

- [ ] **Step 3: Run focused tests**

Run:

```bash
./scripts/test_with_guard.sh ./internal/provider/claudecli ./internal/provider/codexapp -run 'Test.*Binary|Test.*Spawn|Test.*CLI|Test.*Setup' -count=1
```

Expected: provider launch paths either use the installed CLI or fail with a clear setup error; no old skill injection path is used as a fallback.

## Task 1: Codex Prompt Injection Removal

**Files:**
- Modify: `internal/provider/codexapp/driver.go`
- Modify: `internal/provider/codexapp/module.go`
- Modify: `internal/provider/codexapp/driver_test.go`
- Modify: `internal/app/modules.go`
- Modify: `internal/contract/skill.go`

- [ ] **Step 1: Write failing test**

Add a test that constructs a Codex driver with a fake manifest renderer returning `"SKILL MANIFEST"` and verifies `startAssemblyInstructions` does not prepend it.

Expected assertion:

```go
if strings.Contains(base, "SKILL MANIFEST") {
	t.Fatalf("Codex base instructions still include skill manifest: %q", base)
}
```

- [ ] **Step 2: Remove `SkillManifestRenderer` from Codex driver**

Remove `manifestRenderer` fields, constructor parameters, and Fx injection from Codex provider. `startAssemblyInstructions` should use only request instructions and fallback base instructions.

- [ ] **Step 3: Remove app-level provider**

Remove `provideSkillManifestRenderer` from `internal/app/modules.go` if no other production consumer remains. If `fbsd.ManifestRenderer` becomes unused, leave a compile-proven cleanup note in the commit body and delete it in the same commit only when affected tests stay focused.

- [ ] **Step 4: Run Codex tests**

Run:

```bash
./scripts/test_with_guard.sh ./internal/provider/codexapp ./internal/app -run 'Test.*Assembly|Test.*Manifest|Test.*Fx' -count=1
```

Expected: Codex tests pass and `rg "RenderSkillManifest|SkillManifestRenderer" internal/provider/codexapp internal/app` returns no provider runtime usage.

## Task 2: Remove Codex `skill_read_section` Runtime Dependency

**Files:**
- Modify: `internal/platform/toolbridge/host_tools.go`
- Modify: `internal/platform/toolbridge/module.go`
- Delete: legacy `skill_read_section` implementation file
- Modify: `internal/platform/toolbridge/host_tools_test.go`
- Delete or rewrite: `internal/platform/toolbridge/skill_read_section_test.go`
- Modify: `internal/app/toolbridge_adapters.go`
- Modify: `internal/contract/toolbridge.go`

- [ ] **Step 1: Write failing list-tools test**

Change the host tool tests so `ListToolsForCodex` with the production registry does not include `skill_read_section`, while memory read/write host tools still appear when enabled.

- [ ] **Step 2: Remove registry wiring**

Remove `NewSkillReadSectionRegistry` from `provideHostToolRegistry`. Remove `SkillReadSectionTool` construction from `internal/app/toolbridge_adapters.go`.

- [ ] **Step 3: Remove contract ports**

Delete `SkillSectionReader` and `SkillCallRecorder` from `internal/contract/toolbridge.go` once no references remain.

- [ ] **Step 4: Run toolbridge tests**

Run:

```bash
./scripts/test_with_guard.sh ./internal/platform/toolbridge ./internal/app -run 'Test.*HostTool|Test.*ListTools|Test.*RouteTool' -count=1
```

Expected: no dynamic tool named `skill_read_section` is exposed to Codex.

## Task 3: Claude Startup Mirror Reconcile

**Files:**
- Modify: `internal/provider/claudecli/driver.go`
- Modify: `internal/provider/claudecli/module.go`
- Modify: `internal/provider/claudecli/driver_test.go`
- Modify: `internal/provider/claudecli/transport.go`
- Modify: `internal/provider/claudecli/transport_config.go`
- Modify: `internal/provider/claudecli/history.go`
- Create: `internal/provider/claudecli/provider_home.go`
- Test: `internal/provider/claudecli/provider_home_test.go`
- Modify: `internal/contract/skill.go`

- [ ] **Step 1: Write failing startup test**

Inject a fake `contract.SkillMirrorReconciler` and assert `prepareSessionStart` calls it with `cwd`, provider `claude`, project mirror `<cwd>/.claude/skills`, and default personal mirror `~/.claude/skills` before `launchCLI`. Also assert default launch does not set `CLAUDE_CONFIG_DIR`; explicit Claude home is the only path that sets a provider home.

Add launch-policy regression tests proving the Claude native `Skill` tool is not present in default `--disallowedTools` after V1 cutover and is not dropped when an existing UI builtin-tools preference or frontend launch payload sends a `BuiltinTools` allowlist. The current hard-disabled built-in registry must be updated so provider-native skill discovery is not blocked by Super-Dolphin launch flags.

- [ ] **Step 2: Replace symlink setup**

Remove `skillCacheDir` and `setupWorkspaceSkills` from the Claude driver. Add required `SkillMirrorReconciler` wiring for provider startup. Startup must call `ReconcileProviderMirrors(ctx, spec.cwd, targets)` with generated provider-native project and personal targets. The driver passes physical roots only; the skill module derives `TargetID` from trusted `cwd` / owner context and provider. Missing reconciler wiring is a startup configuration error in V1.

- [ ] **Step 3: Preserve user Claude CLI identity by default**

Default Claude startup must not create or inject an App-managed Claude home. If the request omits `claude_home` / `claudeHome` / `history_dir`, leave `CLAUDE_CONFIG_DIR` unset so the Claude CLI uses the user's own login/config. Personal skill mirrors still publish to `~/.claude/skills`.

At minimum, cover these concrete sinks:

- default child process environment does not include Super-Dolphin-managed `CLAUDE_CONFIG_DIR`
- explicit Claude home is canonicalized, receives `<explicit-home>/skills`, and is passed to launch config
- runtime config snapshot omits Claude home keys by default and includes them only for explicit home
- settings/config reads and writes use explicit Claude config root only when the user configured one

Add tests asserting default startup uses user CLI identity and explicit home remains supported.

Do not reintroduce startup-time skill injection or `.claude/skills` symlink behavior.

- [ ] **Step 3b: Migrate Claude builtin-tool policy**

V1 treats Claude native `Skill` as required provider discovery plumbing. Update `internal/provider/claudecli/module.go`, `internal/module/uistate/builtin_tools.go`, `internal/module/uistate/config_rpc.go`, `cmd/agent-terminal/frontend/vue-app/stores/builtin-tool-policy.js`, and launch payload builders so old persisted preferences cannot keep `Skill` disabled after cutover.

Required behavior:

- `Skill` is not `DefaultDisabled` in the backend builtin registry after V1.
- Reading old builtin-tool settings that contain disabled `Skill` normalizes or migrates that entry away for Claude launches.
- When frontend sends a `claude_builtin_tools` allowlist, the launch builder includes native `Skill` or omits the allowlist so default native discovery remains enabled.
- User-visible policy screens must not present `Skill` as a normal optional tool toggle if disabling it would prevent native skill discovery.
- Tests seed old UI state with `Skill` disabled, start Claude through the normal thread action path, and assert `transport_config` does not generate an allowlist that excludes `Skill`.

- [ ] **Step 4: Classify reconcile failures**

Use stable failure kinds:

- `no_existing_mirror_fail_closed`: publish failed before any owned mirror existed. Startup fails closed because V1 has no degraded gray launch path.
- `stale_owned_mirror_fail_closed`: an owned mirror exists, but publish failed before it could be refreshed. Startup fails closed because provider may read stale skills.
- `unmanaged_conflict_fail_closed`: unmanaged same-name provider-native content exists. Startup fails closed until the user imports it, takes it over after backup, removes/renames the unmanaged content, or changes canonical names in V1 resolution UI/RPC. Read-only acknowledgement is not a launch-unblocking resolution.
- `same_name_conflict_fail_closed`: project/personal or personal/personal canonical conflict. Startup fails closed.
- `mirror_drift_fail_closed`: owned mirror drift or canonical-deleted-with-drift. Startup fails closed until user resolves drift.

In all cases emit a runtime/provider event with conflict kind, paths, hashes, and next actions. Do not silently shadow one skill with another. Warning-only launch is not a V1 acceptance path.

- [ ] **Step 5: Run Claude tests**

Run:

```bash
./scripts/test_with_guard.sh ./internal/provider/claudecli -run 'Test.*PrepareSessionStart|Test.*Skill|Test.*Mirror' -count=1
```

Expected: Claude no longer calls `SetupWorkspaceSkills`, startup reconcile is observable in tests, default launch keeps user CLI identity, explicit provider home remains supported, and default launch args do not hard-disable Claude native `Skill`.

## Task 4: Codex Startup Mirror Reconcile And Provider Home

**Files:**
- Modify: `internal/provider/codexapp/driver.go`
- Modify: `internal/provider/codexapp/module.go`
- Modify: `internal/provider/codexapp/driver_test.go`
- Modify: `internal/provider/codexapp/driver_pool_routing.go`
- Modify: `internal/provider/codexapp/pool_spawner.go`
- Modify: `internal/provider/codexapp/pool_spawn_cmd.go`
- Modify: `internal/module/thread/lifecycle_helpers.go`
- Modify: `internal/module/thread/lifecycle_helpers_test.go`
- Modify: `cmd/agent-terminal/frontend/vue-app/provider-config-options.js`
- Modify: `cmd/agent-terminal/frontend/vue-app/stores/thread-actions-helpers.js`
- Modify: frontend settings/provider config files after locating them with `rg "codexHome|CodexHome|CODEX_HOME|providerConfig"`
- Modify: cron/provider-start config builders after locating them with `rg "CodexHome|codexHome|CODEX_HOME" internal cmd`
- Modify: `internal/provider/shared` files only if provider-home env handling already lives there

- [ ] **Step 1: Write startup reconcile test**

Inject a fake `SkillMirrorReconciler` and assert Codex startup calls it with provider `codex`, project mirror `<cwd>/.agents/skills`, and default personal mirror `~/.agents/skills` before `ServerPool.Acquire`, app-server spawn, or any `CODEX_HOME` process launch.

- [ ] **Step 2: Use the existing Codex home identity path**

Default `codexHome` to the user's `~/.codex` before Codex identity resolution. This is the Codex CLI config/login home, not the skill mirror root. Personal skill mirrors use `~/.agents/skills` unless the user explicitly configured a provider home.

Update provider-start defaults only where they still send an obsolete skill mirror path. Tests must prove start, resume, UI provider config, and cron/provider-start builders:

- set `codexHome` / `CodexHome` to canonical user `~/.codex` when the request omits Codex identity
- create the directory before identity resolution requires it
- keep explicit user-provided `codexHome` unchanged only for advanced/manual sessions

Explicit caller-provided `codexHome` remains valid for advanced/manual sessions. Omitted Codex identity resolves to the user's `~/.codex`.

The reconcile call must receive both the resolved `codexHome` and the project `cwd` before pool acquisition. Reconcile invocation failure and blocking mirror infrastructure/safety errors fail closed; ordinary content conflicts are surfaced in the Skills UI and must not block provider startup. Add a test where the fake pool panics if acquired before reconcile; the test must pass.

Provider startup must not bypass mirror reconcile. Add tests where `CODEXAPP_USE_POOL=false`, `d.pool == nil`, or missing mirror reconciler fails closed before provider launch when the driver requires pool-backed Codex startup. Manual fallback behavior must be explicitly marked in diagnostics if retained.

- [ ] **Step 3: Run Codex provider tests**

Run:

```bash
./scripts/test_with_guard.sh ./internal/provider/codexapp -run 'Test.*Start|Test.*Mirror|Test.*Config' -count=1
```

Expected: Codex startup reconciles mirrors, uses user `~/.codex` as CLI identity, writes provider-native mirrors under `.agents/skills` / `~/.agents/skills`, rejects missing mirror wiring, and does not receive injected skill prompt content.

## Task 5: Retire Old Claude Symlink Adapter

**Files:**
- Modify: `internal/module/cliadapter/symlink.go`
- Modify: `internal/module/cliadapter/symlink_test.go`
- Modify: `internal/module/cliadapter/symlink_posix_test.go`

- [ ] **Step 1: Rewrite tests around migration only**

Keep only migration helper tests:

- exact old symlink to `~/.super-dolphin/skills-cache` is detected and blocks publish/startup until a resolution UI/RPC action backs it up and replaces it with audit
- regular directory at `.claude/skills` is conflict
- symlink to any other target is conflict

- [ ] **Step 2: Remove runtime symlink setup**

Delete or deprecate `SetupWorkspaceSkills` so no provider startup can call it. Keep a small migration helper if the V1 publisher needs to recognize the exact old symlink.

Do not auto-replace the old symlink from provider startup or publisher code. Replacement is a mutating takeover-style resolution: preview hash -> backup old symlink/target metadata -> audit intent -> remove symlink -> create owned mirror directory -> audit finalize. Detection alone keeps startup fail-closed.

- [ ] **Step 3: Run cliadapter tests**

Run:

```bash
./scripts/test_with_guard.sh ./internal/module/cliadapter -count=1
```

Expected: no test expects `.claude/skills` to be symlinked to old cache.

## Task 6: Gitignore And Docs

**Files:**
- Modify: `.gitignore`
- Modify: `README.md`
- Optionally modify: `docs/doc/codemap/README.md`

- [ ] **Step 1: Add `.agents/` ignore**

Add:

```gitignore
# Local Codex provider-native skill mirror generated by Super-Dolphin
/.agents/
```

Keep existing `/.claude/` ignore.

- [ ] **Step 2: Update README**

Replace old descriptions of `~/.super-dolphin/skills-library`, `~/.super-dolphin/skills-cache`, `.claude/skills` symlink, and Codex `skill_read_section` with:

```text
Project skills are managed in .agent/skills and published into generated provider-native mirrors under .claude/skills and .agents/skills. Personal skills are managed under ~/.super-dolphin/skills/personal and published into provider-native personal mirrors under ~/.claude/skills and ~/.agents/skills by default. Explicit provider homes may use their own skills directory. Provider mirrors are generated outputs and should not be committed.
```

- [ ] **Step 3: Run doc checks**

Run:

```bash
git diff --check
rg -n "skills-cache|skills-library|skill_read_section|SetupWorkspaceSkills|RenderSkillManifest" README.md internal/provider internal/platform/toolbridge internal/app internal/contract
```

Expected: remaining matches are deleted or tests intentionally asserting absence. No production runtime path depends on old skill cache/library injection.

## Task 7: Remove Old Skill Runtime Consumers

**Files:**
- Modify: `internal/app/modules.go`
- Modify: `internal/module/prompt/module.go`
- Modify: `internal/module/prompt/assembler_support.go`
- Modify: `internal/module/uistate/config_rpc.go`
- Modify: `internal/module/uistate/builtin_tools.go`
- Modify: `internal/provider/claudecli/factory.go`
- Modify or delete: `internal/provider/claudecli/fbsd_hook.go`
- Modify or delete: `internal/module/skill/*candidate*`
- Modify or archive: `internal/store/skillcandidate/**`
- Modify: `internal/store/module.go`
- Modify or archive: `sql/queries/skill_candidate.sql`
- Modify generated sqlc files only through `make sqlc-verify`
- Modify: `internal/module/turn/module.go`
- Modify: `internal/module/turn/prompt_assembly.go`
- Modify: `internal/module/turn/skills.go`
- Modify: `internal/module/turn/skill_extractor.go`
- Modify: `internal/module/turn/feedback_proposer.go`
- Modify: `internal/contract/prompt.go`
- Modify: `internal/module/memory/kairos.go`
- Modify: `internal/module/skill/rpc.go`
- Modify: `cmd/agent-terminal/frontend/vue-app/services/skills-api.js`
- Modify: frontend candidate review screens after locating them with `rg "candidate|skills/candidate"`
- Modify or delete: `internal/module/skilllibrary/**`
- Modify or delete: `internal/module/skillforge/**`
- Modify or delete: `internal/module/fbsd/**`

- [ ] **Step 1: Write consumer-removal tests**

Cover:

- prompt assembly no longer reads `SkillReplacementAggregator` or `ReplacedNativeTools`
- uistate config/builtin-tools no longer reads `SkillLibraryLister`
- app modules no longer provide `skillforge.Module`, `skilllibrary.Module`, or `fbsd.Module` to production runtime
- Claude provider no longer installs or calls `SetFBSDRecorder`, `recordSkillReadIfApplicable`, or `FBSDRecorder`
- runtime no longer writes `.super-dolphin/skills-stats` or reads `.super-dolphin/skills-stats` as skill usage truth
- turn-level prompt assembly no longer injects selected `SKILL.md` bodies through `turnSkillPrompt`, `TurnInput.SkillPrompt`, or equivalent prompt fields; selected skills are passed only as provider-native mirror state or metadata needed for UI
- old `skills/candidate/*` RPC/UI/writer paths are removed, disabled, or migrated out of production before V1 acceptance; no runtime code writes `skill_candidates`
- production store wiring no longer exposes a live `skillcandidate.Module` writer; if historical migrations or archived store code remain, they are compile-proven unreachable from the production Fx graph
- source SQL either removes old candidate write queries or leaves them only for migration/history tests; generated sqlc drift is reconciled through `make sqlc-verify`
- `rg "SkillReplacementAggregator|ReplacedNativeTools|SkillLibraryLister|skillforge.Module|skilllibrary.Module|fbsd.Module|SetFBSDRecorder|recordSkillReadIfApplicable|FBSDRecorder|skills-stats|turnSkillPrompt|SkillPrompt|skillcandidate|skills/candidate|InsertSkillCandidate" internal cmd sql` returns only deleted-code tests, migrations, explicit compatibility rejection tests, or historical notes

- [ ] **Step 2: Refactor consumers**

Replace old native-tool replacement UI/config data with canonical/proposal-backed data where the product still needs a screen. If a screen was only for old injection/cache behavior, remove it from runtime UI rather than leaving a dead compatibility path.

If V2 proposal UI has not landed yet, V1 must still retire old candidate entrypoints by hiding/removing the UI and returning a removed/unsupported error from old RPC handlers. Do not keep the old candidate pipeline alive as a temporary bridge.

Decide the `skillcandidate` storage boundary explicitly during implementation:

- Preferred: delete or archive old source queries such as `sql/queries/skill_candidate.sql`, regenerate sqlc, remove `skillcandidate.Module` from `internal/store/module.go`, and keep only migrations/historical docs.
- Allowed only if deletion is unsafe: keep old table/query code as migration/history compatibility, but remove all production Fx wiring and runtime writers/readers. Add a compile or Fx graph test proving no production module can call `InsertSkillCandidate`.

- [ ] **Step 3: Run consumer tests**

Run:

```bash
./scripts/test_with_guard.sh ./internal/module/prompt ./internal/module/uistate ./internal/app ./internal/module/turn ./internal/module/skill ./internal/store -run 'Test.*Skill|Test.*Builtin|Test.*Prompt|Test.*Fx|Test.*Candidate|Test.*Local|Test.*Store' -count=1
make sqlc-verify
rg "SkillReplacementAggregator|ReplacedNativeTools|SkillLibraryLister|skillforge.Module|skilllibrary.Module|fbsd.Module|SetFBSDRecorder|recordSkillReadIfApplicable|FBSDRecorder|skills-stats|turnSkillPrompt|SkillPrompt|skillcandidate|skills/candidate|InsertSkillCandidate" internal cmd sql
cd cmd/agent-terminal/frontend && npx vitest run
```

Expected: old skill runtime consumers are gone or compile-proven unreachable, frontend candidate/runtime-injection UI no longer exposes production entrypoints, and the grep gate returns only deleted-code tests, migrations, explicit compatibility rejection tests, or historical notes.

## Task 8: Provider-Native Discovery Smoke

**Files:**
- Create: `scripts/skill_native_smoke.sh`
- Create: `scripts/skill_native_smoke_testdata/README.md` only if the script needs static fixture notes

**2026-05-17 manual smoke note:** A desktop debug run with temp Postgres and temp `SUPER_DOLPHIN_HOME` created project skill `sd_project_smoke` under `.agent/skills`, published mirrors to Claude's project skill root and the then-tested Codex project mirror root, and both Claude and Codex answered with the probe marker `SD_PROJECT_SKILL_OK`. Treat this as historical exploratory evidence only. 2026-05-18 correction changes Codex official mirror to `.agents/skills`; release acceptance must use `.agents/skills`.

- [ ] **Step 1: Create a smoke script**

The script should:

1. Create a temp project.
2. Write `.agent/skills/probe/SKILL.md`.
3. Run file-level mirror reconcile through this fixed command, passing temp paths through environment variables consumed by the test:

```bash
SKILL_NATIVE_SMOKE_PROJECT="$tmp_project" \
HOME="$tmp_home" \
SUPER_DOLPHIN_HOME="$tmp_home/.super-dolphin" \
go test ./internal/module/skill -run '^TestSkillNativeSmokeFileLevel$' -count=1
```

with `HOME` and `SUPER_DOLPHIN_HOME` pointing at temp directories.
4. Assert `.claude/skills/probe/SKILL.md` exists.
5. Assert `.agents/skills/probe/SKILL.md` exists.
6. Assert personal mirrors are under temp `~/.claude/skills` and `~/.agents/skills` when personal fixtures are included.
7. Assert the real user `~/.claude`, `~/.agents`, and `~/.codex` paths were not created or modified by the smoke run.
8. Launch through the Super-Dolphin Claude and Codex drivers with installed provider CLIs and a probe skill, not only by invoking naked binaries. The probe skill contains a random nonce and behavior that cannot be inferred from the user prompt. Release validation must observe a provider skill discovery/invocation event, CLI diagnostic discovery output, or a native skill-call trace that includes the nonce and provider-native path. Ordinary model text is auxiliary only and is not sufficient proof by itself.
9. Negative smoke covers missing CLI / missing login as clear setup errors; V1 does not require PATH-negative bundled-binary proof.

- [ ] **Step 2: Run smoke locally**

Run:

```bash
bash scripts/skill_native_smoke.sh
```

Expected: file-level mirror checks pass without touching real user home. Driver-level and provider-binary checks pass in release validation. CI-only `SKIP provider-binary` is allowed only as an environment limitation and is not sufficient for V1 acceptance.

## Task 9: Plan-Level Verification

- [ ] **Step 1: Run affected packages**

Run:

```bash
./scripts/test_with_guard.sh ./internal/provider/claudecli ./internal/provider/codexapp ./internal/platform/toolbridge ./internal/module/cliadapter ./internal/module/prompt ./internal/module/uistate ./internal/module/turn ./internal/module/memory ./internal/module/skill ./internal/app ./internal/contract -count=1
```

- [ ] **Step 2: Run guard and build**

Run:

```bash
make sqlc-verify
make guard
make build-plain
```

- [ ] **Step 3: Run mandatory frontend checks**

Run from `cmd/agent-terminal/frontend`:

```bash
node scripts/size-guard.cjs
npx vitest run
npm run build
```

Expected: provider config defaults, Claude builtin-tool policy migration, and thread/cron provider-start builders preserve user CLI identity by default and keep explicit provider homes working.

- [ ] **Step 4: Run provider-native smoke**

Run:

```bash
bash scripts/skill_native_smoke.sh
```

Expected: file-level and driver-level provider-native smoke pass with installed provider CLIs or documented fake test doubles. Missing CLI/login negative paths return clear setup errors.

- [ ] **Step 5: Check status**

Run:

```bash
git diff --check
git status --short
```

Expected: provider cutover has no old runtime skill injection path, generated SQL is synced, frontend defaults are built, and provider-native discovery is proven through the smoke script.

## Accepted Defaults And Gates For This Plan

- D1 is fixed: Claude/Codex CLIs are user-installed; Super-Dolphin manages canonical skills and provider-native mirrors, not provider installation or login identity.
- Provider CLI preflight is fixed: release smoke uses installed CLIs when available, and missing CLI/login paths fail with clear setup errors.
- Startup reconcile failure classification is fixed: every mirror publish failure or conflict fails closed in V1; no degraded gray launch path is accepted.
- Old `skilllibrary`, `skillforge`, `fbsd`, FBSD hook/stat paths, and old `skills/candidate/*` production consumers must be removed or refactored in this cutover before V1 acceptance, not deferred as a runtime cleanup fallback.
- Claude native `Skill` is required discovery plumbing. V1 must migrate old builtin-tool settings and frontend launch allowlists so they cannot keep `Skill` disabled after cutover.
- Old `skillcandidate` may remain only as migration/history data if deletion is unsafe; production Fx wiring, UI/RPC handlers, and writers must be removed or compile-proven unreachable, with `make sqlc-verify` covering any SQL/query change.
- Driver-level provider-native smoke is required for release acceptance; file-level-only CI evidence is insufficient to claim provider-native discovery works.
