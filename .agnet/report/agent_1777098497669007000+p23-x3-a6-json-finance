# last_report — a6 JSON/finance 第五轮复审

✅ **P13 provider path 拆分已落盘**  
- 证据：agent final answer schema 适配与 P8/P12 arbiter JSON mode 已拆成两行，且说明 arbiter light JSON 不得替代 runtime validate（`P13_StrictJSONOutput.md:87-88`）。  
- 建议：保留；owner 开工前把 `provider / launcher turn 请求路径` 精确到文件，避免“待补路径”长期悬空。

✅ **schema_version 类型已修正一致**  
- 证据：node `output_schema_version INTEGER`（`P13_StrictJSONOutput.md:101`），validation row `schema_version INTEGER`（`P13_StrictJSONOutput.md:110`）。  
- 建议：CAS SQL/archtest 覆盖 hash+version 同时匹配。

⚠️ **parse_mode 已 schema 化，但流程旧文案仍冲突**  
- 证据：新增 `output_parse_mode`（`P13_StrictJSONOutput.md:102`），风险段限定 `strict|strip_markdown|lenient` 且金融 strict（`P13_StrictJSONOutput.md:151`）；但流程仍写“jsonpath / json5 容错可选”（`P13_StrictJSONOutput.md:55`）。  
- 建议：把 line55 改为“按 `output_validation.parse_mode` 解析；金融 strict 禁 json5/jsonpath/markdown wrapper”。

✅ **canonical hash 明显加强**  
- 证据：已指定 JCS/RFC8785 或等价稳定规范，并把 defaults、继承合并、递归 additionalProperties transform、`$ref` 边界纳入 hash 前置步骤（`P13_StrictJSONOutput.md:191`）。  
- 建议：把“owner 在 PR 中冻结”前移成文档内必填子段，列出 `$ref` 是否 dereference、annotation 是否参与 hash，减少实现漂移。

✅ **raw_blob_ref 已改 redacted-only**  
- 证据：v1 `raw_blob_ref` 只能指向 redacted blob；未来 encrypted raw 需 KMS/ACL/retention/break-glass audit，且不得复用本字段语义（`P13_StrictJSONOutput.md:195`）。  
- 建议：DDL 字段名可考虑改 `redacted_blob_ref`，避免后续误存 raw。

✅ **P10 金融 badge 已补关键合规项**  
- 证据：badge 展示 `unanimous + additionalProperties=false + audit_log=true + parse_mode=strict + schema_draft=2020-12 + hash-chain`（`P10_TemplateAndUI.md:192`）。  
- 建议：再补“recursive additionalProperties / opt-out 需审批”的帮助文案。

⚠️ **README P13 摘要仍是旧金融 preset**  
- 证据：README 只写 `unanimous swarm + additionalProperties=false + audit_log=true`（`README.md:393`），缺 `parse_mode=strict`、`schema_draft=2020-12`、hash-chain、P12 feature gate。  
- 建议：同步为 P13/P10 完整口径，避免入口文档误导。

⚠️ **P12 feature gate 分散，P12 自身未写 UI/模板隐藏规则**  
- 证据：P13 写“未合入 P12 时隐藏 swarm 选项”（`P13_StrictJSONOutput.md:77`, `P13_StrictJSONOutput.md:144`）；P10 字段注册表有 `feature_gate`（`P10_TemplateAndUI.md:222`）；但 P12 依赖段只写 P8/P9 已合入（`P12_SwarmArbiter.md:129-133`），未声明 P10/P13 暴露需 feature gate。  
- 建议：在 P12 “依赖/风险”补：任何 `verify.arbiter_swarm` UI/API/template 暴露必须受 `p12_swarm_enabled` feature gate 控制，未合入 P12 时 schema reject 或隐藏字段。

✅ **runtime validate hard guarantee 仍保持正确**  
- 证据：provider native 只优化，hard guarantee 来自 runtime validate + audit（`P13_StrictJSONOutput.md:65-67`；`README.md:391`）。  
- 建议：继续保持，不允许 provider JSON mode 成为金融合规依据。