## 审查结论：BLOCK

只读审查完成，未修改文件。结论是 **BLOCK**：P23 文档方向总体正确，但 P0/P1/P2 与 README 存在几处会直接阻塞实施的硬不一致，且有至少一个现有代码锚点已经失真。

### High 问题

| 严重度 | 位置 | 证据 | 影响 | 建议修法 |
|---|---|---|---|---|
| High | `docs/plans/迁移/p23/README.md:79-83`；仓库事实：`migrations/0063_agent_thread_name.sql:1` | README 声称“当前最大已占用编号是 0062”，并要求 P23 从 `0063_dag_state_machine.sql` 起算。但仓库已有 `migrations/0063_agent_thread_name.sql:1`。 | P0 migration 编号方案已与当前仓库冲突；若按文档实施会产生重复编号/迁移顺序漂移，COMPLIANCE 的 migration sequence gate 也会误判。 | 立即重排 P23 migration 编号。不要保留 README 中“0063 起算”的说法；以当前仓库最大编号重新分配，并同步 `README.md`、`P0_DAGRuntimeSkeleton.md`、`COMPLIANCE_GATES.md`、各子计划中的编号和 gate 表。 |
| High | `docs/plans/迁移/p23/README.md:91-95`、`README.md:193`、`P1_WakeupDispatcherAndLaunchBinding.md:32` | README 把 `pending→running` 归 `dagWatcherActor`，又说 `assigned_agent_id` “由 dispatcher 在 `pending→running` 同事务内写入”；P1 又说 `assigned_agent_id/active_turn_id` 不能在 launcher 前已知，要在 launcher 幂等调用后由 `BindRunningNodeTurn` CAS 写入。现有 SQL `cmd/mcp-orch/sql/queries/task_dag_node_runtime.sql:24-29` 只写 `status/result/active_wakeup_id`，没有 `assigned_agent_id`。 | runtime ownership 与 crash-window 协议不自洽：到底是 watcher 还是 dispatcher 推 `pending→running`？`assigned_agent_id` 是状态推进前可预分配，还是 launcher 后才可绑定？这会影响 CAS 形状、恢复分支、重入幂等和 P1 测试。 | 冻结一个单一协议：推荐 `dagWatcherActor` 只做 ready claim + `pending→running + active_wakeup_id`；`dagDispatcherActor` 持久化 `launch_intent` 后调用 launcher，再用 `BindRunningNodeTurn` CAS 绑定 `assigned_agent_id/active_turn_id`。然后把 `README.md:193` 改为“dispatcher 在 launcher accepted 后绑定”，不要说“pending→running 同事务”。若坚持同事务写 `assigned_agent_id`，必须明确 dispatcher 预分配 agent id，并改 P1 三阶段协议。 |
| High | `docs/plans/迁移/p23/README.md:94`、`P0_DAGRuntimeSkeleton.md:15-16`；仓库事实：`migrations/0023_dag_watcher_phase1.sql:38-43` | README 定义 `dagLeaseActor` “续租所有已 claim 的 node lease”；P0 把 “worker lease” 锚到既有 `task_dag_worker_leases`。但现有表主键是 `target_agent_id`，字段是 `owner_id/lease_expires_at`，没有 `dag_key/node_key/wakeup_id/actor_id` 等 node/wakeup lease subject。P1 的实际 claim lease 在 `task_dag_wakeups` 上，见 `task_dag_wakeup_dispatch.sql:6-18`。 | lease 设计不可实施：文档把 “node lease / wakeup claim lease / worker lease” 混在一起。`dagLeaseActor` 无法按当前表语义续租 “已 claim 的 node”，也无法表达多个 DAG node 对同一 agent 的并发 lease。 | 明确三类租约的边界：1）wakeup dispatch lease 继续放 `task_dag_wakeups.claimed_by/lease_expires_at`；2）若需要 node execution lease，P0 migration 新增以 `(dag_key,node_key,attempt_no)` 或 `wakeup_id` 为 subject 的表/列；3）`task_dag_worker_leases` 只保留为 agent/worker 级 lease，不要在文档中称为 node lease。 |

### Medium 问题

| 严重度 | 位置 | 证据 | 影响 | 建议修法 |
|---|---|---|---|---|
| Medium | `P0_DAGRuntimeSkeleton.md:31-40`、`P1_WakeupDispatcherAndLaunchBinding.md:9,32,48`；仓库事实：`cmd/mcp-orch/sql/queries/task_dag_node_read.sql:2`、`task_dag_node_runtime.sql:1-11` | P0/P1 要新增 `assigned_agent_id`，但现有 node 字段已有 `assigned_to`，并且现有 runtime bind SQL 只基于 `active_wakeup_id/active_turn_id`，不涉及新字段。文档没有说明 `assigned_to` 与 `assigned_agent_id` 的迁移关系。 | 容易把“人工 assignee / pool hint / target agent / launched runtime agent”混为一谈。P1 的 noop 规则 `assigned_agent_id != ''` 也无法从当前 schema 推导。 | 在 P0 DDL 中明确字段语义：`assigned_to` 是否保留为人类 assignee/调度 hint；`assigned_agent_id` 是否为实际 launcher 返回 agent；是否需要 `target_agent_id` / `runtime_agent_id` 分离。同步 DTO、SQLC、read DTO 映射和 P1 noop 判定。 |
| Medium | `P2_NodeTerminalReconcile.md:9,31,40`；仓库事实：`cmd/mcp-orch/sql/queries/task_dag_node_runtime.sql:37-42`、`cmd/mcp-orch/store/taskdag/store.go:168-176` | P2 要 `CompleteNode/MarkFailed/Retry` 全部带 `active_turn_id/attempt_no` fence。但现有 `CompleteTaskDagNode` 只按 `dag_key/node_key/status IN ('running','awaiting_verify')` 更新，不校验 `active_turn_id`；store `CompleteNode` 也没有传 turn id。README 已在 `README.md:246` 点名该缺口。 | P2 如果只“复用 CompleteNode”会让 late completed 覆盖 timeout/abort/retry 后的新 attempt，违反 terminal fence 与 P21 terminal precedence。 | P2 必须新增 fenced terminal SQL，例如 `WHERE dag_key=$... AND node_key=$... AND status='running' AND active_turn_id=$turn_id AND attempt_no=$attempt_no`；旧 `CompleteNode` 只能留兼容路径，strict DAG 禁用。 |
| Medium | `P2_NodeTerminalReconcile.md:30`；仓库事实：`cmd/mcp-orch/orchestration/hook_consumer.go:260-275` | P2 说在 `OnTurnCompleted` 链上装 enqueue tap。但当前 `handleTurnCompleted` 先执行 `svc.HandleReportEvent` 和 `handleTurnCompletedEvent`，然后才调用 `notifyTap.OnTurnCompleted`。 | 如果 P2 直接复用现有 `NotifyTap`，DAG terminal durable enqueue 不是 hook callback 的第一动作，会被现有 report/event 处理延迟，并共享当前重 DB callback 路径；这与“terminal/reconcile 必须 durable、enqueue-only”的目标有偏差。 | 明确 P2 tap 插入点：建议增加独立 DAG terminal tap，在 decode terminal 后尽早做 bounded parse + durable insert；现有 report handling 继续保留，但不要让 DAG terminal enqueue 依赖 report side-effect。archtest 也应检查 DAG tap 自身不做 launch/LLM/重查询。 |
| Medium | `COMPLIANCE_GATES.md:62-82`、`COMPLIANCE_GATES.md:141-145`；仓库事实：`internal/archtest/dependency_direction_mcp_orch_test.go:23-29` | COMPLIANCE 要 P0 前 hard fail `dag_mcp_orch_dependency_allowlist_tight`；当前实际 allowlist 仍放行 `internal/store` 和 `internal/module`。README 也承认 `README.md:249` “真实代码未收紧”。 | 文档把 gate 设为 P0 前硬前置，但现有守卫还没达到该标准。若 owner 以“已有 archtest”为依据开工，会漏掉 cmd/mcp-orch 依赖边界违规。 | 在 P0 前置 checklist 中把 allowlist 收紧作为明确阻塞项，并给出准确改法：删除宽泛 `internal/store` / `internal/module`，只保留 `modularity-convention.md:377` 允许集合。 |

### Low 问题

| 严重度 | 位置 | 证据 | 影响 | 建议修法 |
|---|---|---|---|---|
| Low | `README.md:112`、`P1_WakeupDispatcherAndLaunchBinding.md:15`；仓库事实：`cmd/mcp-orch/orchestration/service_launcher_bridge.go:28-36` | 文档锚点写 `service_launcher_bridge.go:22-30`，但 `maxConcurrentLaunches=10` 实际在 `31`，semaphore 在 `36`。 | 锚点轻微失真，影响审查定位。 | 改为 `service_launcher_bridge.go:28-36` 或 `:31-36`。 |
| Low | `README.md:397` | README 估算 “8-12 工程日 / 2-3 周”。 | 与当前文档未完全冻结、migration 编号已冲突的状态不匹配，容易给执行预期造成误导。 | 在 BLOCK 修复前删除或降级为非承诺备注；等 P0/P1/P2 协议和 migration 编号冻结后再估算。 |

## 一致性与可实施性小结

- **README 与 P0/P1/P2 不完全一致**：最大问题是 `pending→running`、`assigned_agent_id`、launcher crash-window 的职责划分冲突。
- **runner.actors / fx.Module / BusModule 边界方向正确**：README `:50-53` 与契约 `fx-convention.md:121-127`、`rungroup-convention.md:121-132`一致；但现有 mcp-orch wiring 是根装配显式 `fx.Module("orchestration")` + `group:"runners"`，文档中新增 `cmd/mcp-orch/orchestration/<sub>/runner.go` 需要避免重新导出 `orchestration.Module`。
- **长循环禁止进 fx lifecycle/bus callback 的要求清晰**：README `:52`、P0 `:79`、COMPLIANCE `:62-64`覆盖到位。
- **wakeup/lease/reconcile 可实施性仍不足**：wakeup claim 现有基础可用，但 lease subject 混乱；terminal reconcile 必须新增 active_turn/attempt fence；hook tap 插入点需要更精确。
- **现有代码锚点多数真实，但 migration 编号和少数行号失真**：`0063` 已被占用是阻塞级事实错误。

**最终判定：BLOCK**。修完 High 项后可降为 NEEDS-FIX；Medium 项完成并同步 gate 后才建议 PASS。