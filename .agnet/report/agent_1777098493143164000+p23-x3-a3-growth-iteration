# last_report — 第五轮 a3 growth/iteration 只读复审

✅ **structural growth_budget 已与 runtime/convergence budget 分离**  
- 证据：`P11_DynamicNodeGrowth.md:31-34` 只把 `max_total_nodes/max_depth/max_spawn_per_node` 归 structural，`max_runtime_sec` 改走 convergence/execution；`README.md:367` 同步。  
- 建议修法：保持该口径，后续 schema validation 禁止 `growth_budget.max_runtime_sec`。

✅ **reservation DDL 与 spawned_node_keys 已补齐主 DDL**  
- 证据：`P11_DynamicNodeGrowth.md:90-107` 有 `dag_growth_reservations`、`status CHECK`、`spawned_node_keys JSONB`、idempotency unique、数值 CHECK；`:109-111` 有 reconcile 索引。  
- 建议修法：补 FK/tenant 字段或说明与 DAG owner/tenant 继承，否则多租户审计仍靠应用层。

⚠️ **P11 后文补充段仍是旧字段清单**  
- 证据：`P11_DynamicNodeGrowth.md:153` 的 reservation 字段列表未提 `spawned_node_keys`、`status CHECK`、数值 CHECK、reconcile index。  
- 建议修法：把该段改为“以 DDL 草案为准”，或同步完整字段，避免 owner 只看硬契约漏字段。

✅ **P9 backpressure matrix 已与 P11 对齐**  
- 证据：`P11_DynamicNodeGrowth.md:55` 明确 80% 拒新 spawn、100% hard stop、调高预算 audit 后 replay；`P9_ScaleScheduling.md:121` 同一动作矩阵与 metric `dag_spawn_budget_usage_ratio`。  
- 建议修法：补一句 metric 分子=ledger charged count 或 `total_node_count`，避免实现各算各的。

✅ **repair budget 已跨 P8/P12/P13 串起来**  
- 证据：`P8_VerificationGate.md:77,102-103,278` 增 `repair_chain_id/combined_repair_round/combined_repair_max`，schema/semantic/swarm dissent repair 同扣；`P12_SwarmArbiter.md:173` 要 `repair_with_dissent_summary` 扣同一链路；`P13_StrictJSONOutput.md:118-119` validation row 记录 repair chain/round。  
- 建议修法：P13 必测 `P13_StrictJSONOutput.md:161-164` 仍只测 `max_repair_rounds`，应加 combined budget 触顶测试。

⚠️ **P11/P12 “swarm verifier spawn 吃 growth budget”语义仍偏模糊**  
- 证据：`P11_DynamicNodeGrowth.md:126,141` 写 swarm verifier spawn 也吃 budget；但 P12 主要是 LLM swarm，不一定 spawn node，`P12_SwarmArbiter.md:165,171` 更像 LLM/budget reservation。  
- 建议修法：区分“judge/escalate 创建 DAG node 扣 structural growth budget”和“LLM member call 扣 LLM spend budget”。

🆕 **convergence DSL 仍缺字段级验收表**  
- 证据：`P11_DynamicNodeGrowth.md:41,161-163` 只列四类 kind。  
- 建议修法：补每个 kind 的必填字段、计数来源；尤其 `max_iterations` 绑定 spawn round、growth_depth 还是 repair chain。