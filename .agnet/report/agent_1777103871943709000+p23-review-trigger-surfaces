## 结论：BLOCK

P23 三触发源方向基本写对了，但当前文档与现有代码核验后，仍有几处会导致 P4/P5/P6 实施时绕过统一 `StartDAG`、信任伪造 caller、或让 cron/RPC 安全边界失守。以下按 High / Medium / Low 分级。

---

## High

### 1. Host Agent caller/ownership 当前不可可信获取，P4 仍可能信任可伪造 `agent_id`

**证据**

- MCP 工具输入把 `agent_id` 当普通参数：`cmd/mcp-orch/tools/task_tools.go:12-19`
- `HandleCreateDAG` 直接把输入转 service request：`cmd/mcp-orch/tools/task_tools.go:64-70`
- `agent_id` 仅写入 `CreatedBy`：`cmd/mcp-orch/tools/task_tools.go:144-174`
- MCP `tools/call` 路径只解码并调用 tool，没有 caller identity 注入：`internal/mcpserver/common/server.go:221-264`
- P3 要求 `trigger_meta.caller_identity` 必填：`docs/plans/迁移/p23/README.md:203-207`
- P4 只说 `auto_start` 创建后调 `StartDAG`，未定义 host agent caller 从哪里来：`docs/plans/迁移/p23/P4_HostAgentTriggerSurface.md:27-30`

**风险**

Host Agent 触发 `auto_start` 时，如果继续从 tool 参数读取 `agent_id`，调用者可伪造 owner。后续 `owner_id / tenant_id`、audit、idempotency scope 都会建立在不可信 identity 上。

**建议**

P4/P3 必须明确：

- `agent_id` 参数只能作为 legacy display / compatibility，不可作为 authenticated caller。
- MCP tool handler 要从 MCP session / runtime context 注入 `caller_identity`。
- `StartDAG(ctx, dagKey, triggerMeta)` 缺 caller 必须 hard fail：`ErrCallerIdentityRequired`。
- P4 必测补：伪造 `agent_id` 不改变 `owner_id`，真实 caller 来自 ctx。

---

### 2. 当前 RPC DAG 写入口直接调用 service，未统一到 `StartDAG`，且 `dag/start` 不存在

**证据**

- TCP JSON-RPC 已暴露 `task/dag/create|get|list`、`task/node/update`：`cmd/mcp-orch/orchestration/rpc.go:127-137`
- `task/dag/create` 直接调用 `svc.CreateDAG`：`cmd/mcp-orch/orchestration/rpc.go:127-129`
- `task/node/update` 直接调用 `svc.UpdateNodeStatus`：`cmd/mcp-orch/orchestration/rpc.go:136-138`
- 当前 `CreateDAG` 只 upsert DAG/nodes 并返回：`cmd/mcp-orch/orchestration/dag.go:109-131`
- `StartDAG` 搜索未发现实现；文档只存在承诺：`docs/plans/迁移/p23/P3_ExplicitDAGStartAndOwnership.md:9,30-31`

**风险**

P23 要求三触发源汇流到 `cmd/mcp-orch/orchestration/dag_start.go:StartDAG`，但现有外部 RPC 已有写入口，且继续直连 create/update。P6 若只加 middleware，不改 service 层 guard，仍会绕过 start/idempotency/auth/audit。

**建议**

P3/P6 需要把 service 层 guard 写成强制入口：

- 新增 `dag/start` 后，所有触发推进只能调 `StartDAG`。
- `task/dag/create` 只能创建 draft/auto 兼容，并按 trigger 规则决定是否内部调用 `StartDAG`。
- `task/node/update` 在 strict DAG 默认拒绝推进状态，必须经过 CAS/fence 路径。
- archtest 不只扫 handler，还要扫 `svc.CreateDAG` / `UpdateNodeStatus` 是否绕过 guard。

---

### 3. External RPC 的 AuthN/AuthZ/rate/quota/audit 落点仍不够可执行，三入口 adapter 没有具体文件级闭环

**证据**

- RPC config 只有 `DatabaseURL/RPCAddr/LogLevel/...`，无 auth/rate/quota/audit 配置：`internal/platform/config/config.go:33-41`
- TCP JSON-RPC 默认监听 `127.0.0.1:8090`：`internal/platform/config/config.go:43-47`
- TCP RPC accept 后直接 serve connection：`internal/platform/rpc/server.go:291-317`
- Wails WS 使用默认 upgrader，无 origin/session/caller 检查：`internal/platform/rpc/transport_ws.go:16,26-39`
- Wails HTTP 直接挂 `/wails/ws`：`internal/ui/wails/http_server.go:39-46`
- MCP HTTP `/mcp` 只校验 method/body，未鉴权：`internal/mcpserver/common/http_transport.go:73-91`
- P6 只泛称 `WithCallerIdentity` middleware：`docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:30-37`

**风险**

P6 的方向正确，但目前“Transport 层 identity adapter + service 层 guard”没有绑定到具体实现点。Wails WS 特别需要 Origin/CSRF/session 规则，否则 browser-accessible localhost WS 会成为本机跨站调用面。

**建议**

P6 文档补具体落点矩阵：

- TCP JSON-RPC：`internal/platform/rpc/server.go` / connection ctx 注入 identity。
- Wails WS：`internal/platform/rpc/transport_ws.go` + `internal/ui/wails/http_server.go`，明确 Origin/CSRF/session policy。
- MCP HTTP：`internal/mcpserver/common/http_transport.go` / `server.go` 注入 caller。
- Service 层：`cmd/mcp-orch/orchestration/dag_start.go` 和 DAG read/write service 统一执行 AuthN/AuthZ/rate/quota/audit。
- Audit fail-closed/spool 要覆盖 `task/dag/create`、`dag/start`、`task/node/update`。

---

### 4. Idempotency scope 对 Host Agent / External RPC / StartDAG 仍不完整；只有 P5 cron 写得较明确

**证据**

- README 只说 triggers 通过 `idempotency_key` 防重，但没有统一 schema/DDL/window：`docs/plans/迁移/p23/README.md:203-207`
- P3 说 `StartDAG` 包含 idempotency，但 DDL 只列 owner/last_trigger，没有 idempotency 表或唯一约束：`docs/plans/迁移/p23/P3_ExplicitDAGStartAndOwnership.md:29-42`
- P6 对 external matrix 写了部分 scope：`docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:74-82`
- P4 `auto_start` 没有说明 idempotency key 来源：`docs/plans/迁移/p23/P4_HostAgentTriggerSurface.md:27-30`
- 当前 cron run idempotency key 是 UUID：`internal/module/cron/scheduler.go:318-326`
- 当前 dedupe hash 包含随机 idempotency key：`internal/module/cron/schedule.go:125-139`

**风险**

P5 对 DAG cron path 已要求 deterministic `hash(cron_job_id, scheduled_at, target_dag_key)`，但 P3 的统一 `StartDAG` 没有定义存储和冲突语义。Host `auto_start` 和 external `dag/start` 可能产生不同防重规则，导致双 start 或冲突返回不一致。

**建议**

P3 先冻结统一 idempotency contract：

- `dag_start_requests` 或等效唯一索引，字段至少含 `tenant_id, caller_id, dag_key, trigger, idempotency_key, params_hash, result`.
- Cron deterministic key：`hash(cron_job_id, scheduled_at_utc, target_dag_key)`.
- Host auto_start deterministic key：例如 `hash(agent_caller_id, dag_key, create_request_hash)`.
- External write：client key 必填；同 key 同 params 返回同 result，不同 params 返回 conflict。
- 所有路径由 `StartDAG` 统一处理，不让各触发源自定义语义。

---

## Medium

### 5. P4 向后兼容语义与 README “trigger 缺失 hard error” 存在冲突

**证据**

- README 阶段 0 说 `schedule.trigger` 改 enum，缺失/非枚举 hard error：`docs/plans/迁移/p23/README.md:100-105,209-214`
- README 兼容段又说旧 trigger 空值只读兼容，1 release 后 hard fail：`docs/plans/迁移/p23/README.md:244`
- P4 说旧调用方不传 `auto_start` 仍按当前隐式期望工作，旧 DAG 默认 `auto`：`docs/plans/迁移/p23/P4_HostAgentTriggerSurface.md:27-30,46-50`
- 当前 MCP schema 仍把 trigger 当自由 string 且可空：`cmd/mcp-orch/tools/task_tools.go:118-124,246-250`

**风险**

实现者可能把“旧调用方 100% 兼容”理解为新建 DAG 缺 trigger 也继续 auto start，冲掉 P3 的 fail-fast enum 目标。

**建议**

补一张兼容矩阵：

- 已存在旧 DAG：空 trigger 只读兼容。
- P0/P3 后新建 DAG：缺 trigger hard fail，除非显式 legacy endpoint/compat mode。
- `auto_start` 与 `trigger=manual` 冲突 hard fail。
- `auto_start` 不等价于缺 trigger；应要求 `trigger=auto` 或明确 legacy migration path。

---

### 6. Cron cross-root 方案方向正确，但现有 archtest 仍放行 `cmd/mcp-orch -> internal/module`，P5 守卫未闭环

**证据**

- P5 禁止 `internal/module/cron` import `cmd/mcp-orch/orchestration`，并要求 TriggerSink/interface：`docs/plans/迁移/p23/P5_CronTriggerSurface.md:30-37,71-72`
- 当前反向 import 没有发现，`internal/module/cron` 未 import `cmd/mcp-orch/orchestration`。
- 但 archtest allowlist 仍放行 `internal/module`：`internal/archtest/dependency_direction_mcp_orch_test.go:23-29`
- README 也承认 allowlist 未收紧：`docs/plans/迁移/p23/README.md:249`

**风险**

P5 文档说“禁止 cmd/mcp-orch 直接 import internal/module/cron concrete”，但当前 archtest 无法拦住。后续实现很容易走最短路径形成 concrete import。

**建议**

P5 开工前先落 archtest：

- 禁止 `internal/module/cron` import `cmd/mcp-orch/*`。
- 禁止 `cmd/mcp-orch` 直接 import `internal/module/cron` concrete。
- 若需要复用 cron runtime，必须经 platform-level interface / bus / RPC sink，或在 `cmd/mcp-orch` 本地化 DAG cron bridge。

---

### 7. Cron DDL 需明确 tenant 来源，否则 P5 无法满足 P3/P6 owner/tenant

**证据**

- P5 DDL 草案只给 `cron_jobs` 加 `target_dag_key` / `target_dag_trigger_meta`：`docs/plans/迁移/p23/P5_CronTriggerSurface.md:41-57`
- P5 待办才提到 bridge 调 `StartDAG` 前必须带 `tenant_id`：`docs/plans/迁移/p23/P5_CronTriggerSurface.md:91`
- 当前 cron job 表无 tenant/owner 字段：`migrations/0045_cron_jobs.sql:15-53`

**风险**

Cron 触发 DAG 时，`StartDAG` 需要 `caller_identity` 和 tenant scope。若 cron job 自身没有 tenant/owner，bridge 只能从 target DAG 反查或从 trigger_meta JSONB 信任配置，容易形成跨 tenant 触发。

**建议**

P5 DDL 明确新增：

- `cron_jobs.owner_id`
- `cron_jobs.tenant_id`
- `cron_jobs.scope`
- `cron_job_runs.target_dag_key`
- run audit 字段或关联 audit log

并规定 tenant 必须来自已授权 cron job row，不允许只从 `target_dag_trigger_meta` 读取。

---

## Low

### 8. P6 错误映射已有方向，但需要补 JSON-RPC code catalog 的文件落点

**证据**

- P6 指定 rate limit JSON-RPC code `-32029`：`docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:58-64,89-92`
- 当前 RPC handler 直接返回 service error，没有统一 DAG error mapper：`cmd/mcp-orch/orchestration/rpc.go:127-138`

**建议**

补具体落点，例如：

- `cmd/mcp-orch/orchestration/errors.go`
- `internal/platform/rpc/error_mapping.go`
- `ErrCallerIdentityRequired`
- `ErrRateLimited`
- `ErrQuotaExceeded`
- `ErrAuditRequired`
- `ErrIdempotencyConflict`

---

### 9. `task_update_node` 兼容窗口需要更硬的截止条件

**证据**

- README 说 `task_update_node` 旧路径保留但 strict mode 收紧：`docs/plans/迁移/p23/README.md:86-90,191-194`
- 当前 SQL 仍是裸 status update：`cmd/mcp-orch/sql/queries/task_dag_node_write.sql:14-26`
- P6 matrix 说 strict DAG 默认拒绝 status 推进：`docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:81`

**建议**

把兼容窗口写成可测试条件：

- 旧 DAG 才能用 flexible update。
- 新 DAG 默认 `strict_state_machine=true`。
- P6 后 external `task/node/update` 只允许 assigned agent / owner 且走 CAS/fence。
- 1 release 后 external write-compat 默认 disabled。

---

## 审查点逐项结论

| 审查点 | 结论 |
|---|---|
| 三触发源是否统一 `StartDAG` | 文档目标正确，但现有代码未实现；RPC 当前仍直连 create/update。BLOCK |
| Cron 是否跨 root 违规 | 当前代码未见 cron → mcp-orch 反向 import；但 archtest 仍放行 concrete import，守卫未闭环。NEEDS-FIX |
| Idempotency key 是否 deterministic | P5 cron DAG path 写得较明确；P3/P4/P6 统一 StartDAG idempotency schema 不完整。NEEDS-FIX |
| RPC auth/rate/quota/audit 是否落点明确 | 方向正确，service 层 guard 有提及；三入口 adapter 文件级落点不足，当前代码全无。BLOCK |
| Host agent caller/ownership 是否可获取 | 当前只能从可伪造 `agent_id` 参数获取，不可信。BLOCK |
| 向后兼容窗口是否合理 | README 有 1 release 窗口，但 P4 “100% 兼容 / 默认 auto”与 fail-fast 有歧义。NEEDS-FIX |

---

## 最终判定

**BLOCK**

阻塞项是：Host caller identity 不可信、现有 RPC 写入口绕过未来 `StartDAG`、P6 三入口 AuthN/AuthZ/rate/quota/audit 没有文件级强制落点、统一 idempotency contract 不完整。建议先修 P3/P4/P5/P6 文档中的上述契约，再允许实现开工。