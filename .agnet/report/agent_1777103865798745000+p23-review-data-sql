只读审查完成，未修改文件。结论：**BLOCK**。

## High

| 问题 | 文件:行 | 证据 | 建议 |
|---|---:|---|---|
| P23 migration 编号基线已失真，`0063/0064` 已被占用；README 仍要求 P23 从 `0063_dag_state_machine.sql` 起并保留 `0064` unused。 | `docs/plans/迁移/p23/README.md:79-83`; `migrations/0063_agent_thread_name.sql:1`; `migrations/0064_skill_candidates.sql:1` | README 说当前最大已占用是 `0062`，但仓库已有 `0063_agent_thread_name.sql` 和 `0064_skill_candidates.sql`。我额外跑只读脚本确认：重复编号还有 `0001/001`、`0006`、`0025`，缺号 `0008/0011/0017`。 | 阶段 0 必须先重排 P23 编号：P0 不能再用 0063，0064 也不能声明 unused；更新 README/P0/P3/P5/P8/P9/P13/COMPLIANCE_GATES 的编号口径，并补 migration sequence guard。 |
| 存在裸 node status 写，外部 `task_update_node` 可绕过 expected-status CAS。 | `cmd/mcp-orch/sql/queries/task_dag_node_write.sql:14-20`; `cmd/mcp-orch/sql/queries/task_dag_node_write.sql:22-26`; `cmd/mcp-orch/store/taskdag/store.go:71-79`; `cmd/mcp-orch/orchestration/dag.go:167-184`; `cmd/mcp-orch/tools/task_tools.go:84-105` | `UpdateTaskDagNodeStatus` / `UpdateTaskDagNodeStatusFlexible` 只有 `WHERE dag_key/node_key`，没有 `WHERE status = $expected`；tool handler 直接调用 service → store → 裸写。README 明确要求 status 推进走 CAS，旧路径 strict mode 下拒绝跳态。 | P0 前必须把外部写路径变为 strict-state-machine 默认 true；SQL 统一改成 `expected_status` CAS；保留兼容路径也必须 gated + audit，且新 DAG 不可降级。 |
| terminal 回写没有 `active_turn_id` fence，late terminal 可覆盖当前运行轮次。 | `docs/plans/迁移/p23/P2_NodeTerminalReconcile.md:31-40`; `cmd/mcp-orch/sql/queries/task_dag_node_runtime.sql:37-42`; `cmd/mcp-orch/store/taskdag/contract.go:83-88`; `cmd/mcp-orch/store/taskdag/store.go:168-176` | P2 要求 `status=running AND active_turn_id=$turn_id`；当前 `CompleteTaskDagNode` 只检查 `status IN ('running','awaiting_verify')`，输入结构也没有 turn_id/attempt。 | `CompleteNode/MarkFailed/Retry/observe_lost` 全部加 `turn_id + attempt_no` fence；0 rows 作为 stale terminal，不允许重试覆盖。 |
| ready node claim 仍是整 DAG `FOR UPDATE`，不满足 `FOR UPDATE SKIP LOCKED LIMIT K` 硬门。 | `docs/plans/迁移/p23/P0_DAGRuntimeSkeleton.md:75`; `docs/plans/迁移/p23/P9_ScaleScheduling.md:65-67`; `cmd/mcp-orch/sql/queries/task_dag_node_read.sql:13-18`; `cmd/mcp-orch/store/taskdag/store.go:100-103` | P0/P9 明确首版 ready claim 必须 `SKIP LOCKED LIMIT K`；当前 `GetTaskDagNodesForUpdate` 按 dag 全量锁所有 node。wakeup claim 已有 SKIP LOCKED，但 ready node claim 没有。 | P0 实现独立 ready claim SQL：`WHERE status='pending' AND remaining_deps=0 ... FOR UPDATE SKIP LOCKED LIMIT $K`；禁止 watcher 先全量 load 再内存过滤。 |
| `EnqueueWakeup` 返回 execrows，被当成 wakeup id 的风险已经写在 P0 待办，现代码确实如此。 | `docs/plans/迁移/p23/P0_DAGRuntimeSkeleton.md:72`; `cmd/mcp-orch/sql/queries/task_dag_wakeup_dispatch.sql:1-4`; `cmd/mcp-orch/store/taskdag/store_wakeup.go:9-19` | SQL 是 `:execrows`，store 返回 int64 行数；P0 说不能把 execrows 当 wakeup id，否则 `active_wakeup_id` fence 误绑。 | 改为 `INSERT ... RETURNING id`；冲突时按 `idempotency_key` 查询既有 id；绑定 node 前必须使用真实 wakeup id。 |
| 当前核心 DAG schema 缺状态 CHECK / FK / owner-tenant / 依赖计数字段，P0/P3/P9 前置 DDL 不可跳过。 | `migrations/0004_ack_dag.sql:51-68`; `migrations/0023_dag_watcher_phase1.sql:9-30`; `docs/plans/迁移/p23/README.md:80-89`; `docs/plans/迁移/p23/P3_ExplicitDAGStartAndOwnership.md:38-42`; `docs/plans/迁移/p23/P9_ScaleScheduling.md:46-49` | `task_dag_nodes.status` 是自由 TEXT；node 没 FK 到 `task_dags`；wakeup 只有 dag_key/node_key 文本，无 FK/GC；无 `remaining_deps`、`owner_id/tenant_id/scope`。 | P0/P3 必须先补 CHECK、FK/归档策略、`remaining_deps`、owner/tenant/scope 及对应索引；P9 再做 no-tx concurrent index/归档。 |
| P8/P13 新审计/校验表草案缺 FK/GC 归属说明，虽然有 hash-chain 字段。 | `docs/plans/迁移/p23/P8_VerificationGate.md:110-153`; `docs/plans/迁移/p23/P13_StrictJSONOutput.md:104-124`; `docs/plans/迁移/p23/P13_StrictJSONOutput.md:191-195` | `dag_arbiter_calls`、`dag_verify_jobs`、`dag_output_validations` 都只有 `dag_key/node_key` 文本；P13 已要求 hash-chain，但没有约束到 DAG/node，也未冻结 retention/GC 与 archive cascade。 | 为 P8/P13 表补 FK 或明确 archive-safe FK 替代策略；补 TTL/retention、append-only guard、hash-chain 校验 query/index。 |

## Medium

| 问题 | 文件:行 | 证据 | 建议 |
|---|---:|---|---|
| `schedule.trigger` 仍是自由 string，strict_state_machine 默认也只存在于文档。 | `docs/plans/迁移/p23/README.md:100-105`; `cmd/mcp-orch/tools/task_tools.go:26-33`; `cmd/mcp-orch/tools/task_tools.go:118-125`; `cmd/mcp-orch/tools/task_tools.go:246-266` | 工具 schema 对 trigger 用 `StringSchema`；落库只是写 metadata JSON；没有 enum 或 ErrInvalidTrigger。 | 阶段 0/P3 前加 enum schema + service 校验；`manual/auto/cron/external` 缺参按 README 的硬错误返回。 |
| sqlc 归属总体方向正确，但根 `sqlc.yaml` 仍包含 `0023_dag_watcher_phase1.sql`，容易让 DAG schema ownership 漂移。 | `docs/plans/迁移/p23/README.md:50-51`; `cmd/mcp-orch/sqlc.yaml:4-15`; `sqlc.yaml:5-9` | README 要求 DAG 新表/查询只维护 `cmd/mcp-orch/sqlc.yaml`；当前 cmd 配置确实指向 `cmd/mcp-orch/sql/queries`，但根 sqlc 也加载了 DAG watcher migration。 | P23 新 DAG migrations/queries 只进 `cmd/mcp-orch/sqlc.yaml`；同时评估根 sqlc 是否应移除 DAG watcher schema 或加 ownership 注释/guard，防止生成面漂移。 |
| wakeup 状态机 DDL 没有 CHECK，且文档要求的 `claimed/sent/acked/expired` 与当前 `dispatching/sent/failed` 不一致。 | `migrations/0023_dag_watcher_phase1.sql:17-29`; `cmd/mcp-orch/sql/queries/task_dag_wakeup_dispatch.sql:6-18`; `docs/plans/迁移/p23/P0_DAGRuntimeSkeleton.md:69` | DDL `status TEXT DEFAULT 'pending'` 无 CHECK；SQL 使用 `dispatching/sent/failed`；P0 待办要求补状态机、TTL、reclaim、FK/归档。 | 冻结 wakeup 状态枚举；迁移或兼容 `dispatching` 命名；加 CHECK、TTL/expired、claim owner/expires 统一字段。 |
| P0 文档要求 `assigned_agent_id`，现有 schema/store 只有 `assigned_to`，语义不同。 | `docs/plans/迁移/p23/README.md:193`; `docs/plans/迁移/p23/P0_DAGRuntimeSkeleton.md:37-39`; `migrations/0004_ack_dag.sql:57`; `cmd/mcp-orch/store/taskdag/contract.go:172-190` | `assigned_to` 是预分配/展示字段；P1 要 dispatcher 写 `assigned_agent_id` 且不可覆盖。当前没有该列。 | P0 DDL 新增 `assigned_agent_id`，不要复用 `assigned_to`；绑定 launcher 成功后同事务或可恢复协议写入。 |
| `last_event_at` 与 P23 `last_activity_at` 语义冲突风险。 | `migrations/0023_dag_watcher_phase1.sql:1-4`; `docs/plans/迁移/p23/README.md:108-110`; `docs/plans/迁移/p23/P7_LivenessProbe.md:76-94` | 现有列叫 `last_event_at`；P23 冻结 `last_activity_at` 并要求 P7 活性语义独立。P0 文档也提示不要混用。 | P0 明确新增 `last_activity_at`，或迁移改名并冻结语义；P7 需要 `activity_state/tool_call_id/next_probe_at` 等 fenced 字段。 |
| DAG create 仍是单事务逐 node upsert + 全量 load，P9 N>50 async/batch 未具备。 | `cmd/mcp-orch/orchestration/dag.go:109-126`; `cmd/mcp-orch/orchestration/dag.go:202-220`; `docs/plans/迁移/p23/P9_ScaleScheduling.md:96-107` | 当前 create 在一个事务内循环 `UpsertNode`，之后 `ListNodes` 全量返回。P9 要 N>50 async job、批量拓扑校验、cursor progress。 | P9 前设计 batch create job/idempotency hash；detail 默认分页和 `include_result=false`。 |
| DAG/list 查询无 tenant filter，与 P3/P6 多租户目标冲突。 | `cmd/mcp-orch/sql/queries/task_dag_dag.sql:13-22`; `docs/plans/迁移/p23/COMPLIANCE_GATES.md:135`; `docs/plans/迁移/p23/P3_ExplicitDAGStartAndOwnership.md:38-42` | 当前 list 只按 status/keyword；COMPLIANCE 要 DAG/template/audit 查询 RPC 必带 tenant_id filter。 | P3 加 `tenant_id` 后，所有 list/get/update/start query 都必须带 tenant/caller scope filter。 |

## Low

| 问题 | 文件:行 | 证据 | 建议 |
|---|---:|---|---|
| `task_dag_nodes` 在 baseline sqlc schema 中看不到唯一约束，真实历史 migration 有唯一约束，schema 来源不一致可能影响 sqlc/测试事实判断。 | `migrations/001_baseline.sql:186-195`; `migrations/0004_ack_dag.sql:51-68`; `cmd/mcp-orch/sqlc.yaml:5-9` | `0004` 定义 `UNIQUE (dag_key,node_key)`；`001_baseline` 片段没有显示该约束。cmd sqlc 使用 baseline + 0023。 | 校验 baseline 是否完整包含约束；若没有，修复 baseline 或 cmd sqlc schema 输入，避免 sqlc 与实际 DB 漂移。 |
| `command_ref` 保留但不执行的事实与 README 一致，需防止 P23 runtime 误用。 | `docs/plans/迁移/p23/README.md:54`; `cmd/mcp-orch/orchestration/dag.go:223-233`; `migrations/0004_ack_dag.sql:60` | node create 会持久化 `command_ref`，但 README 明确本期 launch source of truth 是 `nodes[].launch`。 | P0/P1 schema 增加 `nodes[].launch` 后，runtime 不应读取 `command_ref` 发起执行；可加 archtest/grep guard。 |
| P13 DDL 使用 `CREATE INDEX CONCURRENTLY` 拆 no-tx 文件，但主表 DDL 和索引编号都写 `0072`，需与 migration runner 编号规则协调。 | `docs/plans/迁移/p23/P13_StrictJSONOutput.md:90-95`; `docs/plans/迁移/p23/P13_StrictJSONOutput.md:128-136`; `docs/plans/迁移/p23/P9_ScaleScheduling.md:90` | 文档要求 no-transaction migration 禁 `BEGIN/COMMIT`；但 P13 主 DDL 与 index no-tx 同属 0072 口径，实际文件编号可能冲突。 | 为每个 no-tx index migration 分配独立编号/后缀规则，并让 migration sequence guard 允许 no-tx 拆分。 |

## 最终判定

**BLOCK**

阻塞理由：P23 文档的 migration 编号基线已与仓库事实冲突；当前 SQL/store 仍存在裸 status 写、terminal 无 `active_turn_id` fence、ready claim 无 `SKIP LOCKED LIMIT K`、wakeup id 返回错误、FK/GC/CHECK/tenant 基础 DDL 缺口。阶段 0 必须先修正文档与 gate，再允许 P0/P1 实现进入。