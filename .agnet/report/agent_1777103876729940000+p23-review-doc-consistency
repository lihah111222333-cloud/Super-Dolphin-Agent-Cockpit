只读审查完成；未修改文件。结论：**BLOCK**。核心原因是 P23 migration 编号已经与当前仓库事实冲突，且 README / stub / COMPLIANCE 里仍把冲突编号当作冻结前提。

## High

| 严重度 | 文件:行 | 证据 | 建议 |
|---|---:|---|---|
| High | `docs/plans/迁移/p23/README.md:79-84`；`docs/plans/迁移/p23/COMPLIANCE_GATES.md:75,239,243`；`docs/plans/迁移/p23/P0_DAGRuntimeSkeleton.md:31,37,73,75` | README 声称“当前最大已占用编号是 `0062`”，P23 从 `0063` 起，且 `0064` unused；但仓库事实是 `migrations/0063_agent_thread_name.sql` 和 `migrations/0064_skill_candidates.sql` 已存在。COMPLIANCE 还把 `0063–0072` / `0064 unused` 写成 gate。 | 立即重排 P23 全部 migration 编号并同步 README、COMPLIANCE、P0/P3/P5/P8-P13 stub、RESEARCH_VERDICT 裁决残留；`0064 unused` 规则必须删除或改成当前真实编号之后的 buffer。此项不修不能派单。 |
| High | `docs/plans/迁移/p23/README.md:164-172` vs `README.md:132,374,376` | 依赖拓扑写 `P13 financial swarm preset ─► P12`，箭头语义表示 P13 先于 P12；但同文档明确“金融 swarm preset 另依赖 P12 / hard depends on P12”。 | 拓扑改成 `P12 ─► P13 financial swarm preset`，并注明非 swarm JSON validation 不依赖 P12。 |
| High | `docs/plans/迁移/p23/P2_NodeTerminalReconcile.md:32` vs `docs/plans/迁移/p23/P13_StrictJSONOutput.md:141-144` | P2 要求“带 `output_schema` 的 node 必须先转入 P13 `outputValidationActor`”；但 P13 依赖 P0/P1/P2 全部合入。形成 P2 ↔ P13 语义循环。 | P2 只能落 durable terminal event / extension hook；`output_schema` 分支应 feature-gate 到 P13，P2 不得阻塞依赖尚未存在的 actor。 |
| High | `docs/plans/迁移/p23/README.md:284,288-289` vs `README.md:126-132,164-172,290-376` | README 一处说“不实现活性探针 / 校验闭环 / 大规模能力”“不在 P23 范围内”，但路线图和 P7-P13 stub 又把这些列为 P23 后段子任务。 | 改成“P0-P6 前段不实现；P7-P13 后段承接”，不要写“不在 P23 范围内”。 |
| High | `docs/plans/迁移/p23/P8_VerificationGate.md:23-24`；`docs/plans/迁移/p23/RESEARCH_VERDICT.md:62-63` | 文档称 `service.go:323-325`、`service_launcher_bridge.go:277-290` 是“同 agent 再投 turn”接口；仓库事实：`cmd/mcp-orch/orchestration/service.go:323-325` 是遍历 `s.agents`，`service_launcher_bridge.go:277-290` 是 `stopAgentViaLauncher`。行锚已漂移/语义错误。 | 重新 grep 当前同 agent submit / resume turn 真实入口，替换 P8 和 RESEARCH_VERDICT 的锚点；否则 P8 owner 会从错误入口开工。 |

## Medium

| 严重度 | 文件:行 | 证据 | 建议 |
|---|---:|---|---|
| Medium | `docs/plans/迁移/p23/COMPLIANCE_GATES.md:3,31,105,124,162,243` vs `README.md:177-178,263-264` | COMPLIANCE 顶部说 P0 启动前需 L1+L3+L4 骨架；第 31 行又说 L4 P0 合入前；第 105/243 行允许 CI manual fallback；README 也说 CI 不存在、可 fallback。P0 启动/合入前置口径不一致。仓库事实：`.github/workflows` 不存在。 | 明确一个口径：P0 前必须有“CI”还是“manual hard fallback”即可；若 fallback 合法，应把 L4 从 hard CI 改成 hard CI-or-fallback，并列出必贴命令。 |
| Medium | `docs/plans/迁移/p23/COMPLIANCE_GATES.md:56-82` | 21 项 archtest 是“唯一权威清单”，但当前 `internal/archtest` 中这些 P23 专用文件如 `dag_runtime_test.go`、`dag_state_test.go`、`dag_verify_test.go` 等均不存在。文档虽标“未实施”，但 README `README.md:229` 又写“P23 必落 21 项 archtest”。 | 在 COMPLIANCE 表增加“planned / existing”列，或在阶段 0 checklist 明确最小必须先落哪些测试文件，避免 owner 误以为清单已经存在。 |
| Medium | `docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:31-33,72-91` | P6 AuthN 写“API key / mTLS / JWT 三选一（v1 推荐 API key）”，但 endpoint matrix 已经要求三入口统一 identity adapter + service enforcement。AuthN 方案未冻结，安全派单仍需 owner 决策。 | P6 开工前冻结 v1 AuthN 方案、caller identity schema、配置字段、错误码；否则 P6 不是可直接实现的 stub。 |
| Medium | `docs/plans/迁移/p23/P1_WakeupDispatcherAndLaunchBinding.md:25-27`、`P3_ExplicitDAGStartAndOwnership.md:25-27`、`P4_HostAgentTriggerSurface.md:23-25`、`P5_CronTriggerSurface.md:24-26`、`P6_ExternalRPCTriggerSurface.md:26-28`、`P7_LivenessProbe.md:31-33`、`P9_ScaleScheduling.md:31-33` | 多个 stub 的“改动清单”仍是 `_待补_` 表，虽然下方有关键方向，但缺精确落点/写集边界。 | 将 `_待补_` 表补成文件级写集和接口级输出。否则并行派单容易冲突，尤其 P1/P2/P3/P6/P9。 |
| Medium | `docs/plans/迁移/p23/README.md:50-51` vs `README.md:32-34,192,246` | README 基线明确 DAG 走 `cmd/mcp-orch/sql/queries` / `cmd/mcp-orch/store`，但多处锚点写成 `sql/queries/...`、`store/taskdag/...`。仓库事实对应文件在 `cmd/mcp-orch/sql/queries/...`、`cmd/mcp-orch/store/taskdag/...`。 | 全文统一成真实路径，避免 grep/派单锚点失效。 |
| Medium | `docs/plans/迁移/p23/P7_LivenessProbe.md:19`、`RESEARCH_VERDICT.md:47` | 引用 `event_relay.go:64-86` 未带真实路径；仓库真实文件是 `internal/platform/hooks/event_relay.go`。 | 补全真实路径；顺手复核行号是否仍对应 final-answer relay。 |

## Low

| 严重度 | 文件:行 | 证据 | 建议 |
|---|---:|---|---|
| Low | `docs/plans/迁移/p23/P10_TemplateAndUI.md:183` vs `README.md:332-342` | P10 stub 说 README 的 P10 章节“待 owner 启动前由 README 同步追加”，但 README 已有 P10 章节。 | 删除 stale 提示，改成“见 README §P10”。 |
| Low | `docs/plans/迁移/p23/README.md:397` | README 仍给“约 8-12 工程日 / 2-3 周”估时；当前文档已扩展到 P13、21 项 archtest、CI/metrics/audit/hash-chain 等硬门，估时容易误导。 | 若保留估时，注明仅覆盖 P0-P6 或删除估时。 |
| Low | `docs/plans/迁移/p23/README.md:4` vs 多个 stub `authoritative` 头 | README 写“当前 authoritative 文档：README.md”，COMPLIANCE 又写本文件 + README + RESEARCH_VERDICT authoritative，多个 stub 也写三者 authoritative。 | README 顶部改成“入口 authoritative；执行细节以各 stub / COMPLIANCE 对应章节为准”，避免权威层级歧义。 |

## Stub 可派单密度判断

- **P8/P10/P11/P12/P13**：密度相对高，已有 DDL 草案、风险、必测项，但受 migration 编号 BLOCK 影响，不能直接开。
- **P1/P2/P3/P4/P5/P6/P7/P9**：方向足够，但文件级写集仍多处 `_待补_`；P2/P13 循环依赖、P6 AuthN 方案未冻结、P9 容量数字未定会影响派单。
- **COMPLIANCE_GATES**：权威清单完整，但“设计未实施 / 必须 hard gate / fallback 可用”三种口径混在一起，需要拆清楚 existing vs planned vs required-before-P0。

## 最终结论

**BLOCK**

必须先修：
1. migration 编号与当前 `migrations/0063_*` / `0064_*` 冲突；
2. P13 ↔ P12 拓扑箭头；
3. P2 ↔ P13 循环依赖；
4. P23 范围口径；
5. P8 同 agent re-turn 错误行锚。