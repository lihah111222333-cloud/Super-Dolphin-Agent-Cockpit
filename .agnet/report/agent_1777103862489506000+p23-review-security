审查完成：只读审查，未修改文件。结论：**BLOCK**。

P23 文档已经覆盖了多数安全主题（AuthN/AuthZ、tenant、rate/quota/audit、append-only/hash chain、PII、prompt injection），但仍有 2 个 High 会导致同一 localhost/RPC 暴露面绕过 DAG 安全边界，必须修正文档口径后才能 PASS。

| 等级 | 文件:行 | 代码证据 | 影响 | 修复建议 |
|---|---|---|---|---|
| **High** | `docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:72-83`；`docs/plans/迁移/p23/README.md:46` | 同一 RPC facade 不只暴露 `task/*`。`cmd/mcp-orch/orchestration/rpc.go:77-99` 暴露 `agent.launch` / `agent.submit` / `agent.submitPrompt`，`rpc.go:100-120` 暴露 `agent.stop/list/snapshot/getState/getReport`；TCP server 直接 `net.Listen` 并接入完整 method map：`internal/platform/rpc/server.go:291-299,331-333`；Wails WS 也接入同一 server：`internal/ui/wails/http_server.go:39-42`。 | P6 endpoint matrix 只守 DAG / node 方法，会让外部/本地 caller 直接走 `agent.launch` 或 submit 绕过 DAG owner/tenant/rate/quota/audit，相当于“越权 spawn / 执行”旁路。即使 `dag/spawn` 被 `SpawnChildNodes` 保护，攻击者仍可直接 launch agent。 | P6 必须把**同一 transport 上所有写 RPC**纳入 guard matrix，至少包括 `agent.launch`、`agent.submit*`、`agent.stop`、`orchestration.reportRuntime`、report event 类。若这些不属于 P23，应明确在 P6 前禁用/隔离外部 transport 或做 service-level global AuthN/AuthZ/rate/quota/audit。`dag_external_rpc_guard` archtest 不应只检查 `task/*`。 |
| **High** | `docs/plans/迁移/p23/P4_HostAgentTriggerSurface.md:27-30`；`docs/plans/迁移/p23/P3_ExplicitDAGStartAndOwnership.md:60` | 当前 MCP tool schema 要求 caller 自报 `agent_id`：`cmd/mcp-orch/tools/task_tools.go:111,141,147,171`，并把它直接写成 `CreatedBy`；MCP server `tools/call` 直接 `s.callTool(ctx, ...)`，没有 caller identity 注入/鉴权：`internal/mcpserver/common/server.go:221-264`。 | P4 的 `auto_start` 若沿用当前 `agent_id` 输入作为 owner，会允许 agent/调用方伪造 owner，创建并自动启动他人 DAG。P3 虽写“缺 caller_identity 返错”，但 P4 没明确禁止信任参数里的 `agent_id`。 | P4 必须显式继承 P3/P6：`auto_start` 的 `triggerMeta.caller_identity` 只能来自 authenticated MCP context，`agent_id` 参数只能兼容/显示，不可作为 owner/tenant 授权来源；创建 + auto_start 必须同一 service guard，缺 identity hard fail 并 audit。 |
| **Medium** | `docs/plans/迁移/p23/P3_ExplicitDAGStartAndOwnership.md:38-41`；`docs/plans/迁移/p23/README.md:244-245` | 当前表只有 `created_by`，无 tenant：`migrations/0004_ack_dag.sql:33-40`；当前 DTO/store 也只有 `CreatedBy`：`cmd/mcp-orch/store/taskdag/contract.go:158-165`。P3 草案把 `tenant_id` 设为 `TEXT NOT NULL DEFAULT ''`。 | 空 tenant 默认值容易在新建/迁移窗口形成“全局空租户”，后续 tenant filter 若漏判空值，会退化为跨租户读写。 | 迁移可临时 default，但文档要冻结硬规则：新 strict DAG `tenant_id` 必须来自 authenticated caller，不允许空；加 `CHECK (tenant_id <> '')` 的分阶段 rollout / postcheck；read/write guard 遇空 tenant fail-closed，旧数据只读兼容并有 backfill 完成门。 |
| **Medium** | `docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:41-43`；`docs/plans/迁移/p23/COMPLIANCE_GATES.md:77,132` | 当前仓库没有 `dag_audit_log` 实现，搜索仅见文档；P6 只列 `prev_hash,row_hash,hash_alg,chain_scope` 字段。 | append-only/hash chain 口径有字段但缺“如何算/如何串/如何防并发断链”的硬契约。实现时可能出现可重排、可分叉、并发 prev_hash 冲突或 hash 不覆盖关键字段。 | P6/COMPLIANCE 补充：row canonicalization（如 JCS/RFC8785 或等价规范）、row_hash 覆盖字段清单、`chain_scope` 粒度（tenant/method/table）、每 scope 串行化策略（advisory lock/serializable/head table）、禁止 UPDATE/DELETE 的 DB 权限/trigger、spool 与主表 hash 关系、postcheck SQL。 |
| **Medium** | `docs/plans/迁移/p23/README.md:241`；`docs/plans/迁移/p23/P6_ExternalRPCTriggerSurface.md:87` | Wails WS 默认 upgrader 未配置 origin 检查：`internal/platform/rpc/transport_ws.go:16,26-39`；Wails 对 `/wails/ws` 直接挂 RPC：`internal/ui/wails/http_server.go:39-42`；TCP JSON-RPC 默认 localhost：`internal/platform/config/config.go:43-47`。 | README 说 `127.0.0.1` 未 opt-in `0.0.0.0` 前“不算对外服务”，容易低估浏览器到 localhost、恶意本地进程、CSRF/WebSocket origin 风险。P6 虽写 Origin/CSRF/session/API key，但 README 风险口径不够硬。 | 改为“不是公网服务，但仍是 untrusted local attack surface”。P6 前 hard 要求 localhost 也走 auth/session/API key；WS 必须 Origin allowlist + CSRF/session token；HTTP MCP 明确 CORS deny/default deny；TCP 本地调用也必须身份化。 |
| **Low** | `docs/plans/迁移/p23/P12_SwarmArbiter.md:106,167,172-174` | P12 consensus DDL 含 `dissent_summary JSONB`：`P12_SwarmArbiter.md:106`；只在待办写 member input/output 走 redactor：`P12_SwarmArbiter.md:167`。 | `dissent_summary` / `aggregation_reason` 可能包含 verifier/arbiter 输出中的 PII 或 prompt-injection 文本；目前红线写得不如 P13 的 raw/redacted 双路径硬。 | P12 补硬契约：`dissent_summary` 只能是 bounded + redacted + quoted data；记录 `dissent_summary_hash` / redaction version；禁止 raw member output 进入 consensus 表；`repair_with_dissent_summary` 已写入 repair prompt 前 redactor，这点应同步到 DDL 字段说明。 |

### 已确认覆盖较好的点

- `task/node/update` 裸 status/CAS/fence 风险已在 README 与 P6 matrix 明确点名：`README.md:246`、`P6_ExternalRPCTriggerSurface.md:81`，且代码事实成立：`cmd/mcp-orch/sql/queries/task_dag_node_write.sql:14-20` 无 expected status。
- Prompt injection 在 P8/P12/P13 有明确方向：P8 sanitize layer、quoted data、JSON schema 强校验；P12 每个 member sanitize；P13 repair prompt 复用 sanitize。主要缺口是 P12 redacted summary 的字段级硬化。
- PII redaction gate 已进入 `COMPLIANCE_GATES.md:80-82,132-136`，但 P12 字段说明需要补强。
- P6 已明确 service 层统一执行 AuthN/AuthZ/tenant/rate/quota/audit，而不是只靠 transport middleware：`P6_ExternalRPCTriggerSurface.md:85-87`。问题是覆盖面应扩大到同 transport 上的非 DAG 写 RPC。

**最终结论：BLOCK**

阻塞原因：High #1 和 High #2 都是可绕过 owner/tenant/spawn 授权边界的问题。修复文档口径后可降为 NEEDS-FIX/PASS 复审。