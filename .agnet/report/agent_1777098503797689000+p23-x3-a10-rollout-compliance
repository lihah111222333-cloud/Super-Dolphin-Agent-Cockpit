# last_report — 第五轮 a10 rollout/compliance 复审

✅ **阶段 0 checklist 已基本收口**  
- 证据：`README.md:75-114` 列 5 项前置冻结；`README.md:176` 明确“阶段 0 最小 checklist…+ compliance checklist”；`COMPLIANCE_GATES.md:237-239` 给 P0 前最小清单。  
- 建议：可接受；后续实现 PR 直接引用 `COMPLIANCE_GATES.md:237-239` 作为 merge checklist。

⚠️ **COMPLIANCE 21 archtest 仍有“双表”残影**  
- 证据：`COMPLIANCE_GATES.md:56-82` 已有单一 authoritative 21 表；但 README 仍完整列 21 项 `README.md:228-252`，且 COMPLIANCE 自称“README 只摘要展示” `COMPLIANCE_GATES.md:58`。  
- 建议：README 改成指针 + 高层摘要，删除/折叠完整 21 行，避免执行清单双源。

✅ **manual hard fallback 已硬化**  
- 证据：`COMPLIANCE_GATES.md:105` 要 PR 模板贴 `make guard`、archtest、`make sqlc-verify` 输出，reviewer 签收，缺项禁止 merge。  
- 建议：补 PR 模板文件名/路径即可；口径已满足 hard fallback。

✅ **migration checklist 已变 merge blocker**  
- 证据：`COMPLIANCE_GATES.md:233-235` 明确缺任一项不得 merge migration PR。  
- 建议：把该 checklist 加到 PR 模板；`dag_migration_sequence_guard` 已含 no-tx concurrent index 分拆 `COMPLIANCE_GATES.md:75`。

✅ **L6/P7 hard gate 已成立**  
- 证据：L6 标注 “P7 前 hard；P0 可 fallback” `COMPLIANCE_GATES.md:28`；P7 前无 `/metrics` 且无可执行 fallback 则 P7–P13 freeze `COMPLIANCE_GATES.md:160-162`。  
- 建议：补 scheduled-audit fallback 脚本名、artifact 路径、owner，否则实现时仍不可验收。

⚠️ **cost approval gate 分散，缺中央 hard gate**  
- 证据：P9 只写 `cost_preview` + budget hard stop `P9_ScaleScheduling.md:69`；P10 UI 写超过预算需 approval/hard block `P10_TemplateAndUI.md:193`；P12 只要求成本映射/估算字段 `P12_SwarmArbiter.md:165,171`。  
- 建议：在 COMPLIANCE 增 “cost approval gate” 条款：超过 tenant/subscription/月度阈值、provider capacity 不足、ROI 未填时，P11/P12/P13 Start/Spawn/Swarm 必须 hard block，不只 UI 阻断。

❌ **后段依赖拓扑仍有 P13/P12 不一致**  
- 证据：路线表已写 P13 金融 swarm preset 另依赖 P12 `README.md:132`；但拓扑仍写 `P0+P1+P2+P8 sanitize+P10 preset -> P13`，未含 P12 `README.md:171`；后文也写金融 preset `unanimous swarm`，依赖却仅 P0/P1/P2+P8+P10 `README.md:393-395`。P13 子文档已正确写“启用金融 swarm preset 依赖 P12” `P13_StrictJSONOutput.md:141-144`。  
- 建议：README 拓扑和 P13 段同步加 “金融 swarm preset → P12 hard dependency；非 swarm JSON validation 可不依赖 P12”。

✅ **P0/P9 rollout ownership 已修正**  
- 证据：P9 明确 SKIP LOCKED 首版由 P0/P1 落，P9 只调参；未落则阻塞 `P9_ScaleScheduling.md:87`。  
- 建议：保持；P0 PR 必测 ready claim 小批锁。