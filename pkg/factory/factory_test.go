package factory_test

import "testing"

// TestFactoryPackageExists is a smoke test to verify the factory package compiles.
func TestFactoryPackageExists(t *testing.T) {
	// Zone A: cross-package DRY primitives
	// This test will be replaced with real tests as code is migrated.
	t.Log("pkg/factory package exists and compiles")
}
